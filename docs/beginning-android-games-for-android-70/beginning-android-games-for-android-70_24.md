# O

- **OpenCore**
- **OpenGL ES**
  - **2D 变换**
    - `Bob class`
    - `combining transformations`
    - `matrices`
    - `moving bobs`
    - `rotation`
    - `scaling`
    - `translation`
    - `world and model space`
  - **3D 网格**
    - `createCube()` 方法
    - `cube and corner points`
    - `CubeTest` 类
    - `present()` 方法
    - `resume()` 方法
    - `spinning texture cube`
    - `texture coordinates`
    - `update()` 方法
  - **3D 顶点**
    - `coordinate systems`
    - `positions`
    - `present()` 方法
    - `right-hand rule`
    - `Vertices3Test`
  - **相机系统**
    - `attributes`
    - `Euler rotation camera`
    - `gimbal lock`
    - `GLU.glLookAt()` 方法
    - `present()` 方法
  - **C 头文件**
  - **坐标系**
  - **帧率测量**
  - **`Game.getStartScreen()`**
  - **GLGame**
    - `getGLGraphics()` 方法
    - `getters and setters`
    - `GLGraphics.java`
    - `onCreate()` 方法
    - `onPause()`
    - `onResume()` 方法
    - `onSurfaceChanged()`
    - `onSurfaceCreate()`
    - `Screen implementation`
    - `synchronization`
  - **GLGraphics 和 FloatBuffer**
  - **GLSurfaceView**
    - `color buffer`
    - `EGLConfig`
    - `glClearColor()`
    - `GLSurfaceViewTest.java`
    - `onCreate()` 方法
    - `onDrawFrame()`
    - `onPause()` / `onResume()`
    - `onResume()` 和 `onPause()` 方法
    - `onSurfaceChanged()`
    - `onSurfaceCreated()` 方法
  - **矩阵**
    - `identity`
    - `model-view`
    - `projection`
    - `texture`
  - **矩阵和变换**
    - `crate solar system`
    - `createCube()` 方法
    - `HierarchicalObject` 类
    - `hierarchical systems`
    - `matrix stack`
    - `model-view matrix`
    - `projection matrix`
    - `render()` 方法
    - `texture matrix`
    - `update()` 方法
  - **矩阵模式与活动矩阵**
  - **NIO 缓冲区**
  - **归一化设备空间**
  - **优化**
    - `batching`
    - `binding vertices`
    - `issues`
    - `rendering`
    - `state modifications`
  - **正交投影矩阵**
  - **透视投影**



## glFrustumf() 方法

## GLU.gluPerspective()

### 与正交投影的对比

### 参数

### 逐顶点颜色

### 图元

### 编程模型

### 投影矩阵

### 投影

### 渲染管线

### 发送顶点

#### 代码片段

### 三角形规格

### 版本

### 视口

## 深度缓冲

### 混合

### 深度测试或 Z 测试

### 深度值

### 精度与深度冲突

### ZBufferTest

## 开放手机联盟 (OHA)

