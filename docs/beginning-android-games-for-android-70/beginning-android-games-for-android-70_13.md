# 版本分布

## Android API

### Activity 生命周期

参见 `Activity` 生命周期

## `AndroidBasicsStarter` Activity

参见 `AndroidBasicsStarter` Activity

## 音频编程

参见 音频编程

### 文件处理

参见 文件处理

### 图形编程

参见 图形编程

### 输入设备处理

- 加速度计状态
- 按键事件
- 多点触控事件（参见 多点触控事件）
- 单点触控事件

## Android 应用

- API（参见 Android API）
- Google Play 过滤器
- 图标
- 清单文件（参见 清单文件）

## `AndroidBasicsStarter` Activity

- `ArrayAdapter` 构造函数
- `ListActivity`
- `onCreate()` 方法
- `onListItemClick()` 方法
- `setListAdapter()` 方法
- 测试 Activity

## Android 事件日志系统

## Android 游戏开发框架

- `AccelerometerHandler`
- `Activity` 类
- `AndroidAudio` 类
- `AndroidFastRenderView`
- `AndroidFileIO` 类
- `AndroidGame.setScreen()` 方法
- `AndroidMusic` 类，`MediaPlayer`
  - 构造函数
  - `dispose()` 方法
  - 实现
  - `isLooping()`、`isPlaying()` 和 `isStopped()`
  - `OnCompletionListener.onCompletion()`
  - `pause()` 方法
  - `play()` 方法
  - `setLooping()` 和 `setVolume()` 方法
  - `stop()` 方法
- `AndroidSound` 类
- 注释
- `CompassHandler`
- `getInput()`、`getFileIO()`、`getGraphics()` 和 `getAudio()` 方法
- `getStartScreen()` 方法
- 图形（参见 AndroidGraphics）
- `KeyboardHandler`（参见 KeyboardHandler）
- `onCreate()` 方法
- `onPause()` 方法
- `onResume()` 方法
- 攻击计划
- `Pool` 类
- 职责
- `setScreen()` 方法

## AndroidGraphics

- `AndroidFastRenderView`
- `AndroidGraphics.drawPixmap()`
- `AndroidPixmap`
- 人工帧缓冲区
- 宽高比
- `clear()` 方法
- 构造函数
- 密度
- `drawPixel()` 方法
- `drawPixmap()` 方法
- `drawRect()` 方法
- 实现，图形接口
- `newPixmap()` 方法
- Replica Island
- 屏幕尺寸与分辨率
- 目标分辨率

## Android NDK

- C/C++ 头文件与实现
- 定义
- Java 原生方法，创建
- 设置
- 共享库
- 测试，原生方法

## Android 机器人

## Android 软件开发工具包 (SDK)

- 组件类型
- 调试与分析
  - 断点设置
  - logcat
  - 实现
- 高级模拟器功能安装
- 安卓虚拟设备创建
- AVD 管理器窗口
- 设备连接
- HAXM
- 真机 vs. 模拟器
- JDK 设置
- 安装配置
- 软件

### Android Studio

- 断点
- 代码补全
- 调试
- 安装
- 项目
  - 应用代码
  - 创建
  - 探索

### Android Studio

参见 Android 软件开发工具包 (SDK)

### Android Studio 2 版本

## 安卓虚拟设备 (AVD)

## 应用程序编程接口 (API)

## 街机游戏

## `ARGB4444` 格式

## `ARGB8888` 格式

## ASCII 位图字体

## ASCII 字符

## `Assets` 类

## 音频编程

- 音乐流
- 音效
- 音量控制

## 音频质量

