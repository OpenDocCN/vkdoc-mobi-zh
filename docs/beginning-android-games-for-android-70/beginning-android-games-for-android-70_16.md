# D

## Dalvik 可执行文件 (DEX) 字节码格式

## Dalvik 虚拟机

## Defender

## 2D 图形编程

- 摄像机
  - `Camera2D` 类
  - `Camera2DScreen` 类
  - 视锥体
  - 缩放
- 碰撞（参见 碰撞检测）
- `GameDev2DStarter`

### 物理

- `CannonTest`
- 力与质量
- `getDeltaTime()`
- 数值欧拉积分
- 质点
- `present()` 方法
- 三角形加农炮
- `update()` 方法

### 精灵动画

- 动画类
- 洞穴人行走
- 关键帧
- 循环动画
- `present()` 方法
- `resume()` 方法
- `update()` 方法

### `SpriteBatcher` 类

### 精灵批处理程序

- `batcher.endBatch()`
- `beginBatch()` 方法
- 定义


```markdown
#### drawSprite() 方法

#### endBatch() 方法

#### offerings（此处指项目/提供的内容）

#### scaling（缩放）

#### SpriteBatcher.drawSprite() 方法

#### SpriteBatcherTest

#### usage pattern（使用模式）

#### `vertices` 实例

#### sprites（精灵）

#### texture atlas（纹理图集）

#### arbitrarily sized images（任意尺寸的图像）

#### bitmap font（位图字体）

#### face-lifting operation（翻新操作）

#### `GL_LINEAR`

#### objects（对象）

#### `resume()` 方法

#### usage（用法）

#### vertex-creation（顶点创建）

#### texture regions（纹理区域）

#### trignometry（三角学）

#### `atan2` 函数

#### cosine and sine（余弦和正弦）

### vectors（向量）

#### `add()` 和 `sub()` 方法

#### ad hoc representation（临时表示）

#### `angle()` 方法

#### cannon touch event（炮弹触摸事件）

#### cannon triangle test（炮弹三角形碰撞检测）

#### class implementation（类实现）

#### `cpy()` 方法

#### direction and distance（方向和距离）

#### `len()` 方法

#### `mul()` 方法

#### normalizing（归一化）

#### `nor()` 方法

#### position（位置）

#### `present()` 方法

#### `rotate()` 方法

#### scalars（标量）

# semi-mathematical representaion（半数学表示）

#### `set()` 方法

#### static constants（静态常量）

#### unit vectors（单位向量）

#### `update()` 方法

#### velocity and acceleration（速度与加速度）

## Digital encoding colors（数字编码颜色）

## Discoverable by design（设计即可发现）

## Discovery Services（发现服务）

## 3D meshes（3D 网格）

### cube and corner points（立方体与角点）

## Donut（甜甜圈）

## Double-buffering（双缓冲）

## 3D programming（3D 编程）

### cameras（摄像机）

### handcrafted matrix（手动构建的矩阵）

### instances（实例）

### LookAtCamera

### collision detection and object representation（碰撞检测与对象表示）

#### bounding shapes（包围形状）

#### DynamicGameObject3D

#### real time collision（实时碰撞）

#### sphere class（球体类）

#### sphere overlap testing（球体重叠测试）

#### testing methods（测试方法）

### first-person camera（第一人称摄像机）

### SeeEuler camera（视点欧拉摄像机）

### initialising（初始化）

### loading models（加载模型）

#### exception handling（异常处理）

#### implementing OBJ loader（实现 OBJ 加载器）

#### normals and texture coordinates（法线与纹理坐标）

#### ObjLoader

#### plethora（大量）

#### subset（子集）

#### syntax（语法）

#### texture triangle（纹理三角形）

#### WaveFront OBJ format（WaveFront OBJ 格式）

### mipmapping（Mipmap）

### aliasing effects（锯齿效果）

### `canvas` 类

### chain（链）

### define（定义）

### texture class（纹理类）

### in OpenGL ES, lighting（OpenGL ES 中的光照）

#### Ambient lights（环境光）

#### color specification, source（颜色规范与光源）

#### cube, vertex（立方体与顶点）

#### directional lights（方向光）

#### enabling and disabling（启用与禁用）

#### gathering performances（收集性能数据）

#### material specification（材质规范）

#### normal specification（法线规范）

#### notifications（通知）

#### point lights（点光源）

#### reflections（反射）

#### render object（渲染对象）

#### source, material（光源与材质）

#### specular reflection（镜面反射）

#### sphere, vertex（球体与顶点）

#### spotlights（聚光灯）

#### syntax, source enabling（语法与光源启用）

#### types, material/light（类型：材质/光源）

#### version, normals（版本与法线）

#### vertex normals（顶点法线）

#### working process（工作流程）

### in physics（物理中的）

### JNI

### mass based model（基于质量的模型）

### pseudocode（伪代码）

### vectors（向量）

#### components（分量）

#### constructors and setters（构造方法与设置方法）

#### coordinates（坐标）

#### `dist()` 方法

#### `distSquared()` 方法

#### implementation（实现）

#### interpretations（解释）

#### java coding（Java 编码）

#### methods（方法）

#### OpenGL ES

#### Pythagorean equation（毕达哥拉斯方程/勾股定理）

#### `rotate()` 方法

#### statements（语句）

## Dragon, Fly!（龙，飞！）

# E

## Éclair

## Euler camera（欧拉摄像机）

### angles（角度）

### attributes（属性）

### disabling states（禁用状态）

### EUlerCameraScreen

### first person shooter（第一人称射击游戏）

### gesture processing（手势处理）

### grid formation（网格形成）

### initial setup（初始设置）

### operational methods（操作方法）

### point light（点光源）

### projection and model view matrix（投影矩阵与模型视图矩阵）

### SetMatrices() 方法

### SpriteBatcher

### stub methods（存根方法）

### transformation matrix（变换矩阵）

### UI rendering（UI 渲染）

# F

## File handling（文件处理）

### assets/directory（assets 目录）

### external storage（外部存储）

### SharedPreferences

## Firmware（固件）

## Fixed-time-step simulation（固定时间步长模拟）

## Font class（字体类）

## Fragmentation（碎片化）

## Framebuffer memory（帧缓冲内存）

## Frame rate–independent movement（帧率无关移动）

## FreeType

## Froyo

# G

## Game design（游戏设计）

### building blocks（构建模块）

### core mechanics（核心机制）

### mechanics（机制）

### screens（屏幕）

### sketching（草图绘制）

### snake（贪吃蛇）

### story and art style（故事与美术风格）

### transitions（过渡）

## Game development（游戏开发）

### alpha compositing（Alpha 合成）

### Android

### audio interface（音频接口）

### audio quality（音频质量）

### be-all and end-all solution（终极解决方案）

### blending（混合）

### color models（色彩模型）

### compression（压缩）

### entertainment factor（娱乐性）

### framebuffer（帧缓冲）

### framework（框架）

### Google Play application（Google Play 应用）

### graphics（图形）

### mechanics（机制）

### music interface（音乐接口）

### pixmap（像素图）

### primary colors（原色）

### requirements（需求）

### RGB color cube（RGB 颜色立方体）

### simulation code（模拟代码）

### sound interface（音效接口）

### tablet（平板电脑）

### three-minute songs（三分钟歌曲）

### WASD keys（WASD 按键）

## Game-mechanics item（游戏机制条目）

## Garbage collector (GC)（垃圾回收器）

## Gem Miner \: Dig Deeper（宝石矿工：深度挖掘）

## General Public License (GPL)（通用公共许可证）

## `generateLevel()` 方法

## Gingerbread

## `GL3DBasicsStarter`

## `glFrustumf()`

## Global Positioning System (GPS)（全球定位系统）

## `glPopMatrix()` 方法

## `glPushMatrix()` 方法

## GLScreen Class（GLScreen 类）

## `GLScreen.update()` 方法

## `gluPerspective()`

## Glyph（字形）

## Google

### Android

## Google I/O

### Google Play

### Open Source Project（开源项目）

## Google I/O

## Google Play filters（Google Play 过滤器）

## Graphics（图形）

### `Graphics.drawPixmap()` 方法

### `Graphics.newPixmap()` 方法

## Graphics processing units (GPUs)（图形处理单元）

## Graphics programming（图形编程）

### bitmaps（位图）

#### Canvas.drawBitmap

#### dispose（释放）

#### loading and examining（加载与检查）

#### test activity（测试活动）

### continuous rendering, surface view（持续渲染与 SurfaceView）

#### motivation（动机）

#### SurfaceCreation and validity（Surface 创建与有效性）

#### SurfaceHolder and locking（SurfaceHolder 与锁定）

#### test activity（测试活动）

### coordinate systems（坐标系）

### drawing methods（绘制方法）

### blending（混合）

### circles（圆形）

### lines（线条）

### pixels（像素）

### rectangles（矩形）

### testing（测试）

### going fullscreen（全屏显示）

### hardware-accelerated rendering（硬件加速渲染）

### rendering text（文本渲染）

#### alignment and boundaries（对齐与边界）

#### drawing text, fonts（绘制文本与字体）

#### loading fonts（加载字体）

#### test activity（测试活动）

### RenderView

### screen resolution（屏幕分辨率）

## Graphics software program（图形软件程序）

# H

## Half-size VGA (HVGA) screens（半尺寸 VGA 屏幕）

## Hardware Accelerated Execution Manager (HAXM)（硬件加速执行管理器）

## HelloWorldActivity.java

## Help screens（帮助屏幕）

## Hierarchical systems, matrix stack（层级系统与矩阵堆栈）

### crate solar system（创建太阳系）

### `glTranslatef()`

## HierarchicalObject Class（层级对象类）

### parent-child relationships（父子关系）

### `render()` 方法

## High-scores screen（高分屏幕）

## `hitPlatform()` 方法

## `hitSquirrel()`

## Honeycomb
```


