# E

## 相等运算符
`**.**equals()` 方法 `.equalsIgnoreCase()` 方法

## 事件处理
`activity_main.xml` 文件 按钮 视图 监听器对象 监听器对象 `MainActivity.java` 模型 `onClick()` 方法 `onLongClick()` 方法 项目详情 注册 `setOnClickListener` Toast 消息 用户交互

## 外部存储，优缺点

