# L

## 布局文件
`activity_main` 容器 部分 文本模式 视图对象 XML 文件

## 生命周期感知组件

## 生命周期观察者

## 生命周期所有者

## Linux 内核

## LiveData
`MainActivity` `RandomNumber` 使用 `ViewModel`

## 实时模板

## 本地化测试

## Logcat 工具窗口

## 逻辑运算符

## 逻辑错误

## 长按事件
`build.gradle` 文件 显示 Snackbar 模拟器 `id` 属性 `MainActivity.java` `onCreate` 方法 Toast 消息 `for-loop` 语句

