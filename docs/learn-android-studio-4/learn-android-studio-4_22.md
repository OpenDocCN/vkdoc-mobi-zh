# 索引 A

## 活动（Activity）
`AppCompatActivity` 类 组件 布局文件 参见 布局文件 `MainActivity.java` `addNumber()` 方法 Android 活动 应用程序 应用入口点 历史 Intent 操作系统

## Android 开发者工具 (ADT)
Android 安装包 (APK) Android 运行时 (ART) Android Studio (AS) 配置 创建 AVD 创建项目 硬件加速 设置 Android 虚拟设备 (AVD) `append()` 方法

## 应用发布
账户 APK 资源 构建 发布就绪配置 Google Play Google Play Console JKS 启动 Play Console 准备 材料 签名 APK 签名 Bundle 网站

## 算术运算符
数组 创建 定义 for 循环 `java.until` 管理 标量

## 赋值运算符

