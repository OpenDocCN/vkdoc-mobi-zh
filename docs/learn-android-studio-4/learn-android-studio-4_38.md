# R

## `RandomNumber` 类

## 恢复测试

## 重构

## `registerReceiver()` 方法

## 关系运算符

## Room
优势 组件 依赖项 Java 源文件 `MainActivity` `Runnables` `activity_main.xml` `AndroidManifest` 文件 应用的布局绑定类 `build.gradle` 类 工作线程 `fetchUserInfo` 接口 `java.lang.Thread` 类 `MainActivity` 对象 `RunBackground` 类 `runOnUiThread()` 方法

## 运行时错误

