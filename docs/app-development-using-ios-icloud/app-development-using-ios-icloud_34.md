# 索引 P

`popUpChat()` 函数 `popUpContact()` 函数 `popUpMarket()` 函数 `popUpProfile()` `popUpProfile` 函数 `presentPhotoOptions()` 函数

`Profile` 模态类 `class level object` 代码 `init` 方法 `NSObject` `save profile` 方法 `profileSave()`

推送通知 App Delegate 注册 聊天应用订阅, 设置

