# 索引 M

模态类 `business modal` `Businesstable` `chattable` `contact modal` `conversation` 一对一关系 `profile`

模型-视图-控制器 (MVC) 应用组件 功能 层级 用户体验

`modifyRecordsResultBlock`

多个 iCloud 表格 类定义 代码 保存业务资料 保存函数 保存操作 表格记录初始化

MVC 设计 `controller` 定义 示例代码 `Controller` `UIViewController` 类 `Model` `UIViewController` 类 `View` `UIViewController` 类 `model definition` `checkProfileExist` 代码 错误常量 iCloud 查询操作 `profile` 类 `profileExists` `recordID` `Swift` 结构 `UIKit` 对象 `view definition`

