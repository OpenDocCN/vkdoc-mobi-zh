# 索引 C

`cellForRowAt` 委托方法

`ChatDetailViewController` `cancel` 函数 代码 在屏幕上绘制对象 `handleTextChange()` 函数 `layout subview` 生命周期方法 生命周期方法 属性 `queryChats()` 函数 `removeChatDetailsConstraints()` 函数 运行应用 `save chat event` 方法 `scrollToLastRow()` 函数 `setup()` 函数 `text view event` 方法 UI 组件 UI 表格方法

`ChatViewController` 类属性 代码 显示 `chat button action` 方法 `draw screen` 方法 `getChatTables` `get last chat` 方法 生命周期方法 移除约束 `setup()` 函数 `tab bar` 方法 `table view` 方法 UI 组件

`checkProfileExist()` `CKModifyRecordsOperation` CloudKit `CKModifyRecordsOperation` 系统函数 `completion @escaping`

`Contact` 模态类 `class level variables` 代码 CRUD 函数 `get contacts` 方法 `getICloudUserID` `profileExists` `UIKit` 模块

`ContactViewController` 业务方法 `chat button action` 事件 类定义 代码 `draw screen` 生命周期方法 运行应用 `setup()` 方法 `tab bar` 方法 `table` 方法 `text field event` 方法 UI 元素

`Controller` 类 `class declaration` 代码 生命周期方法 `profile exit` 方法, 检查属性 `UIKit` 框架 视图控制器, 导航

`Conversation` 模态类 `chat table exists` 类变量 `cloud second` 方法, 保存 代码 `get conversation` 方法 `get last chat` `init` 方法 `NSObject` `save conversation` 订阅

`CreateProfileViewController` 业务对象 类定义 代码 `draw profile` 屏幕 图像增强函数, 类扩展 图像事件方法 生命周期方法 `run app` `save action` 方法 保存个人资料信息 `setup()` 方法 `tab bar` 方法 `table view` 方法 `text field event` 方法 UI 对象定义 `user’s documents` 目录

