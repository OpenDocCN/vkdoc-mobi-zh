# 展示器

展示器是 VIPER 架构的核心：

*   它充当不同组件之间的连接纽带。
*   它从视图接收用户交互，并包含处理这些交互的逻辑，以及向交互器请求信息。
*   它以简单数据结构的形式（而非实体）接收来自交互器的信息。
*   它不知道视图由哪些组件构成，但知道应该显示什么内容以及何时显示。
*   它负责以最高效的方式将信息传递给视图。
*   它通过路由器处理导航到其他屏幕的逻辑。

#### 实体

关于实体：

*   它们是简单的对象模型。
*   通常是数据结构（`struct`）。
*   唯一能与实体打交道的组件是交互器。

