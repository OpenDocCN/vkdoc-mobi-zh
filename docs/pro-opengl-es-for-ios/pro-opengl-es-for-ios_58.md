# H

`glClientActiveTexture()` 方法，186–187 87，197

- 概述，202– 207

`glColor()` 方法，196

- 太阳缓冲对象，208– 210

`glColor4f()` 方法，168– 169，1 73，183，195

- 镜头光晕，210– 218

`GL_COLOR_ATTACHMENT0_OES`，203– 204

- 反射表面，218– 225

`glColorMask()` 方法，177– 178

- 阴影，226–243

`glColorPointer()` 方法，179，1 82，198

- 斑点，229

`GL_COMBINE`，188，190 – 191，195

- 映射，227

`GL_COMPRESSED_RGBA_PVRTC_2BPPV1_IMG`，

- 投影，229– 243 159
- 体积，227– 228

`GL_COMPRESSED_RGBA_PVRTC_4BPPV1_IMG`，

- 视场 (FOV)，55，75 – 76 159
- 过滤，155–157 57

`GL_COMPRESSED_RGB_PVRTC_2BPPV1_IMG`，

`float m_FieldOfView` 变量，251 159

- 格式，颜色，297– 298

`GL_COMPRESSED_RGB_PVRTC_4BPPV1_IMG`，

- FOV（视场），55，75 – 76 159
- FPS（每秒帧数），294

`GL_CONSTANT_ATTENUATION`，112， 115

帧缓冲对象。*参见* FBO

`glCreateShader()` 方法，312

帧统计，304，306

`GL_DECAL`，188– 190

每秒帧数 (FPS)，294

`GL_DECR` 类型，221，228

菲涅尔反射定律，117

`glDeleteBuffer()` 方法，291

视锥体

`GL_DEPTH_ATTACHMENT_OES`，203– 204

- 创建，67– 69

`GL_DEPTH_STENCIL_ATTACHMENT`，204

- 定义，54– 55

`glDetachShader()` 方法，315– 316

`GL_DIFFUSE` 参数，98，106，114，120，124 – 125

## G

`glDisable()` 方法，292，295，3 03

`GL_DOT3_RGB`，191– 193， 195– 196

`gColor4f()` 方法，169

`GL_DOT3_RGBA`，191–192 92，196

- 几何体，用于 3D 世界，60–63 3

`glDrawArray()` 方法，171，20 7



`getPosition()` 方法，253, 256, 268

`glDrawArrays()` 方法，25, 66, 207, 209, 222

GL 实用工具库 (GLUT)，77

`glActiveTexture()` 方法，187, 195, 330–331

`glDrawElements()` 方法，66, 224, 232, 238–241

`GL_ADD`，188–191

`GL_ALPHA_TEST`，298

`glDrawTexfOES()` 方法，281–282

`GL_ALWAYS` 函数，220

`GL_DST_ALPHA`，176

`GL_AMBIENT` 参数，109, 114

`GL_DST_COLOR`，176

`GL_APPLE_texture_2D_limited_npot`

`GL_EMISSION`，110–111, 125, 158

`glEnable()` 方法，76, 171, 181–182, 186, 197–198

`GL_BACK`，65, 76, 83

`glBindBuffer()` 方法，290, 292–293

`glEnableClientState`，145, 147, 153, 164, 168, 181–182, 186, 197

`glBindTexture()` 方法，202, 206–207, 209, 215, 330

`glEnableClientState(GL_VERTEX_ARRAY)`

`GL_BLEND`，171, 182, 188–190

`glBlendEquation()` 方法，176, 178

`glEnable(GL_COLOR_MATERIAL)` 方法，111

`glBlendEquationSeparate()` 方法，176

`GLenum light`，112

`glBlendEquationSeparateOES()` 方法，177

`GLenum pname`，112

`glBlendFunc()` 方法，171–174, 176, 179, 182

`GLenum type` 文件，16, 311

`GL_EXT_blend_minmax`，176

`GL_LIGHT_MODEL_TWO_SIDE`，110, 120–121

`GLfloat`，98–99, 102, 106, 109, 114, 120, 123, 124, 125, 128

`GL_LINEAR`，153–154, 156–157

`glFlush()` 方法，303

`GL_LINEAR_ATTENUATION`，112, 115

`gl_FragColor`，317, 322–323, 328, 331

`GL_LINEAR_MIPMAP_LINEAR`，156

`GL_FRAGMENT_SHADER` 文件，312, 314

`GL_LINEAR_MIPMAP_NEAREST`，156

`GL_FRONT`，76

`GL_LINE_LOOP`，63



`GL_FRONT_AND_BACK`， 106–109， 111， 120， 124–125

`GL_LINES`， 63

`GL_LINE_STRIP`， 63， 88

`glFrontFace()` 方法， 110

`glLineWidth()` 方法， 283

`glFrustum()` 方法， 338

`glLinkProgram()` 方法， 312

`glFrustumf()` 方法， 67

`glLoadIdentity()` 方法， 206–207， 209， 215

`glGet*`， 95–96， 224， 232

`glGetError()` 方法， 298

`glMakeAwesomeLightsDude()` 方法， 96

`glGetFloatv()` 方法， 48， 265， 275， 277

`glMapBufferOES()` 方法， 290–291

`glGetIntegerv`， 185

`glMaterial*`， 108

`glGetLight`， 121

`glMaterialfv`， 106–107， 109， 111， 120–121， 124–126

`glGetLightfv()` 方法， 98

`glGetUniformLocation()` 方法， 315， 320， 330

`glMaterialfv()` 方法， 305

`GL_INCR` 类型， 221， 228

`glMatrixMode()` 方法， 55， 127

`GL_INTERPOLATE`， 191

`glMatrixMode(GL_MODELVIEW)` 方法， 55

`GL_INVERT` 类型， 221

`GL_MAX_COMBINED_TEXTURE_IMAGE_UNITS`， 185

`GLKBaseEffect` 类， 16， 18， 22， 24–25， 94

`GLKBaseEffects` 类， 333

`GL_MODELVIEW`， 55， 57， 65， 67， 83–84

`GL_KEEP` 类型， 220–221

`GL_MODULATE`， 187–188， 190–191， 195

`GLKEffectPropertyMaterial` 类， 333

`GL_NEAREST`， 153， 156–157

`GLKEffectPropertyTransform` 对象， 338

`GL_NEAREST_MIPMAP_NEAREST`， 156

`GLKEffects` 库， 333–334

`glNormal`， 122

`GLKit` 工具， 333–339

`glNormal3f`， 103

`GLKEffects` 库， 333–334

`glNormalPointer`， 102， 122

`GLKReflectionMap` 类， 334–339

`GL_OES_blend_equation_separate`， 176

概览， 49

`GL_OES_blend_func_separate`， 176

`GLKMatrix4MakePerspective`， 22， 24

`GL_OES_blend_subtract`， 176

`GLKQuaternion m_Quaternion`， 254

`GL_ONE`， 172–174， 176， 178–180， 182

`GLKReflectionMap` 类， 333–339

`GL_ONE_MINUS_SRC_ALPHA`， 172， 174， 176



GLKReflectionMapEffect, 336

GLKSkyboxEffect 类, 333

`GL_ONE_MINUS_SRC_COLOR`, 215, 224–225

GLKTexture, 143

`glOrthof()` 方法, 215–216, 261, 263

`GLKTextureInfo` 对象, 143–144, 146, 163, 337

`GL_POINTS`, 63, 88

`GLKTextureLoader()` 方法, 143, 146, 152, 159

`GL_POINT_SMOOTH`, 275–276

GLKTextureLoaderGenerateMipmaps, 152, 159

`glPolygonOffset()` 方法, 236

GLKVector3 对象, 255

`glPopMatrix`, 122, 125–127

`GLKVector3` 类型, 256

`gl_Position`, 316–317, 322, 327

GLKVertexAttribNormal, 18, 21

`GL_POSITION` 参数, 98, 106, 114, 120,

GLKVertexAttribPosition, 18, 21 124–125

GLKView 类, 20, 245, 285, 320, 333, 338

`GL_PREVIOUS`, 195–196

GLKViewController 对象, 24, 333

`GL_PRIMARY_COLOR`, 196

`glLight*`, 114, 121

`GL_PROJECTION`, 215–216

`glLightfv()` 方法, 98

`glPushMatrix`, 122, 125–126, 209, 216, 222, 224,

`glLightModel`, 121 234–235, 238

`glLightModelfv`, 109–110

[www.it-ebooks.info](http://www.it-ebooks.info)

■ **索引**

**345**

`GL_QUADRATIC_ATTENUATION`, 112, 115, 120,

`glVertexPoint()` 方法, 59 124

`GL_VERTEX_SHADER` 文件, 312, 314

`GL_REPLACE` 函数, 188, 191, 220–221

`glViewPort()` 方法, 245

`glRotate()` 方法, 54, 248, 307, 338

`GL_ZERO` 类型, 176, 221, 238

`glRotatef()` 方法, 64, 72

Gouraud 着色, 110, 119

`glRotatef()` 方法, 125, 127

GPU (图形处理单元), 289

`glRotatef()` 方法, 247, 255

`grazeDist` 值, 267–268, 270

`glRotation()` 方法, 126, 248

希腊字母标识符, 82

`glScale()` 方法, 72

`glScalef()` 方法, 64, 81

`glScalex()` 方法, 64

**H**

`glShadeModel`, 98, 106, 120, 122, 124

`glShadeModel()` 方法, 258, 266

`handlePanGesture()` 方法, 250–251

`GL_SMOOTH`, 258, 266, 275



`handlePinchGesture()` 方法, 250–251

`GL_SOURCE0_RGB`, 190

哈苏相机, 210

`GL_SPECULAR` 参数, 107, 114, 120, 124

赫德利缓冲对象, 125

概述, 202–207

`GL_SPOT_CUTOFF`, 113–115

太阳, 208–210

`GL_SPOT_DIRECTION`, 113

计算机图形学历史, 3–4

`GL_SPOT_DIRECTION`, 113–114

好莱坞的计算机图形学发展, 8–11

`GL_SPOT_EXPONENT`, 113–115

`GL_SRC_ALPHA`, 172, 174, 176, 182

齐次坐标, 37

`GL_SRC_COLOR`, 176

HSV 色环, 93

`glStencilOp()` 函数, 220–221, 228

`glTexCoordPointer()` 方法, 181, 186, 190, 197

**I, J**

`glTexEnvf()` 方法, 185, 187, 190, 195

图像格式, 142–143

`GL_TEXTURE_2D`, 143, 145–146, 153–154, 164–165

图像纹理, 136–137

`init()` 方法, 78, 84–85, 96, 128, 159–160, 163, 196

`GL_TEXTURE_CROP_RECT_OES`, 281–282

`GLTextureInfo`, 143

`initGeometry()` 方法, 84, 87, 96, 122–123, 246

`GL_TEXTURE_MAG_FILTER`, 143, 153–154

`GL_TEXTURE_MIN_FILTER`, 143, 153–154

`initLighting()` 方法, 98, 104–105, 109–110, 112, 119, 123

`GL_TEXTURE_WRAP_S`, 143, 146

`InitLighting()` 方法, 123

`GL_TEXTURE_WRAP_T`, 143, 146

`glTranlsation`, 126

`initWithAPI`, 17, 19

`glTranslate()` 方法, 233, 307, 338

OpenGL 分析器的仪器检查器, 234, 238, 267, 269

`glTranslatef()` 方法, 58, 66, 71, 209, 216, 224, 302–304

OpenGL 分析器的仪器检查器, 302–304

`glTranslation()` 方法, 207

`GL_TRIANGLE_FAN`, 63, 65



颜色强度，影响因素，118–119

`GL_TRIAN`GLES，63

干扰，92

`GL_TRIANGLE_ST`RIP，58, 63, 65， 83, 88

交错数据，295

`gluGetScreenLocation()` 方法，264–](#index_split_004.html#p267)266，

插值着色，110

269–270

逆变换，47–48

`gluLookAt()` 方法，254–255

`gluMultMatrixVector3f()` 方法，264–266

`glUnmapBufferOES()` 方法，291](#index_split_005.html#p294)

`gluProject()` 方法，264–](#index_split_004.html#p267)266

**K**

`glUseProgram()` 方法，25–](#index_split_000.html#p30)26， 318, 330

`kEAGLRenderingAPIOpenGLES1` 标志，56, 58, 85](#index_split_001.html#p60)

`GLUT`（GL 实用工具包），77

[www.it-ebooks.info](http://www.it-ebooks.info)

■ **索引**

**346**

**L**

变换的数学基础，33–49

二维变换，34–38

朗伯光照模型，115

旋转，34–37

镜头光晕，210–218, 259–270

缩放，37–38

`LensFlar`e 对象，214, 216

平移，34

`LensFlar`e.mm，263

三维变换，38–49

光照参数，114–115

`GLKit`，49](#index_split_001.html#p54)

光照

逆变换，47–48

与颜色，91–94

将物体投影到屏幕，43–47

颜色中的组成部分，94–95

四元数，49

创建并定位光源，95–104

`max()` 方法，317](#index_split_005.html#p320)

着色背后的数学，115–116

`maxSize`，144](#index_split_002.html#p147)

与其他材质，105–115

`m_Eyeposition` 类型，246, 254, 258, 267–268

环境光，109–110

`m_FieldOfView`，251–2](#index_split_004.html#p254)52， 268

衰减，112–113

缩小，143, 156

自发光材质，110–111

光照参数，114–115

`miniGLU`，254, 258, 264, 270, 27](#index_split_004.html#p257)3

Minnaert 模型，110

着色模型，110

mipmap，152–155, 297

镜面光照，107–108



MIT，计算机图形学开发，5–6

聚光灯，113–114

模型矩阵（`modelMatrix`），264–265

次级光源，119–122

模型视图（`modelview`），24，54–55，58，66，68
在太阳系模型中，122

模型视图矩阵（`modelViewMatrix`），41，48，238–239，337–338

镜面反射，116–119

模型视图投影矩阵（`modelViewProjectionMatrix`），315–316，321–322

衰减，118，326–327，331

理解的好处，119

3D 运动，249–259

颜色和强度的因素，118–119

`m_Pos`变量，256

轨道显示部分的折线图，303

`MSAA`（多重采样抗锯齿），285

`loadShaders()`方法，313–314，317，319–320

`m_Scale`变量，78，80，82，270，330

`m_Squash`，78，80，82

`loadTexture()`方法，143–144，146，159，163

`m_Texture0`，181–182，187，194，182，197，259–260

多颜色混合，178–179

`lookAtTarget()`方法，250，252–253，257

多重采样抗锯齿（`MSAA`），285

`multiTexture`，187

`multiTextureBumpMap()`方法，194

**M**

`MultiTextureBumpMap()`方法，194

放大，143，155

`multiTextureBumpMap()`方法，197–198

`main()`方法，309，316–317，321–323，326，328，331

`multTexture()`方法，196

映射
带凹凸映射，191–200
阴影映射，227

**N**

材质与光照，105–115
环境光照，109–110
衰减，112–113
自发光材质，110–111

`NewTek`，9

非模板像素，228

归一化向量，99

`normalMatrix`，315–317，321，326–327，331



光照参数, 114–115

`[nPtrs]` 值，102

着色模型，110

`NSData` 对象，282

镜面光照, 107–108

聚光灯，113–114

着色背后的数学原理，115–116

**O**

数学坐标, 51–52

`Objective-C` 对象，84

[www.it-ebooks.info](http://www.it-ebooks.info)

■ **索引**

**347**

`OGRE`，13

录制/停止按钮，302

`OpenGL`

目标 `Selector` 窗格，304

分析，14–29

`Track Display` 窗格，304

着色器，27–29

`VBOs`，289–299

在显示屏中展示场景，22–26

批处理，295–296

调整值，26

颜色格式，297–298

分析器，299–306

交错数据，295

上下文相关的设置，305

mipmaps，297

`Detailed` 窗格，305–306

精灵表，296–297

`Extended Detail` 窗格，305

纹理，296–297

`instrument inspector`，302–304

使用技巧，298–299

录制/停止按钮，302

`Performance Detective`，294–295, 299, 305

目标 `Selector` 窗格，304

透视投影, 44–45, 55

`Track Display` 窗格，304

`phiIdx`，79, 82

架构，29–31

`Phong` 着色，110

坐标，51–53

生理光学，93

`ES API`

流水线，着色, 308, 318

扩展，以及 `PVRTC`，157–159

[着色器]上的限制, 310, 318

以及纹理，137–141

着色器结构，309–310

`Game template`，3

两个立方体示例，308–309

一般讨论, 11

`Planet` 对象，78, 83–84, 88

灯光，94

`Planet.h` 文件，163

打开项目，2–3

`Planet.m` 文件，96, 99, 102, 127–128

`OpenGLCreateTexture.mm` 文件，260

点采样，156

`OpenGLCreateTexture:renderTextureInRect()` 

`pos` 向量，112



`[method, 269]`

`[positioning lights, 95]`[–104]

`[OpenGL Outlines, 272, 282]`

`[POT (power-of-two), 143]`[–144, 147, 158]

`[OpenGLSolarSystemController, 84]`[–85, 122]–[123]

`[PowerVR graphics, 158]`

`[OpenGLSolarSystem.h file, 97, 122]`

`[PowerVR Texture Compression (PVRTC)]`[, 157]–[159, 297]

`[OpenGLSolarSystemViewController, 129]`

`[opening OpenGL ES project, 2]`[–3]

`[prepareToDraw( ) method, 338]`

`[OpenSceneGraph, 13]`

`[primitive, 63]`

`[Oren-Nayer, 110]`

`[projecting object onto screen, 43]`[–47]

`[orthogonal, 99]`

`[projection shadows, 229]`[–243]

`[orthographic projection, 44, 55]`

`[PVRTC (PowerVR Texture Compression), 157]`[–159, 297]

`[outlines.plist file, 278]`

`[PVRTexTool, 158]`[–159]

**P**

`[painter's algorithm, 130]`

**Q**

`[parallel projection, 44]`

`[quaternions, 49, 248]`[–249]

`[parameters, light, 114]`[–115]

`[QuickDraw 3D, 12]`[–13]

`[pattern-matrix, 36]`

`[percentVisible, 267]`[–268, 270]

`[performance, 289]`[–306]

**R**

`[OpenGL analyzer, 299]`[–306]

`[context-sensitive settings, 305]`

`[rasterization, 30]`

`[Detailed pane, 305]`[–306]

`[Record/stop button, for OpenGL analyzer, 302]`

`[Extended Detail pane, 305]`

`[red, green, blue, and alpha (RGBA), 142]`

`[instrument inspector, 302]`[–304]

`[red-green-blue (RGB), 93]`

`[reflect( ) method, 327]`

`[www.it-ebooks.info](http://www.it-ebooks.info)`

`**索引**`

`**348**`

`[reflections, specular, 325]`[–328]

`[overview, 94]`

`[reflective surfaces, 218]`[–225]

`[shadow volume, 227]`[–228]

`[regions, clipping, 73]`[–75]

`[shadows, 226]`[–243]

`[render parameter, 260, 263, 267]`[–269, 276]

`[blob, 229]`

`[renderAtPoint( ) method, 279, 2]`[81]–[282]

`[mapping, 227]`

`[renderFlareAt( ) method, 263]`[–264]

`[projection, 229]`[–243]

`[rendering, FBOs, 201]`[–243]




`volumes`, 227–228

`Hedley`, 202–207

`shininess`, 108, 117

`lens flares`, 210–218

在`display`中显示场景, 22–26

`reflective surfaces`, 218–225

`sizeWithFont()`方法, 281

`shadows`, 226–243

`slerping`, 249

`rendering order`, 129

`so glScale`, 64

`renderStage()`方法, 221

太阳系示例, 77–90

`renderTextureAt()`方法, 261, 263

太阳系模型, 245–288

`renderToStencil()`方法, 219–220, 223, 225

3D 运动, 249–259

`Retina displays`, 245–246

镜头光晕效果, 259–270

RGB（红-绿-蓝）, 93

光照效果, 122

RGBA（红、绿、蓝、透明度）, 142

`quaternions`, 248–249

滚转、俯仰和偏航（RPY）, 42

`Retina display`, 245–246

旋转矩阵, 36

`stars`, 271–285

`rotations`, 34–37

`textures`, 159–165

RPY（滚转、俯仰和偏航）, 42

用户界面, 285–287

`SolarSystemController`, 266

镜面反射指数, 117

**S**

`specular lighting`, 107–108

`specular reflections`, 116–119, 325–328

`scaledX`值, 214–216

`attenuation`, 118

`scaledY`值, 214–216

理解其优势, 119

`scaling`, 37–38

颜色和强度中的因素, 118–119

场景，在显示器中显示, 22–26

`specularColorVarying`变量, 326–328, 331

屏幕，将物体投影到, 43–47

球面线性插值, 249

`secondary lights`, 119–122

旋转的 3D 物体, 69–73

`setClipping()`方法, 69, 75, 86, 154, 239–240

`spotlights`, 113–114

247, 252, 263, 266

`sprite sheets`, 278, 296–297


`setHoverPosition()` 方法，250–252，256

`squareColors`，57–58，66

`setHoverPostion()` 方法，256

`SS_FILLLIGHT1`，119–120，124

`setupGL()` 方法，59，318–319，330，336

`SS_FILLLIGHT2`，119–120，124

`shaders`，27–29，307–340

恒星，用于太阳系模型，271–285

地球夜景，318

太阳缓冲区对象，208–210

云层，329

反射表面，218–225

镜面反射，325–328

GLKit 工具，333–339

GLKEffects 库，333–334

GLKReflectionMap 类，334–339

**T**

管线，308–318

用于 OpenGL 分析器的目标选择器窗格，304

对着色器的限制，310–318

纹素，137，140，144，155–156

着色器结构，309–310

纹理混合，180–200

两个立方体示例，308–309

凹凸映射，191–200

着色

多重纹理，184–200

背后的数学原理，115–116

纹理组合器，184，191–192，197

光照模型，110

纹理环境，185，187

[www.it-ebooks.info](http://www.it-ebooks.info)

■ **索引**

**349**

纹理单元，184–185

**V**

`textureCoords`，145，147，149，151，153，190

微调数值，26

`textureCoords2`，190

`vanishDist` 值，267–268，270

纹理，133，166，296

VAO（顶点数组对象），21，318

弹跳方块示例，143–159

VBO（顶点缓冲对象），289–299

过滤，155–157

批处理，295–296

Mipmap，152–155

颜色格式，297–298

OpenGL ES API 扩展与 PVRTC，157–159

交错数据，295


