# 索引

## 数字和特殊符号

入射角, 95, 116–117

反射角, 116

## 字母 A

API 专家, 305

二维变换，数学基础, 34–38

API 统计, 306

旋转, 34–37

API（应用程序编程接口），

缩放, 37–38

OpenGL ES，与纹理, 137–141

平移, 34

`APPLE_texture_2D_limited_npot` 扩展，

好莱坞的 3D, 4

281

3D 运动, 249–259

应用程序编程接口（API），

3D 混合, 71

OpenGL ES，与纹理, 137–141

三维变换，数学基础, 38–49

衰减, 112–113, 118

GLKit, 49

逆变换, 47–48

将物体投影到屏幕, 43–47

## 字母 B

四元数, 49

背面消除, 65

3D 世界, 51–90

baseModelViewMatrix, 22–25

裁剪区域, 73–75

批处理, 295–296

坐标系

双线性采样, 156

眼睛坐标, 54

广告牌技术, 260

OpenGL 坐标, 51–53

二叉空间分割 (BSP), 130

面剔除, 76–77

混合, 167–200

视野 (FOV), 75–76

Alpha 混合, 167–179

视锥体

混合函数, 171–178

创建, 67–69

多色混合, 178–179

定义, 54–55

纹理混合, 180–200

几何体, 60–63




`mapping with bumps`, 191–200  
`simple demo of`, 56–59  
`multitexturing`, 184–200  
`solar system example`, 77–90  
`Blinn, Jim`, 7  
`spinning`, 69–73  
`Blinn-Phong model`, 110  
`blob shadows`, 229  
`bouncy square example`, 143–159  

## A  
`filtering`, 155–157  
`Adding interface`, 286  
`mipmaps`, 152–155  
`alpha`, 82  
OpenGL ES API extensions and PVRTC,  
`alpha blending`, 167–179  
`functions`, 178  
`BSP (binary space partitioning)`, 130  
`multicolor`, 178–179  
`buffer-matrix`, 36  
`ambient lighting`, 109–110  
`buffer objects`  
`anaglyph technique`, 4  
frame. *See* FBOs  
341  
**[www.it-ebooks.info](http://www.it-ebooks.info)**  

## C  
`CADisplayLink`, 287  
`Call Tree`, 306  

## D  
`depth sorting`, 129–130  
`Detailed pane`, for OpenGL analyzer, 305–306  
`Catmull, Ed`, 6–7  
`development of computer graphics`, 4–11  
`CGPoint m_PointerLocation`, 251  
in Hollywood, 8–11  
`Clark, Jim`, 7  
at MIT, 5–6  
`clipping, regions`, 73–75  
at University of Utah, 6–7  
`clouds`, over earth at night, 329  
`diffuse reflectance`, 115  
`CMYK mode`, 93–94  
`Direct3D`, 11–12  

## C (continued)  
`Carpenter, Loren`, 10  
`Cartesian coordinates`, 52  

## B (continued)  
`bumps, mapping with`, 191–200  
`cube map`, 334–339  
`cubeColors`, 61, 65–66  
`cubeVertices array`, 61, 63, 65–66  

## Index (continued)  
342  
Hedley  
`CoreGraphics`, 281  
`overview`, 202–207  
`creating lights`, 95–104  
`sun buffer objects`, 208–210  
`Crow, Frank`, 10  

## color  
`components in lighting`, 94–95  
`factors in`, 118–119  
`formats`, 297–298  
and light, 91–94  

## D (continued)  
`drawFrame method`, 48  

## displays  
`Retina`, 245–246  
showing scenes in, 22–26



`drawInRect()` 方法, 58, 69, 85, 147, 179, 186

`color.glLightfv()` 方法, 98, 210, 222, 320, 337

`colorIncrement`, 81

`drawLight()` 方法, 232–234, 240

`COM` 接口, 12

`drawShadow()` 方法, 232, 238, 240–241

**计算机图形学**

OpenGL 程序分析, 14–29
着色器, 27–29
在显示器中显示场景, 22–26
调整数值, 26

`EAGLView`, 20
发展历程, 4–11

**地球，夜色中**

在好莱坞, 8–11
云层, 329
麻省理工学院, 5–6
概述, 318
犹他大学, 6–7
镜面反射, 325–328
历史沿革, 3–4
效果，镜头光晕, 259–270

**OpenGL 架构**, 29–31

自发光材质, 110–111
打开 OpenGL ES 项目, 2–3
`execute()` 文件, 102, 126
工具包, 11–14
`execute` 方法, 83, 85, 88, 122, 124, 128
`Direct3D`, 11–12
`execute()` 方法, 164, 197, 208, 247, 259, 266, 270, 273, 276, 293
`OGRE`, 13
`OpenGL`, 11
`executePlanet()` 方法, 127, 197–198, 266, 268, 270
`OpenSceneGraph`, 13
其他, 13–14
`executePlanets()` 方法, 126
`QuickDraw 3D`, 12–13
`executeVBO()` 方法, 292
`Unity3D`, 13
扩展详情窗格，用于 OpenGL 分析器, 305

**控制器文件**, 67

眼坐标, 54
坐标变换, 34

**坐标**

眼坐标, 54
OpenGL 坐标, 51–53

**F**

面剔除，针对 3D 对象, 76–77

`www.it-ebooks.info`

**索引**

**343**

面法线, 65, 82, 99
`glBlendFuncSeparateOES()` 方法, 177
`FBController` 对象, 208
`glBufferData()` 方法, 290–291
`FBOController`, 202, 205–206, 209
`glClear()` 方法, 25



