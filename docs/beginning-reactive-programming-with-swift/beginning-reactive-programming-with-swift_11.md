# 索引

## A, B

`Acid test` `Amazon Web Services (AWS)` 账户 `Mobile Hub` 登录 `add pods` 后端使用 `Cocoa` 基于云的计算和 `Cocoa Touch` 数据管理 共享数据 用户期望 创建项目 关于登录流程的开发者资源 账户 创建组织 `IAM` 工具 `root user` `Xcode` 产品 基于网络的技术 `Application programs` `App Store` `Asynchronous programming` `Authentication process`

## C

`Carthage` `Cloud computing` `Cocoa` `AWS`（参见 `Amazon Web Services (AWS)`）`Core Data` 工具 `SQLite` `CocoaPods` `GitHub` 仓库 安装 集成方法 `podfile` 单视图应用 检查目录 命令行 创建新文件夹 创建新项目 安装命令 `podfile` 审查项目 运行应用 工作区窗口 `Xcode` 项目 `Command-line integration` `Conditional statements` `Core Data` 工具

## D

`Declarative programs`

## E

`European Union General Data Protection Regulation (GDPR)` `Extensible Markup Language (XML)`

## F

`Facebook iOS SDK` 账户 广告 分析 API 应用事件 组件 `Docs` 菜单 登录 导航栏 分享 登录流程 添加框架 添加标签 绑定标识符 创建新 创建新应用 ID `info.plist` 文件 集成 登录按钮 属性列表 安全流程 单视图应用 导航栏 用于 `Swift` `Documentation page` 下载 `GitHub` `Framework model` `Functional programs`

## G

`GitHub` `Good Morning` 程序 `Grand Central Dispatch (GCD)`

## H

`Hello World` 程序

## I

`iCloud` 服务 `Identity and Access Management (IAM)` `Imperative programs`

## J, K, L, M, N

`JavaScript Object Notation (JSON)` 在 `BBEdit` 中的数组 `Excel` 中的组件 对象 `Swift Playgrounds`（参见 `Swift Playgrounds` 应用）在 `TextEdit` 中 在 `Xcode` 中

## O

`Object-oriented (OOP)` 编程 `Operating systems`

## P, Q

`Paradigms` 编程 声明式程序 设计模式 函数式程序 命令式程序 处理配置 `spaghetti-code` `Podfile` 编程 应用程序 异步 设计模式 范式（参见 `Paradigms` 编程） 处理配置 系统程序 `Propagation of change`

## R

`Reactive programming` `ReactiveX/RxSwift` 应用 添加项目 编码 `DemoList` 下载 `GitHub` 仓库 添加现有项目/工作区 项目名称和选项 保存项目 无 Playground 的单视图应用 项目在 `Xcode` 中 `iOS` 模拟器 标签 `RxCocoa` 和 `RxSwift` `storyboard` 语法 `RxSwift-enhanced` 项目 搜索栏 `UITableView` 代码 工作区和 Playground `Xcode` 集成 `Reusable code` 构建块 命令行集成 组件架构 `iOS` 和 `macOS` 委托 拖放 框架 协议 多个构建块 使用工作区 `RxSwift` 格式化的 Playground 添加新行 错误消息 标记代码 原始 Playground 原始代码 `Rx` 目标 选择方案 `GitHub` 开源 响应式编程 `Rx.playground` 文件夹 `Xcode 9`

## S

`Sharing data across apps` 和跨时间 挑战 不一致的数据类型 安全性 用于 `UI` 元素 `Shell model` `Single-view app` 检查目录 命令行 创建新文件夹 创建新项目 安装命令 `podfile` 审查项目 运行应用 工作区窗口 `Spaghetti-code` 程序 `Swift Package Manager (SPM)` `Swift Playgrounds` 应用 创建新文件 创建 Playground `fatalError` 字符串 `JSON` 集成工具 `Swift array` `Swift dictionary` 项目导航器 运行代码 输入/粘贴代码 `System programs`

## T

`Thin client computing`（参见 `Cloud computing`）

## U, V

`Unsustainable model`

## W

`Wikipedia`

## X, Y, Z

`Xcode 9`
