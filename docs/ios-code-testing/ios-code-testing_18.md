# 索引

## A

### AccountOwner 类
`AccountOwnerTests`：创建额外的测试，创建单元测试方法，将验证器类集成到九个测试用例参数中，测试用例文件变量和方法。

- `AccountOwnerTests.addChocolateChipCookie()`
- `addGingerbreadCookie()`
- `addShortbreadCookie()`

### Album 类
`Album.swift`

### AlertFactory.swift
### Apple 开发者团队
### 应用程序传输安全（ATS）
### 断言（Assertions）
### 异步测试技术

## B

### BankAccount 类
Swift 事务变量和方法。

- `BankAccountProtocol.swift`
- `BankAccountSpecification.swift`
- `BankAccount.swift`

### 行为驱动开发（BDD）
优点和缺点，银行账户项目 `BankAccountSpecification.swift`，业务需求和用户场景描述，`Quick` 测试用例类，模型层对象之间的关系，行为验证测试。

### BitBucket
### 机器人（Bots）
配置构建配置、配置源代码控制、配置测试设备、创建环境变量、集成、集成计划触发器、电子邮件通知、集成后脚本、集成前脚本。

### BusinessBankAccount
### 业务需求

## C

### Carthage
创建新项目，依赖管理解决方案，文件夹，安装程序，仓库，测试，测试目标。

### CocoaPods
创建新项目，在终端中安装，新工作区文件，`pod install` 命令，Pod 项目，测试，验证。

### 代码覆盖率报告
### CollectionViewCellProtocol.swift
### CollectionViewCell.swift
### 集合视图控制器，TDD 应用程序架构
- **模型层**：构建 `Album` 类、`City` 类、`Photo` 类。
- **用户界面层**：应用程序故事板、集合视图单元格、创建新类、场景、分区头部附件视图。
- **ViewModel 层**：构建 `cellViewModel` 方法、`CollectionViewCellViewModel` 类、`CollectionViewModel` 类、`CollectionViewSectionHeaderViewModel` 类、`headerViewModel` 方法、`numberOfItemsInSection` 方法、`numberOfSections()` 方法、`performInitialViewSetup` 方法、视图控制器绑定。

### 视图层，绑定到 Xcode 项目
添加资源、导航器。

### ColorDetailViewController
### ColorDetailViewModel
### ColorListTableViewController
### ColorTableViewModel
### `context()` 函数
### 持续集成（CI）机器人
配置构建配置、配置源代码控制、配置测试设备、创建环境变量、集成、集成计划触发器。

### macOS 服务器
安装 Apple 开发者团队，配置 Xcode 服务器，开发设备，启动，在 Xcode 服务器上新建 Git 仓库，仓库，为团队成员设置访问权限，启动 Xcode 服务器，Xcode 版本。

### Xcode 服务器
从 Git 克隆仓库，从克隆现有本地仓库，从 GitHub 克隆 Git 仓库，向 Xcode 提供凭证，为新项目托管仓库。

### Controller 组件
### CookieController 类
特性，`CookieController.swift`，`CookieControllerTests.swift`，`CookieFactory` 应用程序，iOS 模拟器，项目导航器。

### Cookie factory 应用程序
代码覆盖率数据，`Cookie` 类，`CookieController` 类，模型层，视图控制器类。

### Core Data
### CountriesAndCitiesUITests.swift 文件

## D

### `dataToReturnOnSuccess`
### 装饰器
`BankAccountProtocol.swift`，`BankAccount.swift`，`PersonalBankAccount.swift`

### 依赖注入（DI）
### `describe()`
### `displayResultsScreen()` 方法
### 领域特定语言（DSL）
### DownloadListenerProtocol
### `downloadProfileHelper()`

## E

### `element(boundBy:)` 方法
### 电子邮件地址验证器类

## F

### 工厂方法
### 伪造对象（Fake objects）
### FirstNameValidator 类
编译失败的测试，Swift 文件，单元测试用例，`validate()`

## G

### GitHub
从 GitHub 克隆 Git 仓库。

### Git 子模块
### 绿色阶段

## H

### 高层应用程序架构

## I, J, K

### 包含单元测试
### 实例化类
### 交互测试
### LargeViewController.swift
### 姓氏验证器类
### `launch()` 方法
### 遗留代码
`AlertFactory.swift`，开发 iOS 应用程序，DI，`downloadUserProfile()`，`EmailAddressValidator.swift`，`LargeViewController.swift`，`NetworkController.swift`，协议，`RefactoredLargeViewController.swift`，重命名并替换，`UserProfile.swift`

### 登录视图控制器
标识符属性，Identity Inspector，UI 组件，`UITextFieldDelegate`，`LoginViewControllerProtocol`

## M

### Mac App Store
### macOS 服务器
安装 Apple 开发者团队，开发设备，启动，仓库，为团队成员设置访问权限，Xcode 服务器配置，新建 Git 仓库，启动，版本。

### 大规模视图控制器
### 主从应用程序
### 主视图模型
### 模拟对象（Mock object）
`MockCollectionViewCell.swift`，`MockCollectionViewController.swift`，`MockDownloadListener` 类，`MockPhoto` 类，`MockServiceController` 类

### Mock/Stub 基础测试技术
### MockURLSession 类
`MockURLSessionDataTask` 类，`MockURLSession.swift`

### 模拟验证器
### Model 组件
### 模型层
`AccountOwner` 类，银行账户类，构建 `Album` 类，`City` 类，`Photo` 类，复杂应用程序，电子邮件地址验证器类，名字验证器类，姓氏验证器类，`LoginModel` 类，`SignupModel` 类，简单应用程序。

### 模型层更新
`Album` 类，`AlbumTests.swift`，实例变量，`load` 方法，`MockServiceController` 类（创建），`MockServiceController` 对象，修改，`setUp()` 方法，Xcode 导入文件对话框。`Photo` 类：`baseURL` 实例变量，`buildImageDownloadURL` 方法，`downloadedImage` 实例变量，`DownloadListenerProtocol.swift`，`MockDownloadListener` 类，`MockPhoto` 类（创建），`MockServiceController` 对象，修改，`PhotoTests.swift` 文件版本。

### 模型-视图-控制器（MVC）架构模式
控制器组件，iOS 应用程序，模型组件，可测试性问题。

### Model, View, ViewModel (MVVM)
优点，应用架构，架构模式，在 iOS 上的实例化，基于导航控制器的应用程序，`ReactiveCocoa`，`RxSwift`，Swift 协议，表格视图控制器。

### Modified BankAccount.swift

## N

### 基于导航控制器的应用程序
挑战，颜色：`ColorDetailViewController`，`ColorDetailViewModel`，`ColorList` 应用，`ColorListTableViewController`，`ColorTableViewModel`，主视图模型，MVVM 模式。

### 负面单元测试
### NetworkController.swift
### 网络层，PhotoBook 应用
`fetchFromURL` 方法，`MockURLSession` 类，`MockURLSessionDataTask` 类，`ServiceController`，`ServiceControllerTests`

### Nimble
### 面向对象的系统

## O

### `onViewListings()` 操作方法

## P

### 性能测试方法
### PersonalBankAccount
### PhotoBook 应用程序
层和组件类，模型层（参见模型层更新），修改后的应用架构，网络层（参见网络层，PhotoBook 应用），远程内容规范，源代码，用户界面。

### Photo 类
### 项目导航器

## Q

### Quick 的 BDD 测试
`beforeEach()` 方法，创建桩对象，检查实例变量，剩余的编译错误，将餐厅数据文件添加到项目，`RestaurantDirectorySpecification.swift`，`spec()`

### Quick 框架
与 `Xcode` 项目兼容的 `Nimble` 版本，使用 `Carthage`、使用 `CocoaPods`、使用 `Git` 子模块。

### Quick 测试用例类
BDD 测试，`context()` 函数，`Modified BankAccount.swift`，`spec()` 方法，用户场景和相应的 Quick BDD 测试，`XCTestCase`

## R

### ReactiveCocoa
### 红-绿-重构方法
### RefactoredLargeViewController.swift
### 远程内容规范
### `reset()`
### RestaurantDirectorySpecification
### RxSwift

## S

### Segue 标识符
### ServiceController 类
### ServiceControllerTests
### `setUp()` 方法
### 注册视图控制器
标识符属性，`UITextFieldDelegate`

### `spec()`
### 状态验证测试
### 基于桩的测试技术
### 被测对象
### Swift 方法
### Swift 协议

## T

### 表格视图控制器
### TDD 和 BDD 技术
向项目添加资源，BDD 测试与 `Quick`：`beforeEach()` 方法，创建桩对象，检查实例变量，剩余的编译错误，将餐厅数据文件添加到项目，`RestaurantDirectorySpecification.swift`，`spec()`。业务需求审查，高层应用程序架构，模型层构建，`RestaurantDirectory` 应用需求，`RestaurantDirectory` 用户场景，餐厅表格视图单元格，搜索视图控制器，用户界面层，视图控制器到视图模型的绑定，ViewModel 层构建：`RestaurantTableViewCellViewModel` 类，`RestaurantTableViewModel` 类，`SearchViewModel` 类，Xcode 项目创建。

### 拆卸方法
### 测试用例（参见单元测试）
### 测试用例类
性能测试方法，`setup` 方法，Swift 方法，`teardown` 方法，测试方法。

### testCountryListAppearsOnAppLaunch
### 测试驱动开发（TDD）
断言，定义，实例化类，交互测试，负面测试，最少代码原则，红-绿-重构方法，消除重复，先写测试，状态验证测试，被测对象，测试套件，单元测试，编写测试。

### TestGitRepositoryHostedOnXcodeServer
### 测试网络层策略
异步测试技术，Mock/Stub 基础测试技术。

### 测试方法
### 测试导航器
### 测试套件
### Transaction 类
Swift 验证器对象变量和方法。

## U

### 单元测试
为断言添加支持，用例类，创建项目，首次测试，交互测试，负面测试，状态验证测试。

### UI 层
### URLSessionProtocol.swift
### 用户界面（UI）
应用程序故事板，断言，集合视图单元格：添加标签，图像视图，导航控制器，输出口，Pin 约束，按钮，半透明视图，集合视图控制器场景，`CountriesAndCitiesUITests.swift` 文件，创建新类，登录视图控制器，记录，样本应用程序，分区头部附件视图，单独的文件夹组和构建目标，注册视图控制器，从右侧滑入动画，测试用例类创建，复制选项，导航器，`setup` 方法，`teardown` 方法，取消选中单元测试目标，`XCUIApplication`，`testCountryListAppearsOnAppLaunch`，测试现有项目，新项目，测试运行器与被测应用程序，UI 录制，在断言前等待，`XCTest`，`XCUIDevice`，`XCUIElement`，`XCUIElementAttributes`，`XCUIElementQuery`，`XCUIElementTypeQueryProvider`。

### UserProfile.swift

## V, W

### View 组件
### 视图控制器类
另见集合视图控制器，TDD 应用架构，完成的应用用户界面（参见用户界面 UI），视图模型（参见视图模型），Xcode 项目。

### 视图控制器到视图模型的绑定
`viewDidLoad()` 方法

### 视图层更新
集合视图单元格，集合视图控制器。

### 视图模型
登录视图控制器，`LoginViewControllerTests`，`passwordDidEndOnExit`，`passwordUpdated`，`performInitialSetup`，`UITextFieldStub`，`userNameDidEndOnExit`，`userNameUpdated`，注册视图控制器，测试驱动方法。

### ViewModel 层
登录方法，登录控制器，`LoginViewModel`，`MockLoginController` 测试，登录视图控制器，`LoginViewControllerProtocol`，`LoginViewModel` 方法：`passwordDidEndOnExit`，`passwordUpdated`，`performInitialViewSetup`，用户界面：`userNameDidEndOnExit`，`userNameUpdated`。`LoginViewModel`，`LoginViewControllerProtocol`，`LoginViewModelTests`，`SignupViewModel`

### ViewModel 层构建
`cellViewModel` 方法，`CollectionViewCellViewModel` 类，`CollectionViewModel` 类，`CollectionViewSectionHeaderViewModel` 类，`headerViewModel` 方法，`numberOfItemsInSection` 方法，`numberOfSections()` 方法，`performInitialViewSetup` 方法（测试），`RestaurantTableViewCellViewModel` 类，`RestaurantTableViewModel` 类，`SearchViewModel` 类，视图控制器绑定，视图层绑定到：`CollectionViewCell` 类，`CollectionViewController` 类，`CollectionViewSectionHeader` 类。

### View Model 层更新
集合视图单元格视图模型，集合视图模型。

### 视图/视图控制器层

## X, Y, Z

### Xcode
代码覆盖率报告，下载并安装，项目导航器，报告导航器，测试用例类，测试导航器，测试套件，单元测试。

### Xcode 8
### Xcode 项目
添加资源，创建导航器，将 `Quick` 添加到项目：使用 `Carthage`，使用 `CocoaPods`，使用 `Git` 子模块，尺寸检查器。

### Xcode 服务器
从克隆现有本地仓库，从克隆 Git 仓库，从 GitHub 克隆 Git 仓库，向 Xcode 提供凭证，为新项目托管仓库，新 Git 仓库，仓库，启动版本。

### XCTest
断言宏。

### XCUIApplication
### XCUIDevice
### XCUIElement
### XCUIElementAttributes
### XCUIElementQuery
