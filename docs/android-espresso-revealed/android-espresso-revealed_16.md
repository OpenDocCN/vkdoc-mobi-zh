# 索引

## A

`actionOnHolderItem()`

`Activity` 意图流

`ActivityResult` 类

`ActivityScenario` 和 `FragmentScenario`

`Activity.setResult()` 方法

`ActivityTestRule` 规则

`AdapterView`

`AddEditToDoRobot` 类

`addsNewToDo()`

足够的色彩对比度

`allOf()` hamcrest 逻辑匹配器

`allOf()` hamcrest 匹配器

Android 无障碍测试

应用程序

`AccessibilityChecks.enable()` 设置

无障碍 logcat 日志

无障碍扫描器流

`anyOf()` 匹配器

创建任务视图

`RootViewAccessibilityTest.kt` 类

`setThrowExceptionForErrors()` 方法

UI 元素

指南

工具

Android 应用程序包 (APK)

Android 构建过程

`AndroidJUnitRunner`

`AndroidManifest.xml` 文件

`AndroidStudio`

`build.gradle` 文件

`BuildVariant`

插桩测试配置

插桩测试运行配置

实时模板

Android 测试框架

`androidTest` 包

`build.gradle` 文件

依赖项

`todoapp/build.gradle` 文件

`androidx.test`

AndroidX 测试库

`ActivityScenario` 和 `FragmentScenario`

`ActivityScenarioTest.kt`

`android.support`

`androidx.test`

`ApplicationProvider`

JUnit 和 Truth 断言

JUnit `assertTrue()` 方法

迁移

测试金字塔

Truth 断言库

`TruthTest.generatesJunitAssertionError()` 方法

动画时长比例

注解

`anyOf()` hamcrest 核心匹配器

应用程序布局

`ApplicationProvider`

应用程序 UI 元素，识别

## B

`BrailleBack`

`BuilderAddEditToDoRobot.kt` 类

`BuilderToDoListRobot.kt` 测试类

`BundleMatchers` 类

`bySelectorSample()` 测试

## C

`check()` 方法

`checkForCondition()` 方法

`checksToDoStateChange()`

`clearText()`

`clickDoneFabButton()` 方法

`clickTodoCheckBoxWithTitle()` 视图操作

`closeSoftKeyboard()`

消除代码重复

`ComponentNameMatchers`

`ConditionWatcher`

Espresso Kotlin DSL

`Instruction` 类

方法

`CountingIdlingResource`

`CustomLocale.apk` 应用

自定义匹配器

## D

`DataInteraction` 方法

`DataInteractionsTest` 类

`deniesAndGrantsPermission()` 测试

设备监视器应用

`dispatcher.runningCallsCount()` 方法

`doubleClick()`

可绘制对象匹配器

## E

易于遵循的导航

模拟器扩展控件

模拟器快照

模拟器 telnet 连接和加载

`expect` 实用程序

脚本，保存和加载

拍摄快照按钮

`enableCameraPermission()`

Espresso

错误处理

框架

功能

实时模板

`RecyclerViewActions`，创建

滑动操作

`customactions.CustomSwipeActions.java`

`customswipe.CustomSwipe.java`

`swipeDown()` 操作

`swipeUp()` 操作

测试失败，截图

测试机器人实现

测试记录器工具

断言，添加

偏好设置

记录器视图

`tasks` 包

测试设备

测试菜单

`Espresso.onData()` 方法

`Espresso.onView()` 方法

`Espresso.pressBack()`

`Espresso.registerIdlingResources()` 方法

`EspressoUiAutomatorTest`

Espresso-Web

API 组件

驱动原子

Web 断言

Web 交互

API 使用公式

Chrome 浏览器开发者工具视图

布局层次结构

定位器类型

`onWebView()` 方法

`WebViewTest.updatesLabelAndOpensNewPage()`

Espresso-Web 测试

`onWebView()` 方法

`<select>` 下拉菜单

`WebAssertion.webMatches()` 方法

`WebViewTest.failsToClickSelectDropDown()`

`WebViewTest.findsElementsByXpath()`

`WebViewTest.opensModal()`

`WebViewTest.selectsRadioButtonWithCss()`

`WebViewTest.showsOtherLocatorsSample()`

`WebViewTest.updatesLabelAndOpensNewPage()`

`WebViewTest.verifiesSelectDropDown()`

## F

`findObject()` 方法

Firebase 测试实验室

添加项目

设备矩阵

初始状态

运行测试

测试配置

工具栏

## G

`generateToDos()` 方法

`getDescription()` 方法

`gone()` 方法

Gradle `BuildVariant`

授予相机权限对话框

`GrantPermissionRule`

对内容进行分组

## H

Hamcrest 字符串匹配器

## I

`IdlingResource`

代码

`ConditionWatcher`

Espresso

方法

同步条件

测试

使用场景

`IdlingThreadPoolExecutor`

中缀表示法

`init()` 方法

内部类方法

`inRoot()` 方法



### 插桩

`Instrumentation.ActivityResult(Activity.RESULT_OK, null)`

`InstrumentationRegistry` 类

集成开发环境（IDE）

`intended()` 方法

`Intent.createChooser()` 方法

`IntentHelper.kt` 对象

`intentMatchers`

`IntentMatchers.hasExtras()` 匹配器

### Intents

#### 类

依赖项，设置

桩（*参见* 对 Activity 的 Intent 进行桩操作）

#### 类型

- 活动（Activity）
- 广播（Broadcast）
- 服务（Service）

#### 验证

`Intents.intending()` 机制

`IntentsTestRule`

`iSIdleNow()` 方法

`isInternal()`

## J

### JUnit 参数化测试

### JUnit 套件运行器

## K

### Kotlin

- 优点
- 扩展函数
- 函数作为类型
- `R.class` 资源，导入
- 字符串模板
- `Espresso` Java 测试
- *与* Java 对比

### Kotlin，Espresso 领域特定语言

- `checksToDoStateChange()` 测试方法
- 函数声明
- UI 测试代码库
- `viewWithText()` 扩展函数

## L

标记 UI 元素

`launchBackToDoApplication()`

布局检查器

动态模板

`longClick()`

## M

`Monkeyrunner`

`MonkeyTest.kt`

## N

原生应用开发

`NewTo-DoScreen`

## O

`OkHttp3IdlingResource`

`onActivityResult()` 方法

`onData()` 方法

`onImageButtonClick()` 方法

`onView()` 方法

`openActionBarOverflowOrOptionsMenu()`

`openContextualActionModeOverflowMenu()`

`openLink()`

## P, Q

包

参数化测试

`perform()` 方法

`Picasso IdlingResource`

`PreferenceActivity`

`pressBack()`

`pressImeActionButton()`

`pressKey()`

`pressMenuKey()`

## R

### `RecyclerViewActions`

`customactions.CustomRecyclerViewActions.java`

### RecyclerView 匹配器

`repeatedlyUntil()` 方法

`replaceText()`

### 运行插桩测试

- Gradle 命令
- shell 命令

### 运行时权限

- `GrantPermissionRule`
- UI Automator 测试框架

### `RuntimePermissionsTest.kt`

### `RuntimePermissionsUiAutomatorTest.kt`

## S

`ScheduledThreadPoolExecutor`

### `ScreenDimensions` 类

### 屏幕对象设计模式，Android UI 测试

- `BaseScreen` 类
- 优点
  - 代码复用
  - 易于维护的测试
  - 易于构建的用户流程
  - 逻辑测试步骤分离
  - 可读性强的测试
- `clickDoneFabButton()` 方法
- 缺点
  - 详细的屏幕方法
  - 不同屏幕，相同操作
  - 处理视图
- `NewToDoScreen`
- `screens.BaseScreen.MenuDrawerView` 类
- `screens.NewToDoScreen.kt`
- `screens.SettingsScreen.kt`
- `tests.AddToDoTest.kt`
- `tests.SettingsTest.verifiesUpNavigation()`
- `ToDoListScreen`

`scrollTo()` 操作

`scrollToHolder()`

`scrollToPosition()`

### `SearchCondition`

### SeekBar 进度

- 自定义 Espresso ViewAction
- `SetSeekBarProgressTest.kt`
- ViewAction

`setTimeoutLimit()` 方法

`setUp()` 方法

`setWatchInterval()` 方法

`shouldRelaunchTheApp()` 函数

`startActivityForResult()` 方法

`stubAllExternalIntents()` 方法

`StubAllIntentsTest.kt`

### 对 Activity 的 Intent 进行桩操作

- `AddEditTaskFragment.java`
- `@Before` 方法
- `BundleMatchers` 类
- `ComponentNameMatchers`
- 调试
- `Intent.createChooser()` 方法
- `IntentMatchers`
- `IntentsTestRule` 和 `stubAllExternalIntents()` 方法
- `onActivityResult()` 方法
- `.png` 文件，位置
- 结果
- 共享待办事项 Intent，`Intent.createChooser()`
- `startActivityForResult()` 方法
- `StubAllIntentsTest.kt`
- `stubsShareIntent()` 测试
- 待办事项 Intent 实现
- `UriMatchers`

`StubChooserIntentTest.kt` 类

`StubIntentTest.kt` 类

`stubsAllExternalIntents()` 方法

`StubSelectImageIntentTest.kt` 类

`StubSelectImageIntentTest.stubsImageIntentWithAsset()`

`stubsImageIntentWithAsset()` 测试用例

`stubsImageIntentWithDrawable()` 测试

`stubsShareIntent()` 测试

`stubsShareIntentByAction()`

### 监督式 Monkey 测试

- 操作，定义
- Android 屏幕坐标系
- 设备屏幕区域
- `dragNow` 和 `pressNowBack`
- 导航栏
- 依赖包的功能，实现
- `ScreenDimensions.kt` 类
- 状态栏
- `swipeCustom()` 方法
- `swipeLeft()`

## T

`takesCameraPicture()` 测试用例

`TalkBack`

### 测试自动化

- `@BeforeClass` 方法
- `DeviceSetupTest.kt`
- 系统动画
  - 属性
  - 类型
- 触摸和长按延迟
- `ViewActions.closeSoftKeyboard()` 方法
- 虚拟键盘

### 测试机器人模式

- `AddEditToDoRobot` 类
- `addToDo()` 和 `done()` 函数
- `BuilderAddEditToDoRobot.kt` 类
- 构建器模式
- `BuilderToDoListRobot.kt` 类
- Kotlin 的中缀函数表示法
- 内部类，添加
- `RobotsTest.robotChecksToDoStateChangeBuilder()`
- `RobotsTest.robotChecksToDoStateChangeRobotsSeparation()`
- `TasksListRobotWithInnerClasses` 类
- 测试，是什么和怎么做
- `toDoList{ }` 和 `addEditTodo{ }`
- `toDoList()` 函数
- `ToDoListRobot`
- 待办事项列表屏幕
- `test_logo`
- `testsuite.TestSuiteSample.kt`
- `TextView` 和 `ImageView` 可绘制对象
- 待办事项应用，设置页面
- `ToDoListRobot` 类
- `ToDoListScreen`
- 触摸目标要求大
- 过渡动画缩放
- Truth 断言库
- `typeText()` 方法

## U

### UI Automator

- API
- `build.gradle` 文件
- `checkForCondition()` 方法
- `clickNotificationOpenMainPage()`
- 设备控制
- 特性
  - 处理应用过渡
  - 定位 UI 元素
  - 报告
- `UiAutomatorUiWatcherTest.kt` 类
- `UiDevice().act` — 整合
- `UiDevice().find` — 显示
- `UiDevice().get` — 获取
- `UiDevice().set` — 启用
- `UiDevice().wait` — 等待
- `UiDevice().watch` — 表示
- UI 元素（*参见* UI 元素）
- UI/视图操作
- `UiWatcher` 类
- 等待
  - `EventCondition`
  - `SearchCondition`
  - `UiObject2Condition`

`UiAutomatorBySelectorTest.bySelectorSampleWithFindObjects()`

`UiAutomatorUiSelectorTest.kt` 类

`UiAutomatorUiSelectorTest.uiSelectorSampleSimplified()`

`UiAutomatorUiWatcherTest.kt` 类

`UiDevice.findObject(BySelector)`

### UI 元素

- `bySelectorSample()` 测试
- `findObject()` 和 `findObjects()` 方法
- `findObjects(BySelector)` 示例
- `hasObject()`
- `TasksActivity`
- `UiDevice.findObject(BySelector)`
- `UiSelector` 示例测试
- `UiObject2Condition`
- `UiSelector` 示例测试

### UI 测试

`UiWatcher` 类

`UriIdlingResource`

`UriMatchers`

用户界面（UI）

## V

### `ViewActions` 类

`ViewActions.pressBack()` 方法

`ViewActionsTest`

`ViewInteraction` 类

### 视图匹配器，Espresso

- 速查表
- `EditText` 示例
- 常规偏好设置部分
- hamcrest 匹配器
- `hasChildCount()`
- `hasContentDescription()`
- `hasDescendant()`
- `hasErrorText()`
- `hasFocus()`
- `hasImeAction()`
- `hasLinks()`
- `hasMinimumChildCount()`
- `hasSibling()`
- `isAssignableFrom()`
- `isChecked()`
- `isClickable()`
- `isCompletelyDisplayed()`
- `isDescendantOfA()`
- `isDisplayed()`
- `isDisplayingAtLeast()`
- `isEnabled()`
- `isFocusable()`
- `isJavascriptEnabled()`
- `isNotChecked()`
- `isRoot()`
- `isSelected()`
- 待办事项列表
- 待办任务
- `withAlpha()`
- `withChild()`
- `withClassName()`
- `withContentDescription()`
- `withEffectiveVisibility()`
- `withHint()`
- `withId()`
- `withInputType()`
- `withParent()`
- `withParentIndex()`
- `withResourceName()`
- `withSpinnerText()`
- `withTagKey()`
- `withTagValue()`
- `withText()`

Voice Access（语音访问）

## W, X, Y, Z

`waitForCondition()` 方法

`WebView` 组件

窗口动画缩放

`withId()` ViewMatcher

`withText()` ViewMatcher
