# 索引

### A

-   `Acceleration` 属性
-   `Accelerometer`，玩家移动
-   `CMMotionManager`
-   `coreMotionManager`
-   `deinit()` 方法
-   `didSimulatePhysics()` 方法
-   `touchesBegan()` 方法
-   `update()` 方法
-   `addChild()` 方法
-   `addOrbsToForeground()` 方法
-   `环境光`
-   `锚点`
-   `Angle` 属性

### B

-   `Birthrate` 属性
-   `BlackHole` 节点
    -   `addBlackHolesToForeground()`
    -   `addOrbsToForeground()`
    -   `CollisionCategoryBlackHoles`
    -   `didBeginContact()`
    -   用途
-   `Bokeh` 模板
-   `BoxNode` 材质

### C

-   `笛卡尔坐标系`
-   `碰撞检测`
    -   动态
    -   运动学
    -   `SCNPhysicsContact` 协议
    -   `SCNPhysicsBody`
    -   `SCNPhysicsBody` 的 categoryBitMask 和 collisionBitMask
    -   `SCNPhysicsWorld`
    -   `didBeginContact` 协议
    -   形状
-   `SharedConstants.swift`
-   `Contents` 静态
-   `CoreMotion` 框架
-   `createMainScene()` 方法

### D

-   `2D` 游戏
-   `didEvaluateActions()` 方法
-   `didSimulatePhysics()` 方法

### E

-   `发射`

### F

-   `Fireflies` 模板

### G

-   `GameOverlay`
    -   `startTimer()`
    -   `stopTimer()`
-   “游戏结束”界面
-   `GameOverView`
-   `gameOverWithResult()` 方法
-   `GameScene`
    -   `backgroundNode`
    -   代码改动
    -   `foregroundNode`
    -   `impulseCount` 属性
    -   包含额外能量球的修改后场景
    -   `orbNode`
    -   行星表面
        -   `backgroundNode` 声明
        -   `colorizeAction`
        -   `didBeginContact` 方法
        -   星空层
        -   视差效果
        -   `playerNode` 的位置
        -   `SKSpriteNode` 声明
        -   `update()` 方法
        -   利用加速度计控制玩家移动
        -   `playerNode`
        -   滚动场景
        -   `touchesBegan()` 方法
-   `GameScene` 类
-   `GameScene.swift` 文件
-   `init()` 方法
-   `GameScene.init()` 方法
-   `GameScene` 的节点树
-   `GameScene` 的 `touchesBegan()` 方法
-   `GameScene.swift`
-   `GameViewController`
    -   代理函数
    -   iOS Swift 应用程序
    -   移动摄像机
    -   `scaleMode` 属性
    -   场景变量
    -   `SKView`
    -   `super.viewDidLoad()`
    -   `UIViewController`
    -   `viewDidLoad()` 方法
-   `GlobeNode` 光照增强
-   `发光效果`
-   `图形处理单元 (GPU)`

### H

-   `高动态范围` 技术
-   `命中测试`
    -   `CoreMotion` 框架
    -   `GameView.swift`
    -   `positionCameraWithSpaceman()`
    -   渲染委托方法
    -   `SceneKit` 渲染循环
    -   `setupAccelerometer` 函数

### I, J, K

-   `impulseCount` 属性
-   `insertChild` 方法
-   `iOS 10` `SceneKit`

### L

-   `光照、摄像机和材质效果`
    -   `环境光`
    -   `BoxNode` 材质
    -   摄像机类型
    -   漫反射着色
    -   `globeNode` 光照增强
    -   相乘
    -   法线
    -   光照参数
    -   `pyramidNode` 材质
    -   反射光照
    -   剩余障碍物材质
    -   `SceneKit` 摄像机用法
    -   `SCNLightTypeAmbient`
    -   `SCNLightTypeDirectional`
    -   `SCNLightTypeOmni`
    -   `SCNLightTypeSpot`
    -   `setupLighting()` 方法
    -   光泽
    -   透明

### M

-   `Magic` 模板
-   `Maximum` 属性

### N

-   `节点树`, `SKScene`
    -   定义
    -   `enumerateChildNodesWithName()` 方法
    -   `GameScene.init()` 方法
    -   内存属性
    -   嵌套节点
    -   重叠的玩家节点
    -   渲染循环
    -   `SKNode.name` 属性
-   `NSBundle`

### O

-   `OpenGL`
-   `Orb` 节点
    -   `didBeginContact()` 方法
    -   第一组代码
    -   `init()` 方法
    -   布局
    -   包含额外的修改后场景
    -   第二组代码

### P, Q

-   `视差效果`
-   `粒子发射器`
    -   选择“文件”选项对话框
    -   选择模板对话框
    -   创建
    -   默认火焰描述
    -   排气尾迹
    -   `GameScene.init()` 方法
    -   生成的火花
    -   粒子发射器
    -   `hideEngineExhaust()` 方法
    -   反转的火焰属性
    -   `SKEmitterNode` 对象
    -   模板
    -   Xcode 的 `粒子发射器编辑器`
-   `粒子发射器编辑器`
-   `粒子生命周期属性`
    -   `发射器出生率`
    -   `Maximum` 属性
    -   范围
    -   起始
-   `粒子运动属性`
    -   加速度
    -   角度
    -   位置范围
    -   速度
    -   `Z 位置`
-   `playSoundFileNamed()` 方法
-   `Position range` 属性
-   `PyramidNode` 方法

### R

-   `Rain` 模板
-   `Range` 属性
-   `反射光照`
-   `removeFromParent()` 方法
-   `渲染循环`
    -   `didEvaluateActions()` 方法
    -   `didSimulatePhysics()` 方法
    -   iOS 应用程序
    -   `SCNSceneRendererDelegate`
    -   `update()` 方法

### S

-   `场景构建`
    -   可收集节点
    -   组节点
    -   叶子节点
    -   场景图
    -   场景节点
-   `场景图`
-   `SceneKit`
    -   动画
    -   Apple 的 `SceneKit` 库
    -   美术资源
    -   内置模型类 `SCNGeometry` 对象
    -   笛卡尔坐标系
    -   类
    -   坐标系 2D 场景
    -   欧几里得空间
    -   `floorNode`
    -   `GameViewController`
    -   `iPhone`
    -   光照、摄像机和材质效果 (参见 `光照、摄像机和材质效果`)
    -   选项
    -   点
    -   渲染循环
    -   资源
    -   示例应用
    -   场景图
    -   `SCNNode`
    -   3D 太空人
    -   站立的太空人，地板
    -   `SuperSpaceMan3D` 设备
    -   Swift
    -   模板对话框
    -   变换
    -   向量
    -   `viewDidLoad()` 方法
    -   连接与构建
-   `SceneKit 编辑器`
    -   设计区
    -   检查器/属性
    -   对象库
    -   场景，创建
        -   摄像机节点
        -   泛光节点
        -   场景地面，添加
        -   主角节点
        -   多个节点
        -   节点，添加
        -   球体属性检查器
        -   球体节点放置
        -   场景图
        -   `SceneKit` 场景
-   `场景过渡` (参见 `SKTransition`)
-   `sceneView.overlaySKScene`
-   `SCNGeometry` 对象
    -   `pyramidNode`
    -   `SCNBox`
    -   `SCNCapsule`
    -   `SCNCone`
    -   `SCNCylinder`
    -   `SCNFloor`
    -   `SCNPlane`
    -   `SCNPyramid`
    -   `SCNShape`
    -   `SCNSphere`
    -   `SCNText`
    -   `SCNTorus`
    -   `SCNTube`
-   `SCNTransaction` 类
-   `setupCameras()` 方法
-   `SharedConstants.swift`
-   `SKAction` (参见 `Sprite Kit 动作`)
-   `SKCropNode`
-   `SKEffectNode`
-   `SKEmitterNode`
-   `SKLabelNodes`
    -   添加冲量计数器
    -   添加计分
    -   添加简单音效
    -   示例
    -   `GameScene`
    -   `horizontalAlignmentMode`
    -   `init()` 方法
    -   `verticalAlignmentMode`
-   `SKNode`
    -   添加节点
    -   添加碰撞检测
    -   `GameScene.swift`
    -   能量增强
    -   移除能量球
-   `SKPhysicsBody`
-   `SKPhysicsContact` 属性
-   `SKNode.name` 属性
-   `SKNode` 节点树操作方法
-   `SKPhysicsBody`
    -   `applyForce()`
    -   `applyImpulse()`
    -   `backgroundNode`
    -   位掩码
    -   `CollisionCategoryPowerUpOrbs`
    -   `contactTestBitMask`
    -   动态体积
    -   边缘
    -   流体或空气
    -   摩擦
    -   模拟
    -   `GameScene.init(size: CGSize)`
    -   `GameScene.swift`
    -   游戏世界的重力设置
    -   `linearDamping` 属性
    -   基于路径的多边形
    -   `playerNode`
    -   玩家设置代码
    -   渲染循环
    -   `SKScene` 的 `physicsWorld.gravity` 属性
    -   静态体积
    -   `SuperSpaceMan` 应用
    -   `touchesBegan()` 方法
    -   `UIResponder.touchesBegan()`
-   `SKPhysicsContactDelegate` 协议
-   `SKScene`
    -   `SpriteKit` 游戏步骤，节点树
    -   `SKScene` `didMove()` 方法
    -   `SKSceneScaleMode`
    -   `SKScene` `willMove()` 方法
-   `SKShapeNode`
-   `SKSpriteNode`
    -   锚点
    -   单位坐标系
    -   `GameScene.init()` 方法
    -   `playerNode` 尺寸属性
-   `SKTextureAtlas`
-   `SKTexture` 类
-   `SKTransition`
    -   添加新场景
    -   添加过渡效果
    -   结束游戏
    -   `init()` 方法
    -   输掉游戏
    -   暂停场景
    -   场景检测
    -   场景间过渡
    -   赢得游戏
-   `SKVideoNode`
-   `SKView`
-   `Smoke` 模板
-   `Snow` 模板
-   `Sonic Jump Fever`
-   `Spark` 模板
-   `镜面光`
-   `Speed` 属性
-   `Sphere` 节点放置
-   `SpriteKit`
    -   `addBlackHolesToForeground()` 方法
    -   `addOrbsToForeground()` 方法
    -   Apple 的 `SceneKit` 库
    -   创建自己的节点
    -   通过子类化创建自己的节点
    -   `didBeginContact` 方法
    -   `didSimulatePhysicsAtTime` 协议
    -   外部化游戏数据
    -   `GameOverlay` 类
    -   `GameOverView` 类
    -   `GameViewController`
    -   `NSNumberFormatter`
    -   `OpenGL` 上下文
    -   叠加计时器
    -   重写的 `init(size: CGSize)` 函数
    -   粒子发射器
    -   粒子生命周期属性
    -   粒子运动属性
    -   移除不必要的节点
    -   重新开始游戏
    -   重用纹理
    -   记分板
    -   `SKLabelNode` 对象
    -   `SKLabelNode.setup`
    -   `startTimer()` 函数
    -   `stopTimer()` 函数
    -   计时器运行
-   `Sprite Kit 动作`
    -   `BlackHole` 节点定义
    -   `moveToX`, `sequence` 和 `repeatForever`
    -   `SKTexture`
    -   `SKTextureAtlas`
    -   步骤
    -   用途
-   `SpriteKit 坐标系`
-   `Sprite Kit 游戏`
    -   `anchorPoints`
    -   `backgroundNode` 创建
    -   自定义类
    -   `GameScene.init()` 方法
    -   `GameScene.swift`
    -   `GameViewController.swift`
    -   图片资源
    -   基于 Intel 的 Macintosh
    -   iOS 应用
    -   `Main.storyboard`
    -   `playerNode`
    -   竖屏模式
    -   运行
    -   `SKS` 文件
    -   `SKSpriteNodes`
    -   `SpaceShip` 资源
    -   `spites.atlas`
    -   `SuperSpaceMan`
    -   目标设置
    -   `UIViewController`
    -   视觉元素
    -   Xcode 模拟器
-   `SpriteKit 粒子发射器`
-   `Sprite Kit 场景`
    -   `anchorPoint` 属性
    -   坐标系
    -   `gameScene` 的 `children` 属性
    -   `insertChild()`
    -   嵌套节点
    -   `node2.removeFromParent()`
    -   `playerNode.removeFromParent()` 方法
    -   `渲染循环` (参见 `渲染循环`)
    -   `SKScene.addChild()` 方法
-   `Start` 属性
-   `SuperSpaceMan3D` 设备
-   `SuperSpaceMan` 游戏
    -   添加新场景
    -   目标设置
-   `Swift` `Sprite Kit` 项目
    -   项目选项对话框
    -   模板对话框
-   `Swiftystein3D`

### T

-   `touchedBegan()` 方法

### U

-   `UIViewController`

### V, W, X, Y

-   `viewDidLoad()` 方法

### Z

-   `Z 位置` 属性
