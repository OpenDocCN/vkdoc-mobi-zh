# 项目视图

## 运行配置

## 调试器

## 常规选项

## 杂项选项

## 性能分析

## SDK 管理器

## SDK

## 设置

## 确认屏幕

## 数据共享提示

## 已下载视图

## 下载组件进度屏幕

## 模拟器设置

## 注意事项

## 主页

## 导入设置

## 安装类型

## 启动屏幕

## 启动画面

## 用户界面主题

## 验证设置屏幕

## 工具

## URL

## 安卓虚拟设备

## Android Studio

## 安卓虚拟设备管理器屏幕

## 方法

## 配置验证屏幕

## 晦涩的设备名称

## 当前/历史选项

## 密度

## 硬件选项

## 关键软件方面

## 许可协议

## 管理器主页屏幕

## Pixel 选项

## 物理设备

## 分辨率

## 系统镜像选择屏幕

## 欢迎使用 Android Studio 屏幕

## 资产/原始文件

### Android 的文件系统模型

## 输入/输出

## 权限，读/写文件

## 填充，资源文件

## Audacity

## 音频

### 资产/资源，选择

### AudioPlayExample

### 探索播放选项

### 回放

## Android

### 资产

### 代码

### 文件存储

### 媒体包

### 原始音频资源

### 流媒体服务

## 音频回放

### 录制音频

## 流媒体

## AWS Device Farm

### B

### Blender

### ButtonExample 应用

### C

### 回调方法

`onCreate()`  
`onDestroy()`  
`onPause()`  
`onRestart()`  
`onRestoreInstanceState()`  
`onResume()`  
`onRetainNonConfigurationInstance()`  
`onSaveInstanceState()`  
`onStart()`  
`onStop()`

### 通话

`ACTION_DIAL`

### CallExample 应用，布局

### CallExample 应用，工作逻辑

### 处理来电

### 将电话支持设为可选

### 方法

### 指定电话支持

`cancelAll()` 方法

### CDATA 值

### CheckBox 控件

### 类

### 类方法

### Console 对象

### ConstraintLayout

### Flow

### Layer

### Motion

### D

### 数据类型

### DaVinci Resolve

### Debian/基于 Debian 的系统

### 开发者音频工具集

#### Apple Mac 用户

#### Audacity

#### 音频采样/频率

#### 音频理论

#### 编码/解码/数据丢失

#### 分辨率

#### WavePad

### 开发者硬件

#### AVD

#### 线缆

#### 充电线缆/底座/设备

#### 检查清单

#### CPU

#### 磁盘空间

#### 键盘

#### 内存

#### 显示器/显示屏

#### 鼠标

#### 屏幕

#### 操作

#### 目标

#### 尺寸

#### 存储

#### USB 端口

### 开发者视频工具集

#### 编辑套件

#### 编辑工具

#### 开源工具

### 开发环境

`doClick()` 和 `doStartAudio()` 方法  
`doHideKeyboard()` 方法  
`doPlayAudio()` 方法  
`doPlayVideo()` 方法  
`doSaveToFile()` 方法  
`doStartRecording()` 方法  
`doStopVideo()` 方法

### E

#### Eclipse

### EditText 控件

`execSQL()` 方法

### ExoPlayer

### 可扩展标记语言

#### Android

#### 活动布局

#### 资源定义

#### 回顾

#### 属性

#### 创建

#### 元素

#### 命名空间

#### 组成部分

#### 值

#### 版本/编码

### F

### 文件

#### 检查外部文件

#### 代码

##### 布局

#### 保存/读取

#### StreamlineIMEs

#### 外部存储

#### Android 文件系统，历史

#### 环境方法

#### 文件输入/输出

`findViewById()` 方法

### FirstJavaDemo 程序

### FirstJavaDemo.java 文件

### Flow 虚拟布局

### 以片段为中心的设计

### 片段生命周期回调事件

#### 编码

#### 颜色详情

#### 双面板

#### 单面板

#### 颜色列表

#### 基于片段的应用创建

##### 布局

`onAttach()`  
`onCreate()`  
`onCreateView()`  
`onDestroy()`  
`onDestroyView()`  
`onDetach()`  
`onInflate()`  
`onPause()`  
`onResume()`  
`onSaveInstanceState()`  
`onStart()`  
`onStop()`  
`onViewStateRestored()`

### 片段

#### 类

#### 编码实战

#### 特性

#### 生命周期

#### XML

### G

##### 泛型

`getCallState()` 方法  
`getExternalStorageDirectory()` 方法  
`getModels()` 方法  
`getResources()` 方法  
`getSystemService()` 方法

### 全球社区

### GNU 图像处理程序

### Google

### Google Firebase Test Lab

### Gradle 工具

#### 命令行

#### `gradle.org`

#### `gradle.properties` 文件

### GridLayout

### H

### HandBrake

#### 定义

#### 下载/安装

#### 运行

#### VideoPlayExample

### 手机/平板

#### Android 的默认行为

#### AWS Device Farm

#### Google Firebase Test Lab

#### Samsung Remote Test Lab

#### 测试

#### 虚拟/增强

`hasSystemFeature()` 方法

### Honeycomb

### 混合模型

### I

### If 语句

### ImageButton 控件

### ImageView 控件

### Inkscape

### 输入法编辑器

`insert()` 方法  
`insertModelRow()` 方法

### 集成开发环境

#### IntelliJ IDEA

### Intent

#### 活动动作

#### 活动依赖

#### Android 的验证链接

#### 行为

#### 创建

#### 定义

#### 被调用的活动

#### 清单文件

#### 路由

### J

### Java 二进制文件

### Java 9

### Javac 二进制文件

### Java 开发工具包

#### Android Studio

### Java 管理布局

### Java 运行时环境

### Java 虚拟机

### JetPlayer

### K

### Kotlin

### L

##### 布局

#### Android

#### 容器

#### 网格

#### 表格

#### LinearLayout

#### 选项

#### 限定符

### Linting 工具

### Linux

### 有损压缩或编码

### M

### MacBook Air

`main()` 方法

### 基于 MotionLayout 的应用

### MotionLayout 设计

`mp.setAudioStreamType()` 方法

### N

### 原生开发工具包

### 非 Android Java 应用

### 通知

#### API 级别 26

#### 能力

#### 设备

#### 图标

#### NotificationBuilderExample 应用

#### NotificationManager 对象

#### 对象

#### 简单布局

#### 支持逻辑

### O

### 面向对象设计

### 对象关系映射

### Olive

`onAttach()` 方法  
`onBind()` 方法

### OnCheckedChangeListener

`onClick()` 方法  
`onCreate()` 回调  
`onCreate()` 方法  
`onCreateView()` 方法  
`onDestroy()` 方法  
`onDestroyView()` 方法  
`onDetach()` 方法  
`onDowngrade()` 方法  
`onInflate()` 方法  
`onPause()` 方法  
`onPrepared()` 回调方法  
`onReceive()` 方法 (命名为 `onReceive()`)  
`onRestart()` 方法  
`onRestoreInstanceState()` 方法  
`onResume()` 方法  
`onRetainNonConfigurationInstance()` 方法  
`onSaveInstanceState()` 方法  
`onStartCommand()` 方法  
`onStart()` 方法  
`onStop()` 方法  
`onViewStateRestored()` 方法  
`openFileInput()` 和 `openFileOutput()` 方法

### 开放手机联盟

`openRawResources()` 方法

### OpenShot

### 开源工具

#### Audacity

#### Blender

#### DaVinci Resolve

#### GIMP

#### Inkscape

#### LAME

#### Olive

#### OpenShot

### P

### PendingIntent

### 视频回放

#### Android

#### 控制视频，代码

#### VideoView

`println()` 方法

### Q

`query()` 方法

### R

### RadioButton 控件

`raiseNotification()` 方法

### 基于原始资源的方法

`readLine()` 方法

### 接收器

#### 限制

#### 用户界面

#### 面向用户的应用

### RelativeLayout

#### 操作

#### 定位属性

#### 属性

#### 控件位置

#### 控件重叠

#### 工作原理

### 资源定义文件

#### 更改颜色

#### 描述性

### S

### Samsung Remote Test Lab

### 服务到客户端通信

`bindService()`  
回调/监听器  
独立消息传递  
Intent/广播接收器  
通知  
使用 `Messenger`/`Messenger` 对象  
使用 `PendingIntent` 对象

`setAnchorView()` 方法  
`setAudioSource()` 方法  
`setContentView()` 方法  
`setLatestEventInfo()` 方法  
`setLights()` 方法

### 一次性方法

`SmartFileSaver.execute()` 方法

### 软件开发工具包

### 源代码控制

### SQLite

#### ACID 数据原则

#### 内置工具

#### 数据库应用

#### 数据库驱动的活动

#### 定义

#### 示例应用

#### 特性

#### 修改数据

#### 删除数据

#### 插入数据

#### 更新数据

#### 打包/管理

#### 查询方法

#### Room 持久化库

#### 第三方数据库工具

`startService()` 方法  
调用 `stop()` 方法  
`stopService()` 命令

### Switch 控件

`System.console()` 方法  
`System.out.println()` 方法  
`System.print()` 方法  
`System.readln()` 方法

### T

### TableLayout

### TextView 控件

### U

### UI 控件

#### 按钮

#### CheckBox

#### EditText

#### ImageView/ImageButton

#### RadioButton

#### Switch

#### TextView

`update()` 方法  
`updateClickCounter()` 方法

### 用户账户控制

### V

### 视频

### 比特率

### 编解码器

#### 容器/子代码

### 帧率

#### HandBrake

#### VideoView

`viewContact()` 方法

### 视图派生控件

#### 属性

#### 特性

### `View.OnClickListener`

### W

`welcome_text` 值

### X， Y， Z

### XML 布局

### Java 代码

### XML 路径

### XQuery

### XSLT
