# 索引

- **A**
  - 加速计工具
  - 自适应磁贴（`AddAdaptiveTiles()` 事件处理方法、绑定元素、`createTileUpdaterForApplication().update()` 方法、`default.html` JavaScript 文件、`LoadTileandNotifications.js` 文件模板、`TileLarge` 绑定、UWP 应用 `Windows.UI.Notifications.TileNotification()` 工作流程、`XmlDocument` 对象、XML 模式语法、XML 模板）
  - 自适应 UI 技术（流体布局）
  - `app.onactivated` 函数
  - `default.html` `formatDateString` 函数
  - `ListView` 控件（输出屏幕、项目对话框、`resizeListView` 脚本文件引用、`weatherData.js`）
  - 重新定位 CSS 文件（CSS 媒体查询、`default.css`、`default.html`、设备目标/屏幕输出、屏幕项目对话框、Windows Mobile 工作流程）
  - 附加工具（加速计工具、附加工具、功能模拟器、功能位置选项卡、模式、多点触控输入、近场通信、网络选项卡、真实世界交互、模拟鼠标输入、步骤、通用 Windows 平台工具、工作流程）
  - 广告中介控件
  - 应用程序清单文件（功能、内容 URI、声明、包编辑、打包步骤、选项卡、可视化资产、XML 文件）

- **B**
  - 后台任务（`BackgroundTaskBuilder` 类、`BackgroundTaskRegistration.allTasks` 属性、代码片段、`mytask.js` 文件、`package.appxmanifest`、解决方案资源管理器、系统条件、`SystemTrigger`、任务完成、`WebUIBackgroundTaskInstance.current` 属性）
  - `BackgroundTaskBuilder` 类
  - `BackgroundTaskProgressEventArgs` 对象
  - `BackgroundTaskRegistration.allTasks` 属性
  - 锁屏提醒通知
  - 蓝牙设备（`DeviceCapability` 元素、GATT 设备、`Package.appmanifest` 文件、Windows 应用）
  - 内置地图应用（Bing Maps 控件、AJAX Control 7.0、`default.html` 文件、`default.js`、`loadModule`、`map.entities.push` 方法、移动和 Web 应用、引用管理器、Windows 应用工作流程、`default.html` 文件、`default.js`、桌面窗口、方向指示、`bingmaps`、`default.html` 文件、`default.js`、桌面窗口、`LaunchUriASync` 方法、Windows 设备、Windows Mobile 显示地图参数、Windows Mobile 应用）

- **C**
  - 控件
    - AppBar 控件（`appbarevents.js`、`default.html`、页面功能、属性、语法、Windows 应用）
    - FlipView 控件（创建、`data.js` 文件、`itemDataSource` 属性、WinJS 应用）
    - ListView 控件（单元格跨越布局、创建通用应用、`data.js` 文件、筛选功能、网格布局、分组项目、列表数据源、列表布局、`SemanticZoom`）
    - Repeater 控件（`data.js`、`default.html` 页面、HTML 表格、项目列表、员工、Windows Mobile 模拟器）
    - ToolBar 控件（创建、`default.html` 页面、`default.js` 文件、属性、设置、Windows 应用）
  - 每千次展示成本 (CPM)

- **D, E**
  - 数据绑定和导航
    - 数据绑定系统（业务对象、数据属性、`processAll()` 方法、项目对话框 Windows Mobile 输出、工作流程、样式属性数据属性、`default.html`、`default.js`、DOM 元素、项目对话框 Windows Mobile 输出、模板）
    - 数据项（`default.html`、`default.js`、DOM 元素、项目对话框 Windows Mobile 屏幕、WinJS 控件）
    - `data.js` 文件（数据模板、`default.html`、`ListView`、项目对话框、样式表定义、Windows Mobile 屏幕）
  - 数据存储技术
    - 创建和删除应用（`btnSaveClick` 方法、容器、`createContainer` 方法、`DataStorageDemo.js` 文件、`default.html`、js 文件、层级、本地应用数据存储、Windows 10 应用）
      - 文件处理（`btnDataToFile` 和 `btnCreateAFile`、回调函数、`createFileAsync` 方法、创建、读取和写入选项）
      - `DatastorageDemo.js` 文件、`default.html` 文件、默认屏幕、本地文件夹、本地持有文件 `MyFile.txt`、`readTextAsync` 方法、漫游文件夹、临时文件夹、通用 Windows 应用、`Windows.Storage.StorageFile` 类概述）
    - 读取应用数据（`ApplicationDataContainer.values` 创建、`DataStorageDemo.js` 文件、`default.html` 页面、函数、存储和检索应用、Windows 10）
    - 注册和实现（`ApplicationData.DataChanged` 事件、`applicationData` 对象、`DatastorageDemo.js`、`default.html` 代码、事件处理程序、工作流程）
    - 检索本地复合（`ApplicationDataCompositeValue` 类、复合值类、Windows 10 应用）
    - 漫游应用数据存储（`ApplicationDataContainer.values`、`btnCreateRoamingContainerClick` 函数、创建和读取数据、`createContainer` 方法、`DataStorageDemo.js`、`default.html` 页面、`roamingSettings.containers.hasKey` 函数、`roamingSettings.createContainer`、Windows Mobile 输出）
  - 定义类
  - 设计断点
  - 设备功能
    - 应用包清单（`app package.appmanifest` 文件、`DeviceCapability` 元素、问题、Visual Studio XML 编辑器）
    - 音频流和输出语音（音频选项、`audio_SetUp()` 方法、控件、调试模式、`default.html` 页面、下拉控件、`errorMessage()` 方法、纯文本、`ready()` 函数、`speakFn()` 函数、`SpeechSynthesise.js` 文件、`StatusMessage` 错误消息、文本转语音应用、`voicesSelect` 下拉控件、`Windows.Media.SpeechSynthesis.SpeechSynthesizer()`）
    - 蓝牙设备（`DeviceCapability` 元素、GATT 设备、`Package.appmanifest` 文件、Windows 应用）
    - Cortana 语音命令（`activatedEventArgs`、`default.js`、前台、VCD 文件、工作流程、XML 代码）
    - 查找可用性（`default.html` 页面、`DevicePicker.js`、获取枚举、`ready()` 函数、`showDevicePicker()` 方法、UWP 应用、`Windows.Devices.Enumeration.DevicePicker` 类、工作流程）
    - 语音识别（约束条件、`buttonSpeechRecognizerListConstraintClick` 函数、创建、自定义列表、`default.html` 文件、`Speechrecognisation.js` 文件、`speechRecognizer.compileConstraintsAsync()` 方法、UWP 应用、工作流程）
  - 诊断工具（调试菜单、不同选项、因素、GPU 使用率工具、UI 响应性工具的结果、Visual Studio 探查器、Windows 应用）
  - 显示文本（DOM 资源管理器、计算选项卡、调试目的、DOM 资源管理器选项卡、HTML 元素、布局选项卡、刷新 Windows 应用、调试时的 Visual Studio、工作流程）

- **F**
  - 函数作用域

- **G**
  - Getter 方法
  - 获取 HTML 文档
  - 全球化
    - 日期和时间格式化（`DateTimeFormatter`、`default.html` 文件、`onactivated` 方法、输出定义）
    - 数字和货币格式化（`default.html`、`NumberFormatting`、`onactivated` 方法、输出）
    - 资源字符串（添加其他语言、`app.onactivated` 函数、`default.html` 文件、`default.js` 文件、默认语言设置英语、资源文件文件夹创建、法语区域性、`package.appxmanifest` 文件、运行应用、通用应用）
  - 全局作用域
  - `GroupData`
  - `GroupKey`
  - `GroupSorter`

- **H**
  - 中心导航（`default.html`、`default.js` 文件、元素、层次结构模式、输出项目对话框）
  - 超链接（HTML 导航模型、`newpage.html`、工作流程）

- **I**
  - `IBackgroundTaskRegistration` 对象
  - 物联网 (IoT)

- **J, K**
  - JavaScript 控制台窗口
    - 命令（`console.log` 方法、控制台窗口、`default.js` 文件、开发者、错误/写入调试消息、本地计算机选项、日志消息模板、通用 Windows 平台应用、Visual Studio 菜单调试、警告和错误、工作流程）

- **L**
  - 标签
  - `LaunchUriASync` 方法
  - 生命周期和导航模型
    - 超链接（HTML、`newpage.html`、工作流程）
    - `SessionState`（应用激活、`default.html` 页面、`default.js` 文件、页面显示、使用 Visual Studio、`WinJS.Application.sessionState`、工作流程）
    - 单页导航模型（参见：单页导航模型）
    - 状态和事件（`ActivationKind` 枚举、激活方法、应用执行状态、代码、`default.js` 文件、演示、JavaScript 控制台窗口、通用 Windows 应用、Windows 应用、`WinJS.Application.addEventListener` 方法、`WinJS.Application.checkpoint` 事件、工作流程）
    - 终止和恢复（`ApplicationExecutionState` 枚举、`default.js` 文件、`detail.previousExecutionState` 属性、`ExecutionState`、JavaScript 控制台窗口、工作流程）
    - 未处理异常（`default.js` 文件、处理应用、Windows 桌面、`WinJS.Application.error`、工作流程、`WinJS.Application.sessionState`）
  - 本地化
    - WinJS 控件（`data-win-res`、`default.html`、`onactivated` 方法、输出、资源文件、字符串、Visual Studio）
  - 位置
    - 当前位置（`default.html` 文件、`default.js`、`getGeopositionAsync` 方法、`getLocation` 方法、位置提示、`package.appxmanifest` 文件、Visual Studio、Windows 应用、Windows Mobile 应用、工作流程）
    - 检测用户位置 (HTML5)（`default.html` 文件、`default.js`、错误消息、`getCurrentPosition` 方法、`getLocation` 方法、`Onsuccess` 方法、`package.appxmanifest` 文件、问题、更新、W3C Geolocation API、`watchPosition` 函数、`watchPosition` 方法、Windows 应用商店应用、WinJS 应用、工作流程）
    - Geolocator 位置更新（`default.html` 文件、`default.js`、桌面窗口、`getGeopositionAsync` 方法、`getStatus` 方法、`positionchanged` 事件、问题、`Starttracking` 方法、Windows Mobile 模拟器、工作流程）

- **M**
  - 地图（参见：内置地图应用）
  - 主/详细导航模式（`app.onactivated` 方法、`data.js`、`default.html`、`default.js`、`detail.html`、`detail.js`、详细信息屏幕、`ListView`、`master.html`、`master.js`、主屏幕、工作流程）
  - Microsoft Azure 移动服务
    - CRUD 操作（`default.js` 文件、`del()` 方法、`insert()` 方法、`MobileServiceClient` 构造函数、远程服务、todo 数据库表、`update()` 方法）
    - 数据库表创建（数据检索、创建新移动服务、服务器脚本验证、数据排序、WinJS 客户端库）
    - `MobileServiceClient` 构造函数
    - `MobileServiceTable`
  - 盈利
    - 广告中介控件（扩展和更新对话框、应用内广告、显示广告）
    - ApplicationID 和 AdUnitId（编译并运行程序、`default.html` HTML 文件、安装、中介控件、引用管理器对话框、UWP 应用、Windows 广告中介）

- **N, O**
  - 导航结构
    - 元素层级结构
    - 中心控件（`default.html`、`default.js` 文件、元素、层次结构模式、输出项目对话框）
    - 主/详细导航模式（参见：主/详细导航模式）
    - 页面/屏幕（`Pivot` 控件、内容类别、`default.html`、对等、项目对话框、Windows Mobile 输出）
    - SplitView 控件（内容区域、`default.html`、`default.js`、帮助类、窗格、项目对话框、工作流程）
  - 近场通信 (NFC)
  - 通知（参见：Toast 通知）

- **P, Q**
  - 照片库应用

- **R**
  - 远程 Windows 10 设备（应用包、启用开发人员模式、安装、包创建、包文件、远程连接、屏幕）

- **S**
  - `SessionState`（应用激活、`default.html` 页面、`default.js` 文件、页面显示、使用 Visual Studio、工作流程）
  - Setter 方法
  - 共享数据
    - 应用声明（数据类型指南、`package.appxmanifest` 文件、项目标题、共享超级按钮、Windows 共享目标、时间管理报告应用、工作流程）
    - 自定义数据格式类型（应用 UI、`DataSharingDemo.js`、`default.html`、JSON 和 XML 格式、问题、接收器应用、`request.data.setData` 函数、`set data` 方法、`shareDataHandler` 函数、`activatedShareHandler` 函数、工作流程）
    - 事件处理程序（`datarequested` 事件侦听器方法、`DataSharingDemo.js` 文件、`DataTransferManager` 对象、`DataTransferManager.getForCurrentView()` 函数、`default.html`、源应用）
    - 处理共享激活（`activatedShareHandler`、`DataSharingDemo.js` 文件、`default.html`、`dispReceivedContent()` 函数、接收纯文本、共享超级按钮、窗口、`shareOperation` 对象、目标应用、Windows Universal 模板、工作流程）
    - 图像应用（`default.html`、演示屏幕、`FileOpenPicker` 方法、前面代码、问题、项目标题、`selectImage` 函数、`selectImage()` 文件夹、`selectImageButton` 控件、选择、`shareDataHandler` 函数、`shareDataHandler()` 方法、标题和描述字段、通用 Windows 应用概述）
    - 接收图像共享（`bitmaps` 方法、`DataSharingDemo.js`、事件侦听器函数、`getBitmapAsync()` 方法、`package.appxmanifest`、问题、`shareOperation.data.getBitmapAsync()` 方法、目标应用、工作流程）
    - 共享合约（共享纯文本、数据、超级按钮窗口、`DataSharingDemo.js` 文件、演示屏幕、事件参数、OneNote、共享数据、问题、属性、`request.data.title`、用户界面、工作流程）
    - 网页链接（`DataSharingDemo.js` 文件、`default.html`、`request.data.setWebLink()` 函数、`request.data.setWebLink()` 方法、标题和描述字段、Windows 应用程序）
  - 旁加载应用（`Add-AppDevPackage` 文件、确认对话框、开发人员模式、安装消息、包和证书、`Add-AppDevPackage` 文件、`Add-appxpackage` 命令、浏览按钮、证书屏幕、导入过程、安装弹出屏幕、存储位置选择、Windows 桌面、工作流程、PowerShell 菜单步骤、传统桌面应用、通用 Windows 应用、Windows 设备）
  - 单页导航模型（后退按钮、`default.html` 页面、文件、`home.html` 页面、`href` 属性、`navigator.js` 文件、`page1.html` 页面、`page1.js` 文件、`pagecontrol`、`home.html` 文件、`PageControlNavigator` 控件、页面、最简单的解决方案、通用 Windows 应用、Visual Studio、Windows 8.1 项目、`WinJS.Navigate.navigate()` 方法、`WinJS.Navigation` 类、工作流程）

- **T**
  - 磁贴
    - 自适应磁贴（`AddAdaptiveTiles()` 事件处理方法、绑定元素、`createTileUpdaterForApplication().update()` 方法、`default.html` JavaScript 文件、`LoadTileandNotifications.js` 文件模板、`TileLarge` 绑定、UWP 应用 `Windows.UI.Notifications.TileNotification()` 工作流程、`XmlDocument` 对象、XML 模式语法、XML 模板）
    - 默认创建（应用程序 `package.appmanifest`、`package.appxmanifest`、问题、工作流程）
  - Toast 通知
    - 操作（`AddInteractiveToastNotification()` 方法、`default.html` 元素、`GetControl()` 方法、交互式应用、问题、工作流程）
    - 锁屏提醒（`AdaptiveTiles()` 方法、`BadgeNotificationto`、`default.html`、`GetControl()` 方法、`LoadTileandNotifications.js`、磁贴、`UpdateTileBadge()` 方法）
    - 计划的磁贴和 Toast 通知（`AddScheduledTileNotification()` 方法、`default.html` 元素、交互式、`LoadTileandNotifications.js`、计划的自适应磁贴、工作流程）
    - 可视化内容（`AddToastNotification()` 方法、`default.html`、`GetControl()` 方法、交互式、`ToastNotificationManager`、UWP 应用、工作流程）
  - `ToastNotificationManager`
  - `ToggleSwitch` 控件
  - `Tooltip` 控件

- **U**
  - UWP 应用创建（`default.html`、开发工具、设备系列、层次结构、输入处理、js 文件、在本地计算机上运行、移动设备、Windows 10 模拟器、Windows 10 模拟器、解决方案资源管理器、通用控件）
  - 通用 Windows 平台 (UWP)（另请参阅：导航结构）
    - 应用程序包（`appxupload` 包、应用包、应用名称选择、应用包向导完成对话框、上下文菜单、创建、清单文件、包详细信息、配置、远程计算机、Windows 开发人员中心帐户）
    - 后台任务（参见：后台任务）
    - Windows 10（工作流程）

- **V**
  - 语音命令定义 (VCD) 文件

- **W**
  - `WebUIBackgroundTaskInstance.current` 属性
  - Windows 10 Mobile 设备（参见：附加工具）
  - Windows 广告中介（参见：盈利）
  - Windows 应用认证工具包 (WACK)（选择、解决方案、开始菜单、应用商店应用选项、测试结果、适用的测试、通用 Windows 平台应用、验证、工作流程）
  - Windows 应用开发者帐户（访问、帐户注册、Visual Studio、Windows 开发人员中心、开发者帐户、在线上传应用、Windows 应用商店、`appxupload` 包、Windows 开发人员中心仪表板流程）

- **WinJS**
  - `WinJS.Class.define` 方法
  - `WinJS.Class.derive` 方法
  - `WinJS.Class.mix` 方法
  - WinJS 控件
    - 附加属性（`data-win-control` 属性）
    - `DatePicker` 控件（`controldemo.js` 文件、`datePattern` 属性、声明、`default.html`、`monthPattern` 属性、`yearPattern` 属性、声明）
    - 显示文本（编辑文本、密码输入框、富文本框、文本区域、文本框）
    - 获取 HTML 文档（JavaScript 代码、`controldemo.js`、`default.html`、IntelliSense 支持、`maximumRating` 属性、前面代码、`Rating` 控件）
    - `TimePicker` 控件（`controldemo.js`、声明、`default.html`、`hourPattern` 属性、`minutePattern` 属性、`periodPattern` 属性）
    - `ToggleSwitch` 控件
    - `Tooltip` 控件（Windows Mobile 模拟器、Windows 平板电脑）
  - WinJS 功能
    - 创建类（创建 mix 方法、derive 方法、封装）
    - 命名空间（`Define` 方法、`Developerpublish` 函数、对现有命名空间的参数、`WinJSFundamentals.js`）
    - Promise 函数
    - `WinJS.Navigate.navigate()` 方法

- **X, Y, Z**
  - `xhr` 函数
