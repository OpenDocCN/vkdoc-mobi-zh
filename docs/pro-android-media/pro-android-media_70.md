# 索引

`Color.RED` 常量, 82
与权限, 24

合成, 图片, 69–78
`SurfaceView` 类用于, 24–25

`compress` 方法, 102

`CAMERA` 权限, 24, 37, 42

`ContentProvider` 类, 211

`Camera.AutoFocusCallback` 方法, 34

`Content.startActivity` 方法, 229

`Camera.ErrorCallback` 方法, 35

`Context` 类, 127

`Camera.OnZoomChangeListener` 方法,
`Context.getResources().getConfiguration()`
35
方法, 28

`Camera.Parameters` 类, 23, 27, 30–31

`Context.startActivityForResult` 方法, 229

`Camera.Parameters.setRotation` 方法, 28

对比度, 使用 `ColorMatrix` 类更改,
`Camera.PictureCallback.onPictureTaken`
67–69
方法, 32

控制播放, 111

`Camera.PreviewCallback` 方法, 34

`Copyright` 标签, 21

`Camera.ShutterCallback` 方法, 35

`create` 方法, 90, 110–111

`Camera.takePicture` 方法, 33

`createBitmap` 方法, `Bitmap` 类, 53, 64

`cancel` 方法, 163

`createRecording` 按钮, 153

`Canvas` 类, 79–92

`currentState` 变量, 119–120, 122

与圆形一起使用, 86

`Cursor` 类, 18, 116

创建, 81

自定义视频捕获, 235–250 *另请参阅*
创建 Bitmap, 79
用于视频的 `MediaRecorder`

`drawLine` 方法, 84

`CustomRecorder` 活动, 158

`drawPoint` 方法, 83

`drawText` 方法, 87–92

内置字体, 88–89

## D

`drawTextOnPath` 方法, 92

DAC（数模转换）, 179

外部字体, 91

`DATA` 列, 116, 283

字体样式, 90

`DATE_ADDED` 列, 116

与椭圆一起使用, 86

`DATE_MODIFIED` 列, 116

`Paint` 类, 82–83

`decodeStream` 方法, `BitmapFactory`
类, 52

`setColor` 方法, 82

`DEFAULT` 常量, 236–238

`setStrokeWidth` 方法, 83

`DefaultHandler` 类, 274, 276

`setStyle` 方法, 83

`DefaultHttpClient` 类, 278

与路径一起使用, 87

DFT（离散傅里叶变换）, 189

与矩形一起使用, 85

数模转换 (DAC), 179

捕获。*参见* 视频捕获

![](img/index-308_1.png)

**294**

**索引**

离散傅里叶变换 (DFT), 189

`EFFECT_POSTERIZE` 常量,
`Camera.Parameters` 类, 30

`do while` 循环, 214

`EFFECT_SEPIA` 常量,
`Camera.Parameters` 类, 30

`Document Object Model` (DOM), 273

`Document Type Definitions` (DTD), 273

`doInBackground` 方法, 165, 176, 180

`EFFECT_SOLARIZE` 常量,



