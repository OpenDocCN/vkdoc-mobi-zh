# 停止录制与媒体类

- `stopping recording`，244
- `MediaStore.Video.Thumbnails` 查询，213
- 视频帧率，239
- `message` 变量，287，289
- 视频尺寸，239
- 元数据

## MediaRecorder 类

- `MediaRecorder.AudioEncoder` 类，156
- 用于图像，10–22
- `MediaRecorder.AudioSource` 类，235
- 稍后添加，12
- `MediaRecorder.MEDIA_RECORDER_INFO_` 关联到图像，12–16
- `FILESIZE_REACHED` 常量，240
- 是文件的一部分，21–22
- `MediaRecorder.MEDIA_RECORDER_INFO_` 获取图像的 URI，11
- `MAX_DURATION_REACHED` 常量，240
- 预填充，11–12
- 用于视频，232–235
- `MediaRecorder.OutputFormat` 类，155，237
- 方法参数，257
- `MIC` 常量，236
- `MediaRecorder.OutputFormat.MPEG_4` 常量，155
- 使用 `Matrix` 类镜像图像，62
- `MotionEvent` 类，94
- `MediaRecorder.OutputFormat.RAW_AMR` 常量，155
- `MotionEvent.ACTION_CANCEL` 常量，94
- `MotionEvent.ACTION_DOWN` 常量，94
- `MediaRecorder.OutputFormat.THREE_GPP` 常量，156
- `MotionEvent.ACTION_MOVE` 常量，94
- `MotionEvent.ACTION_UP` 常量，94
- `MediaRecorder.VideoSource` 类，236
- `moveToFirst` 方法，18–19，117
- `MediaScanner` 服务，217
- `moveToNext` 方法，19，118

## MediaStore

- 用于音频，115–123
- `moveToPosition` 方法，120，122
- 从其中访问音频，115–117
- MP3 格式，106
- `MPEG_4` 常量，237
- 专辑浏览应用示例，118–123
- `MPEG_4_SP` 常量，238
- `MultipartEntity` 类，278–279，284，286
- 在其中浏览音频，118
- `MULTIPLY` 模式，73
- 向其中插入音频，167
- `Music` 应用，通过 Intent 使用，107–108
- 用于视频，211–218
- `Music` 目录，108
- 示例，212–218
- `myfavoritepicture.jpg` 文件，5
- 来自其中的缩略图，212

---

![](img/index-312_1.png)

**298**

