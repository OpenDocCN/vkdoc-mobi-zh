# 索引

## N

- `onServiceConnected` 方法，134
- `onServiceDisconnected` 方法，135
- `NETWORK_PROVIDER` 常量，264
- `onStart` 方法，111，128，130
- 网络音频，137–150
- `onStartCommand` 方法，128，130
- HTTP 播放，137–143
- `onStatusChanged` 方法，265
- RTSP 流媒体，150
- `onStop` 方法，111，265
- 通过 HTTP 流式传输音频，143–149
- `onTouch` 方法，93，95，98–99，185–186



`networked video`（网络视频），218–228

`OnTouchListener`（触摸监听器）活动，185

HTTP（超文本传输协议），218–219

`OnTouchListener`（触摸监听器）接口，93

使用 `MediaPlayer` 播放，221

`ORDER BY`（排序依据）子句，17, 116

使用 `VideoView` 播放，221

`ORDER BY`（排序依据）变量，121

RTSP（实时流传输协议），219–221

`org.apache.http` 包，252

`nojsoncal back` 参数，257

`org.json` 包，254

奈奎斯特，哈里（Nyquist, Harry），188

`OUT_OF_SERVICE`（服务中断）常量，266

`OutputStream`（输出流）类，286

■

使用 `Canvas`（画布）类绘制椭圆，86

**O**

`MediaRecorder`（媒体录制器）类的方法，156

Ogg 格式，106

■ **P**

`onActivityResult`（活动结果回调）方法，16, 48, 50, 73, 98,

`Paint`（画笔）类，82–83
153–154, 229–231

`setColor`（设置颜色）方法，82

`onBind`（绑定）方法，127–128

`setStrokeWidth`（设置线条宽度）方法，83

`onBufferingUpdate`（缓冲更新）方法，142, 222

`setStyle`（设置样式）方法，83

`onClick`（点击）方法，33, 40, 49, 98, 114, 141,

`Paint.setTypeface`（设置画笔字体）方法，90–91
153, 159, 231, 247

`Paint.Style`（画笔样式）类，83

`OnClickListener`（点击监听器）方法，14–15, 19, 231

`Paint.Style.FILL`（填充样式）常量，83

`onCompletion`（完成）方法，111, 113, 130,

`Paint.Style.FILL_AND_STROKE`（填充并描边样式）常量，83
141–142, 153–154, 161, 204–205

`Paint.Style.STROKE`（描边样式）常量，83

`OnCompletionListener`（完成监听器）接口，140

`Camera`（相机）类的参数，27–30

`onCreate`（创建）方法，13–14, 27, 44, 48, 119,

`Parameters.set`（参数设置）方法，27, 29
140, 159, 203, 209

`parse`（解析）方法，274

下载自 Wow! eBook <www.wowebook.com>

`onDestroy`（销毁）方法，128, 130

`parseButton`（解析按钮）按钮，145

`onError`（错误回调）方法，141, 205

`parsePlaylistFile`（解析播放列表文件）方法，145–146



`onInfo` 方法，`MediaRecorder` 类，240，

`parseResult` 方法，285

242

使用 `Canvas` 类的路径，87

`onItemClick` 方法，214

`pause` 方法，141， 148， 199

`onListItemClick` 方法，`ListActivity` 类，

PCM 格式，107

120

Pelletier, Claude，91

`onLocationChanged` 方法，265， 268

权限，与 `Camera` 类，24

`onPause` 方法，265

`photo` 元素，258

`onPictureTaken` 方法，32–33， 36

`PlayAudio` 类，174

`onPostExecute` 方法，176， 285

`playButton` 按钮，145， 147

`onPrepare` 方法，226

播放视频，195–210

`onPrepareAsync` 方法，226

通过 Intent，196–197

`onPrepared` 方法，142， 148， 204， 206，

使用 `MediaPlayer` 类，200–210

209

使用 `MediaControl` 添加控件

`OnPreparedListener` 接口，140， 206

类，208

`onProgressUpdate` 方法，165， 176, 192，

示例，202–208

285

状态，200–201

`onProviderDisabled` 方法，265, 269

支持的格式，195–196

`onProviderEnabled` 方法，265, 269

使用 `VideoView`，197–200

![](img/index-313_1.png)

**索引**

**299**

`PlaylistFile` 类，149

`removeUpdates` 方法，265

`playlistItems` Vector，145–148

表述性状态转移。*参见* REST

`playPlaylistItems` 方法，145, 147

`requestLocationUpdates` 方法，264–265，

`playRecording` 按钮，153–154, 159–161

267

`playVideoButton` 按钮，230–231, 233

`res` 文件夹，110–111

`PorterDuff.Mode` 类，70

`reset` 方法，205

`PorterDuffXfermode` 类，70, 75–78

`res/layout/main.xml` 文件，20, 37, 231, 234

`post` 方法，用于 `Matrix` 类，61

`res/layout/main.xml` 接口，45

`POST` 请求，252, 278

REST（表述性状态转移），

`postingResult` 变量，282

273–278

`postTranslate` 方法，`Matrix` 类，62

用 XML 表示数据，273

`pre` 方法，用于 `Matrix` 类，61

SAX 解析，274–278

`prepare` 方法，138，159, 162, 166, 204，


好的，作为一名高级文档工程师和翻译员，我将严格遵循您的注意事项和示例，将给定的英文文本翻译成中文。


