# 第 3 章：图像编辑与处理

**图 3–22.** *使用 `android.graphics.PorterDuff.Mode.SCREEN` 作为 `PorterDuffXfermode` 时，前述示例的输出结果*

## 本章小结

通过本章的学习，我们了解到，尽管 Android 主要是面向尺寸、内存和处理器能力受限设备而设计的操作系统，但它依然提供了相当复杂的图像处理支持。我们探讨了许多处理现有图像的能力，但对图像能力的探索并未止步于此。在下一章中，我们将介绍一些 API，这些 API 允许我们从头开始创建图像，并在利用触摸屏等其他传感器时进行进一步的操作。

