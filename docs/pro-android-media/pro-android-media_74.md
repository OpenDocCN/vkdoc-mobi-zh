# 索引

**localName 变量**, 277, 287

**LocationManager 类**, 264–265, 267

`LocationManager.NETWORK_PROVIDER` 常量, 266

`initRecorder` 方法, 246–248

`Log` 命令, 12

`InputStream` 类, 279–280

`InputStreamReader` 类, 279

`inSampleSize` 参数, 6

`insert` 方法, 167

`Intent.ACTION_VIEW` 常量, 196

intents（意图）
- 使用意图进行音频捕捉, 151–154
- 使用意图播放视频, 196–197
- 使用意图录制视频, 229–232

`INTERNAL_CONTENT_URI` 常量, 11

`INTERNET` 权限, 262

`invalidate` 方法, 95

`IOException`, 25, 138

## J, K

`javasource` 目录, 189

`jfftpack.tgz` 文件, 189

JSON (JavaScript Object Notation), 254–272
- 使用 JSON 拉取 Flickr 图片, 257–272

## L

`LocalService`
- 示例, 126–129
- 与 `RemoteService` 对比, 126

`localName` 变量, 277, 287

`LocationManager` 类, 264–265, 267

`LocationManager.NETWORK_PROVIDER` 常量, 266

`initRecorder` 方法, 246–248

`Log` 命令, 12

`lon` 参数, 268

## M

`main.xml` 文件, 16, 127, 131, 149, 161, 207, 216, 262, 271

`makeURL` 方法, 262

`managedQuery` 方法, 17, 19, 116, 118–119, 121, 212

`Matrix` 类
- 在创建 `Bitmap` 时应用, 64–65
- 翻转操作, 63
- 镜像操作, 62
- 概述, 55–58
- 前置和后置方法, 61
- `setRotation` 方法, 58–59
- `setScale` 方法, 60
- `setTranslate` 方法, 61

`MediaController` 类，在使用 `MediaPlayer` 类播放视频时添加控制

`MediaPlayer` 类
- 绑定操作, 132–137
- 实现方法, 129–132

`MediaStore` 内容提供者
- 稍后添加, 12
- 关联到图片, 12–16
- 作为文件的一部分, 21–22
- 获取图片的 URI, 11
- 预填充, 11–12
- 检索已保存的内容, 12
- 使用 `MediaStore` 进行检索, 16–18
- 使用相册应用进行选择, 47–52

`initRecorder` 方法, 246–248



使用 `location` 作为 `request` 的一部分：

`JSONArray` 构造函数，256  
`JSONException`，256  
`JSONObject` 类，255  
`JSONObject` 构造函数，256  

`MediaController` 视图，208  
`MediaPlayer` 类，109–115  
`MediaPlayer` 构造函数，137  
`MediaRecorder` 类，154–166  
用于视频的 `MediaRecorder`，235–245  
`MediaStore` 类，2，12，15，18，33，283  
`MediaStore` 查询，215  
`MediaStore.Audio` 包，115  
`MediaStore.Audio.Albums` 类，119  
`MediaStore.Audio.Albums.ALBUM` 常量，119  
`MediaStore.Audio.Media` 类，151，167  
`MediaStore.Audio.Media.DATA` 常量，167  
`MediaStore.Audio.Media.RECORD_SOUND_ACTION` 动作，153  
`MediaStore.Images.Media` 类，17  
`MediaStore.MediaColumns` 类，211  
`MediaStore.Video` 类，211  
`MediaStore.Video.Media` 类，211  
`MediaStore.Video.Media` 查询，213  
`MediaStore.Video.Media._ID` 字段，211–212  
`MediaStore.Video.Media.DATA` 变量，211  
`MediaStore.Video.Media.EXTERNAL_CONTENT_URI` 常量，211–212  
`MediaStore.Video.Thumbnails` 类，211  

`Camera.Parameters` 类，31  
`Camera.Parameters` 类中的 `LARGEST_HEIGHT` 常量，31  
`Camera.Parameters` 类中的 `LARGEST_WIDTH` 常量，31  

`ListActivity` 类，120  
`ListView` 布局，119  

`VideoView`，使用其播放视频，199–200  

**索引**，297  

**L**  
* 用于视频的 `MediaRecorder`，音频和视频比特率，238；音频和视频编码器，237–238；音频和视频源，235–236；音频声道，239；音频采样率，238–239；最大时长，240；最大文件大小，240；输出文件，242；输出格式，236–237；权限，245；准备录制，244；预览 Surface，242–243；配置文件，241–242；释放资源，244；开始录制，244；状态机，244–245  
* `MediaStore` 类，2，12，15，18，33，283；查询，215  
* `MediaStore.Audio` 包，115  
* `MediaStore.Audio.Albums` 类，119；`ALBUM` 常量，119  
* `MediaStore.Audio.Media` 类，151，167；`DATA` 常量，167；`RECORD_SOUND_ACTION` 动作，153  
* `MediaStore.Images.Media` 类，17  
* `MediaStore.MediaColumns` 类，211  
* `MediaStore.Video` 类，211  
* `MediaStore.Video.Media` 类，211；查询，213；`_ID` 字段，211–212；`DATA` 变量，211；`EXTERNAL_CONTENT_URI` 常量，211–212  
* `MediaStore.Video.Thumbnails` 类，211  

**MediaPlayer**  
* 控制播放，111–115  
* 创建对象，110  
* 使用其播放网络视频，221  
* 使用其播放视频，200–210  

**MediaController**  
* 使用其添加控件，类，208  
* 示例，202–208  
* 状态，200–201  

**MediaRecorder**  
* 示例，157–161  
* `setAudioEncoder` 方法，156  
* `setAudioSource` 方法，155  
* `setOutputFile` 方法，156  
* `setOutputFormat` 方法，155  
* 状态，156  

**布局文件**  
* `layout/main.xml` 文件，13，51  
* `list_item.xml` 文件，216–217，261–262  

**本地服务**  
* 与其绑定，132–137  
* 在其中实现，129–132  

**位置**（作为请求的一部分），208  

**参数**  
* 纬度（`lat`），268  

**参考文献**  
* 208（索引 126）  
* 222（索引 126）  
* 256（索引 152）  
* 270（索引 152）  
* 109–115（索引 78）  
* 123（索引 78）  
* 125（索引 78）  
* 110（索引 78）  
* 124（索引 78）  
* 200–210（索引 122）  
* 214（索引 122）  
* 200–201（索引 122）  
* 202–208（索引 123）  
* 216–217（索引 131）  
* 261–262（索引 131）  
* 120（索引 78）  
* 134（索引 78）  
* 119（索引 78）  
* 133（索引 78）  
* 31（索引 20）  
* 45（索引 20）  
* 151（索引 92）  
* 165（索引 92）  
* 167（索引 101）  
* 181（索引 101）  
* 17（索引 11）  
* 31（索引 11）  
* 211（索引 128）  
* 225（索引 128）  
* 213（索引 130）  
* 227（索引 130）  
* 211–212（索引 128）  
* 154–166（索引 94）  
* 168（索引 94）  
* 157–161（索引 96）  
* 171（索引 96）  
* 155（索引 95）  
* 169（索引 95）  
* 156（索引 95）  
* 170（索引 95）  
* 235–245（索引 143）  
* 249（索引 143）  
* 238（索引 144）  
* 252（索引 144）  
* 237–238（索引 144）  
* 251（索引 144）  
* 235–236（索引 143）  
* 239（索引 145）  
* 253（索引 145）  
* 238–239（索引 144）  
* 240（索引 145）  
* 254（索引 145）  
* 242（索引 146）  
* 256（索引 146）  
* 236–237（索引 143）  
* 245（索引 148）  
* 259（索引 148）  
* 244（索引 147）  
* 258（索引 147）  
* 242–243（索引 146）  
* 241–242（索引 146）  
* 268（索引 152）  
* 282（索引 152）  
* 297（索引 152）  
* 311（索引 152）



