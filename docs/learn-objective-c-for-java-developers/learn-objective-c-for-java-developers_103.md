# 排版后内容

`filtering collections`（过滤集合），312

`NSNotificationCoalescingOnSender` 类，
线程安全与，314

`NSSocketPort` 类，213，231

`NSNotificationNoCoalescing` 常量，334

`NSSocketPortNameServer` 类，225，227

`NSNotificationQueue` 类，329，332—334

`NSNull` 类，123

`NSSortDescriptor` 类，312

`NSNumber` 类，118，122

`NSStream` 类，215，216
属性列表与，203，206

`NSString` 类，119，123—128，203，206
`sorting collections`（排序集合）与，311
C 字符串与，126—128
流与，215
便捷构造函数与，53

`NSNumberFormatter` 类，131，132
异常与，241

`NSObject` 类，29，223，229，230
文件操作与，178
类别与，84
路径/文件名方法与，165
类成员测试与，149
`sorting collections`（排序集合）与，311
延迟消息与，93
线程名称与，264
即时消息与，92

`NSTableDataSource` 类，395
键值编码与，152

`NSTableView` 类，395
对象构造与，49

`NSTask` 类，214

`NSObject` 协议，231，322

`NSTextField` 类，319，372

`NSObjectController` 类，394，399

`NSThread` 类，257
通知与，268

`NSOpenGLView` 类，382
属性，263—264

`NSOpenPanel` 类，171
启动线程与，258

`NSOperation` 类，68
挂起线程与，260
异常与，241
任务与，280

`NSTimeInterval` 类，199，260

`NSOperationQueue` 类，68，280

`NSTimer` 类，282

[www.it-ebooks.info](http://www.it-ebooks.info/)

