# 索引

## I

`Iterator`模式，306—310

**iTunes**（Apple），MVC 设计模式与，

标签，24

懒加载，403—410，430

懒单例，430

## J

`Launch Services`框架文件系统

`Java`

函数，`“LS”`前缀，181

与`C`对比，11—26

遗留数据模型，394

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引

遗留异常处理，247

编程模式，用于，440—447，

长度修饰符，130

轻量级构造函数，403

菜单动作，392，394

线条（`lines`）

绘制，375

菜单项（`menu items`）

动作与，392，394

`listeners`

禁用，393

缺席行为与，112

消息标识符，392

通知与，325—329

消息（`messages`）

本地化，255，402

动作，391，354，364

`loggers`，缺席行为与，112

编译，88—90

逻辑坐标，374

延迟，92，211

`long`关键字，13

即时，91

`Love, Tim`，4

网络与，231—237

低级通信，212—217

发送给对象。*参见* 发送

`“LS”`，作为`Launch Services`前缀

消息给对象

文件系统函数，181

单进程通信与，

■ M

通过排序收集，311

术语与，30

`Mach`端口，212，213

元数据。*参见* 内省

分布式通知与，336

方法调用，30

分布式对象与，226

方法作用域，42

主运行循环，92，265

方法（`methods`）

分布式通知与，336

访问器，35—40，111

事件与，386

歧义，89

主线程，92，260，465

用方法增强类，82

映射（`maps`），297—299

调用。*参见* 发送消息给对象

`Matrix`（示例）类，411—428

类，45—47

单位矩阵与，422

构造函数，47—53

Java 实现，411—413

委托，180

矩阵运算与，417

目录内容，173

Objective-C 实现，414

编码，190

`finalize`，53

MVC 设计模式的调节变体，

实例，28，45

355

内省用于，158

成员运算符（`.`），17，40

隔离，81

成员变量，17，34

用于文件级操作，177

命名约定，31—33

基本规则，455

”未找到方法”编译器警告

C 与，462

与，89

`Core Foundation`与，464

面向对象方法调用与，94

释放与，445

进程通信与，212—231

属性，175

`vs.`垃圾回收，435，440，454

克服编码问题，`setValue`，35

447—452

符号链接，172

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引

术语与，30

`Interface Builder`用于，360，361—369

测试，147，467

主 NIB 与，361，401

未声明，88

所有者对象与，367

未实现，100—102

`nil`

值，35

类构造与，50

可变参数，在以下中实现，

类设计/代码简化与，108—113

`void`，33

字典与，298

减号（`-`）前缀实例方法，

消息返回值与，107

28，45

`NSEnumerator`类与，307

`Model-View-Controller`。*参见* MVC 设计

`NSMutableArray`类与，294

模式

发送消息给对象与，104—

模块化，MVC 设计模式与，357

包装，123

`Moore, Charles`，108

鼠标事件，388

`Nil`常量，157

鼠标跟踪，389

“未找到方法”编译器警告，89

互斥信号量（`mutexes`），

`nonatomic`属性，38，39

270—280

虚无，与虚无一致设计模式与，113

MVC 通信，354

通知中心，263

MVC 设计模式，59，353—402

分布式通知中心与，336，337

优势，357

通知队列与，332

绑定与，65

观察者与，329

工作原理，354—358

通知（`notifications`），179，211，325—337

MVC 通信与，354

异步，332，333，336

变体，355—356

合并，334

出队，334

■ N

分布式，217，334—337

键值观察与，339—352

命名约定

消息与，90

用于类，41

通知的命名约定，330

用于实例变量，34

观察者接收通知的标准，330

用于方法，31—33，34

入队，332—334，336

用于通知，330

同步，发布，329

用于参数，31

线程，268

用于协议，77

`NotSerializableException`，193

网络通信，231—237

`NS_...RETURN`预处理器宏，

网络连接，分布式对象

遗留异常处理与，248

与，227

新对象模式，用于内存


`NS_BLOCK_ASSERTIONS` 预处理器

`management`，441

`macro`，249

`NEXTSTEP` 操作系统，4

`NS_DURING` 预处理宏（旧版）

`NIB` 文档，61, 64, 67

异常处理与，248

连接与，363—367

`NS_ENDHANDLER` 预处理宏，

在其中创建对象，369

旧版异常处理与，248

自定义对象与，367

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引

`NS_HANDLER` 预处理宏（旧版）

线程安全与，314

异常处理与，248

`NSDirectoryEnumerator` 类，174, 307

`NSApplication` 类，385

`NSDistantObject` 类，223, 225, 228

动作与，392

`NSDistributedLock` 类，277

委托与，318, 320

`NSDistributedNotificationCenter` 类，

异常与，244

336, 429

运行循环与，265

`NSDocument` 类，188, 385

作为单例，429

基于文档的应用程序与，384

子类的创建，398

子类的创建，398, 399

`NSArray` 类，203, 205, 288, 292, 294

`TicTacToe` 示例应用程序与，368

集合过滤与，312

`NSDocumentController` 类，384, 398

线程安全与，314

`NSEnumerator` 类，307—309

`NSArrayController` 类，60, 67, 346, 396,

`NSError` 类，253—255, 256

`NSEvent` 类，386

`NSAssert` 宏，250

`NSEventTrackingRunLoopMode` 类，267

`NSBezierPath` 类，375

`NSException` 类，241

`NSBrowserView` 类，395

调用栈与，243

`NSBundle` 类，468

异常链与，243

`NSExceptionHandler` 类，245

框架与，470

`NSFastEnumeration` 类，309

本地化与，402

`NSFileHandle` 类，178, 184, 213

`NSButton` 类，357, 358

`NSPipe` 类与，213

`NSCell` 类，358, 396

与 `NSStream` 类的比较，216

`NSCoder` 类，185, 192

管道/套接字与，214

`NSCoding` 协议，185, 189

`NSFileManager` 类，164

`NSCollectionView` 类，396

委托与，180

`NSComparisionResult` 类，311

文件操作与，178

`NSConditionLock` 类，270, 273—277

作为单例，429

`NSConnection` 类，223, 224—228, 265

符号链接与，172

`NSConnectionReplyMode` 类，267

工作目录路径与，167

`NSControl` 类，371

`NSFormatter` 类，119, 131—134

动作与，392

`NSGarbageCollection` 类，141

`NSController` 类，67, 399

`NSGraphicsContext` 类，377

`NSCopying` 协议，208, 396, 432

`NSHashTable` 类，140, 291, 302

`NSCopyObject` 类，209

与 `NSHashTable`（C 类型）的比较，303

`NSCountedSet` 类，299, 301

集合集合与，299

`NSData` 类，119, 203, 206

`NSImage` 类，382

文件操作与，178

`NSIndexSet` 类，288, 294, 299, 301

垃圾回收与，121

`NSInputStream` 类，215

`NSDate` 类，199, 203, 206

`NSInteger` 类，473

集合排序与，311

`NSInternalInconsistency` 异常，248

线程与，260

`NSInvocation` 类，91, 94

`NSDateFormatter` 类，131, 133

`NSKeyedArchiver` 类，188, 190

`NSDefaultRunLoopMode` 类，267

`NSKeyedUnarchiver` 类，188

`NSDictionary` 类，205, 288, 297, 298

`NSLock` 类，270, 271

属性列表与，203

`NSMachBootstrapServer` 类，225, 227

[www.it-ebooks.info](http://www.it-ebooks.info/)

■ 索引

`NSMachPort` 类，213

`NSOrderedAscending` 类，311

`NSMachPortNameServer` 类，226

`NSOrderedDescending` 类，311

`NSManagedObject` 类，397

`NSOrderedSame` 类，311

`NSMapTable` 类，291, 297, 298

`NSOutlineView` 类，395

`NSHashTable` 类与，302

`NSOutlineViewDataSource` 类，395

强/弱引用与，140

`NSOutputStream` 类，215

`NSMethodSignature` 类，94, 159

`NSPipe` 类，213

`NSModalPanelRunLoopMode` 类，267

`NSPoint` 结构，121

`NSMutableArray` 类，288, 292, 294

`NSPointerArray` 类，140, 291, 292, 295

`NSMutableData` 类，119

`NSPort` 类，213, 224

`NSMutableDictionary` 类，288, 297, 298

`NSPortNameServer` 类，225

`NSMutableIndexSet` 类，288, 313

`NSProcessInfo` 类，467

`NSMutableSet` 类，288, 299, 300

`NSProgressIndicator` 类，342

`NSMutableString` 类，125

`NSPropertyListSerialization` 类，205

`NSMutableURLRequest` 类，237

`NSProxy` 类，223, 230

`NSNetService` 类，231

`NSRecursiveLock` 类，271

`NSNetServiceBrowser` 类，231

`NSRunLoop` 类，265, 268

`NSNotification` 类，211, 325—337

`NSSavePanel` 类，171

`NSNotificationCenter` 类，263, 329

`NSScanner` 类，119

`NSNotificationCoalescingOnName` 类，

`NSSet` 类，288, 299, 300



