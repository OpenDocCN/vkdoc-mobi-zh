# 索引

![images](img/sq.jpg) A

活动视图控制器

苹果 iOS 开发者中心网站



应用程序设置和用户默认设置

桥接控制应用程序（*参见* 桥接控制应用程序）

从应用程序更改默认设置

默认值注册

沉浸式应用

`NSUserDefaults`

读取设置

设置应用切换

设置捆绑包

ARC*. 参见* 自动引用计数 (ARC)

`AutocorrectionType`

自动引用计数 (ARC)

![images](img/sq.jpg) B

后台处理

`applicationDidEnterBackground` 方法

`beginBackgroundTaskWithExpirationHandler`

委托方法

`endBackgroundTask`

执行状态

活跃-非活跃

`applicationWillTerminate`

后台-非活跃

横幅

非活跃-活跃

非活跃-后台

信息应用

`UIMainApplication` 函数

非活跃状态

`animateWithDuration`

核心动画

`@implementation` 块

隐式动画

状态实验室应用

`ViewController.m`

生命周期

活跃状态

后台状态

非活跃应用

未运行状态

挂起的应用

多任务处理

移除资源

`applicationWillEnterForeground` 方法

状态实验室应用

`ViewController.m`

`viewDidLoad` 方法

保存状态

`applicationDidEnterBackground` 方法

`applicationWillEnterForeground` 方法

`ViewController.m`

`viewDidLoad` 方法

状态实验室创建

`UIApplication` 类

球体运动，陀螺仪和加速计

`CGRects`

`currentPoint`

`lastUpdateTime`

`update` 方法

`velocity`

横幅

块 (Blocks)

并发块

`__block` 存储修饰符

`dispatch_group_async()` 函数

`dispatch_group_notify()` 函数

`dispatch_get_main_queue()` 函数

`UIActivityIndicatorView`



`UIKit 线程安全`  
`ViewController.m`  
`桥接控制应用`  
`应用图标`  
`子设置视图`  
`默认值`  
`描述`  
`FirstViewController`  
`分组到常规信息`  
`图标设置到设置捆绑包`  
`主视图创建`  
`多值字段`  
`通知`  
`PreferenceSpecifiers`  
`偏好设置`  
`主设置视图`  
`项目创建`  
`属性列表编辑器`  
`PSGroupSpecifier`  
`获取用户设置`  
`第二个视图控制器`  
`安全文本字段设置`  
`设置应用`  
`设置捆绑包`  
`单个偏好项选择`  
`滑块设置`  
`文本字段设置`  
`开关设置`  
`UIApplicationWillEnterForegroundNotification`  
`Xcode 的属性列表编辑器`  
`子弹添加场景`  
`空方法`  
`playerBullets 节点`  
`发射导弹`  
`单个 SKNode`  
`风暴射击`  
`BulletNode 类`  
`工厂方法`  
`导入 BulletNode.h`  
`SKNode`  
`SKPhysicsBody 实例`  
`按钮`  
`控件状态`  
`可拉伸图像`  

![图片](img/sq.jpg) `C`  

`相机和相册`  
`代理方法`  
`图像选择器控制器`  
`接口`  
`MPMoviePlayerController`  
`pickMediaFromSource`  
`实际测试`  
`updateDisplay 方法`  
`用户界面`  
`viewDidLoad 方法`  
`单元格 ID 位置`  
`CheckPlease 应用`  
`勾选手势`  
`CheckMarkRecognizer`  
`CheckPlease 项目`  
`声明`  
`CLLocationManagerDelegate 方法`  
`Cocoa Touch`  
`代码大师`  
`警告视图调用`  
`cellForAtIndexPath 方法`  
`颜色分段控件`  
`完成处理方法`  
`文档目录`  
`MasterViewController`  
`NSUserDefaults`



`table view data source methods`  
`trigger`  
`UIBarButtonItem`  
`viewDidLoad method`  
`Collection view`  
`configuring`  
`keys`  
`viewDidLoad`  
`controller class`  
`custom cells`  
`ContentCell`  
`defaultFont method`  
`HeaderCell`  
`label property`  
`text property`  
`UILabel`  
`DialogViewer project`  
`header view`  
`cell class`  
`DialogViewer app`  
`UICollectionFlowLayout`  
`layout flow`  
`cramped`  
`flow and wrap`  
`paragraph flow`  
`UICollectionView`  
`providing`, `content cells`  
`dequencing method`  
`format`  
`wordsInSection`  
`Commanding Officer`  
`Control drag method`  
`Control fun application`  
`image view`  
`Alpha value`  
`Background property`  
`Drawing checkboxes`  
`interaction checkboxes`  
`Mode menu`  
`Object Library`  
`resize`  
`stretching`  
`Tags`  
`outlets`, `creating and connecting`  
`text fields`  
`attributes settings`  
`blue guidelines`  
`inspector settings`  
`Core data framework`  
`entities and managed objects`  
`application’s root controller`  
`attributes`  
`backing store`  
`data model design`  
`data model editor`  
`fetched property`  
`key value coding`  
`managed object context`  
`managed objects creation`  
`managed objects retrieval`  
`NSManagedObjectModel`  
`persistence view and controller creation`  
`relationships`  
`new project creation`  
`persistence and supporting file folders`  
`project templates`  
`Core location`  
`Cell ID location`  
`GPS`  
`location manager (*see* Location manager)`  
`WPS option`  
![images](img/sq.jpg) D  
`Data persistence`  



- `应用程序沙盒`
- `文档目录`
- `Library 选项`
- `临时目录`
- `归档模型对象`
- `和数据对象`
- `FourLines 类`
- `泛型类型`
- `NSCoding 协议`
- `属性实现`
- `ViewController 类`
- `核心数据 (*参见* 核心数据框架)`
- `文件保存策略`
- `多文件持久化`
- `单文件持久化`
- `SQLite3 数据库文件`
- `非易失性存储`
- `属性列表 (*参见* 属性列表)`
- `SQLite3 数据库`
  - `优势`
  - `绑定变量`
  - `创建`
  - `动态库`
  - `对象关系映射`
  - `结构化查询语言`
  - `ViewController.h 文件`
  - `视图控制器修改`
  - `ViewController.xib`
- `日期选择器`
- `代理和数据源选择器`
- `详细文本标签单元格样式`
- ![images](img/sq.jpg)  E, F
- `基于事件的动作、陀螺仪和加速度计`
  - `类扩展`
  - `CMDeviceMotion`
  - `CMMotionManager`
  - `容器边距`
  - `代理方法`
  - `dispatch_async( )函数`
  - `iOS 设备`
  - `Main.storyboard`
  - `MotionMonitor`
  - `rotationRate 属性`
  - `ViewController.m`
  - `viewDidLoad 方法`
- ![images](img/sq.jpg)  G, H
- `手势`
  - `CheckPlease 应用程序`
  - `CheckPlease 触摸方法`
  - `自定义手势`
  - `定义`
  - `事件`
  - `处理流程`
  - `iOS 用户界面`
  - `多点触控架构`
  - `捏合与旋转检测 (*参见* 捏合与旋转检测)`
  - `识别器`
  - `Swipes 应用程序 (*参见* Swipes 应用程序)`
  - `轻触 (*参见* 轻触)`
  - `触摸通知方法`
- `全局唯一标识符 (GUID)`
- `Grand Central Dispatch (GCD)`
  - `描述`
  - `未来展望`
- `陀螺仪和加速度计`
  - `alloc 和 init 方法`
  - `轴，图形表示`
  - `球体运动`



CMAcceleration 结构体

CMDeviceMotion

CMMotionManager

commonInit 方法

受控对象

约定（convention）

currentPoint 属性

drawRect: 方法

边缘

基于事件的运动（*参见* 基于事件的运动、陀螺仪与加速度计）

G-Force（重力加速度）

重力加速度值

iOS

iPhone

俯仰角

主动运动访问

滚动弹珠

正在滚动

旋转速率

摇晃事件（*参见* 摇晃事件、陀螺仪与加速度计）

![images](img/sq.jpg)  I, J（字母索引）

iCloud

添加支持

NSMetadataQuery

持续查询

配置文件（Provisioning Profile）

查询（Query）

reloadFiles 方法

保存文件

存储偏好设置

urlForFilename

免电脑备份

基于文档的应用程序

使用 UIDocument 进行文档存储

代码大师（*参见* 代码大师）

configureView 方法

contentsForType

control drag 方法

详细化故事板

对角线图案

向 FileCell 传递标识符

initWithFileURL 方法

loadFromContents

主视图控制器

MVC 原则

setNeedsDisplay

故事板设计

文本编辑器

TinyPix

TinyPixDocument

TinyPixView（*参见* TinyPixView）

视图

viewWillDisappear

IDE。*参见* 集成开发环境（IDE）

图片单元格样式

图片选取器控制器

cropRect

委托协议

didFinishPickingMediaWithInfo

iOS

iPod

NSValue 对象

presentViewController

SourceTypeCamera

UIImagePickerController

集成开发环境（IDE）

交互处理

应用委托

AppDelegate

applicationWillTerminate

Cocoa Touch 应用程序



`声明的链接`

`didFinishLaunchingWithOptions`

`被点击的选项`

`选项键，Xcode`

`引用的链接`

`UIApplication`

`消息嵌套`

`模型-视图-控制器`

`Cocoa Touch 应用程序`

`NSObject 子类`

`模式`

`可复用性`

`NSAttributedString`

`项目创建`

`Git 仓库`

`命名与选择`

`双按钮应用`

`视图控制器`

`操作方法`

`动作`

`对齐按钮`

`参数字段`

`属性检查器`

`attributeText 属性`

`按钮功能组`

`按钮与动作`

`按住 Control 键拖拽`

`拖拽至动作`

`在 Xcode 中编辑`

`编辑器切换按钮`

`空的类扩展`

`事件字段`

`浮动的弹出窗口`

`HIG`

`IBOutlet`

`界面构建器`

`标签与输出口`

`带标签的存储`

`低级虚拟机 (LLVM)`

`malloc()`

`myButton`

`NeXTSTEP`

`NSMutableAttributedString`

`对象库`

`输出口与动作`

`练习`

`发送者参数`

`statusLabel`

`故事板文件`

`样式设计`

`类型字段`

`用户界面设计`

`ViewController`

`Interface Builder`

`应用程序`

`约束`

`描述`

`图形用户界面`

`iPhone 5s 模拟器`

`Xcode`

`界面、相机与照片库`

`自动布局约束`

`类扩展`

`按住 Control 键拖拽`

`imageView 输出口`

`MPMoviePlayerController`

`故事板布局`

`UIImageView`

`UINavigationControllerDelegate`

`视图控制器`

`Xcode`

`iOS 核心软件`

`活动应用`

`苹果 iOS 开发者中心网站`



Cocoa Touch

开发者选项

内容详尽且编写精良的文档

与用户交互

受限的访问权限

有限的响应时间

有限的屏幕尺寸

有限的系统资源

Mac App Store

Macintosh 电脑

Objective-C 编程语言

单个“窗口”

Xcode

iPad 布局

属性检查器

约束

尺寸等级

UIView

Xcode

iPad，分屏视图

控制器

横屏模式

竖屏模式

iPhone 横屏布局

描述

填充视图

Interface Builder

方向

尺寸检查器

UIView

iPhone，分屏视图

应用的详细视图控制器

iPhone 5s 模拟器

iPhone 6 Plus

AppDelegate.m

主从（Master-Detail）应用模板

总统应用在横屏模式下的显示

RootViewController.m

traitCollectionDidChange: 方法

UISplitViewDelegate 协议

UITraitCollection 类

![images](img/sq.jpg) K

键盘，关闭

操作方法

操作与输出口

添加滑块与标签

backgroundTap 操作

编译并运行应用

约束

“完成”按钮，点击

身份检查器

使用 Interface Builder

视图属性

![images](img/sq.jpg) L

LLVM（低级虚拟机）编译器

本地化

开发基础语言

iOS 设备

iOS 本地化架构

iPad 与 iPod touch

ISO 代码

区域设置

项目/文件夹

字符串文件

字符串宏

LocalizeMe

应用显示名称

应用程序

Control-拖拽

开发语言

displayNameForKey

旗标标签

图像视图



导入

Info.plist 文件

在 Xcode 的项目中

iOS 8

iOS 设备

语言和区域

基于语言

语言运行

非本地化应用

NSLocale

NSLocalizedString( )

NSString

插座集合

项目导航器

故事板

瑞士法语本地化

触摸 iPhone 语言

视图控制器

viewDidLoad 方法

Xcode

位置管理器

精确度值

添加新约束弹出菜单

海拔高度

苹果公司

应用程序

更改位置服务权限

CLLocationManagerDelegate 方法

CLLocation 对象

列和 Control-拖拽

Core Location 和 Map Kit

描述

didChangeAuthorizationStatus 方法

didFailWithError 方法

didUpdateLocations 方法

距离过滤器

拖拽标签

错误通知

垃圾数据

horizontalAccuracy

iOS 使用

kCLErrorLocationUnknown

latitudeLabel

精确度级别

位置信息

定位服务

下方视图

Main.storyboard

主视图

地图应用

地图视图加载

NSLocationWhenInUseUsageDescription

option-拖拽

requestAlwaysAuthorization 方法

requestWhenInUseAuthorization

开始轮询

stopUpdatingLocation

使用

verticalAccuracy

viewDidLoad

WhereAmI 应用

![images](img/sq.jpg)  M

Map kit

架构

didUpdateLocations 方法

MKCoordinateRegion

MKMapView

属性声明

主从应用

定义

DetailViewController

功能

AppDelegate



`DetailViewController`  
`MasterViewController`  
`横屏模式`  
`MasterViewController`  
`竖屏模式`  
`PresidentList.plist`  
`结构`  
`MainStoryboard.storyboard`  
`UINavigationController`  
`UISplitViewController`  
`视图控制器`  
`模板应用`  
`媒体访问控制（MAC）`  
`模型-视图-控制器（MVC）`  
`Cocoa Touch 应用程序`  
`NSObject 子类`  
`模式`  
`可重用性`  
`多组件选择器`  
`多文件持久化`  
`多视图应用`  
`内容视图`  
`导航栏`  
`分割视图`  
`股票应用`  
`标签栏`  
`工具栏`  
`实用工具应用`  
`视图切换器`  
`警告框`  
`动画块`  
`动画曲线`  
`blueButtonPressed 方法`  
`BlueViewController`  
`蓝色视图翻转`  
`按钮与工具栏`  
`控制器`  
`didReceiveMemoryWarning 方法`  
`功能、工具栏`  
`翻转风格`  
`懒加载`  
`度量部分`  
`导航控制器`  
`根控制器`  
`根视图控制器`  
`故事板文件`  
`关闭`  
`switchView 方法`  
`标签栏控制器`  
`转场动画`  
`转场方法`  
`viewDidLoad 方法`  
`YellowController`  
`yellowViewController`  
`音乐应用`  
![images](img/sq.jpg) N  
`导航控制器`  
`附件图标`  
`应用骨架`  
`init 方法`  
`导航控制器设置`  
`NSUserDefaults 类`  
`sharedFavoritesList 工厂方法`  
`BIDFontInfoViewController`  
`披露指示器`  
`FontInfoViewController`  
`附件动作`  
`创建`  
`收藏字体`  
`IBOutlet 属性`  
`多个 Segue`  
`设置约束`



`slideFontSize`  
`viewDidLoad`  
`FontListViewController`  
`FontSizesViewController`  
`fundamentals`  
`computer stack`  
`navigation button`  
`navigation controller`  
`root view controller`  
`stack`  
`subcontrollers`  
`root view controller`  
`RootViewController.class`  
`UITableViewDataSource protocol`  
`value classes`  
`viewDidLoad method`  
`storyboard setup`  
`subcontrollers`  
`disclosure button`  
`FontListViewController`  
`font sizes view controller`  
`navigationItem property`  
`row selection`  
`segue connection creation`  
`UIFont class`  
`viewDidLoad`  
`table view niceties`  
`Drag-to-Reorder implementation`  
`Swipe-to-Delete`  
`tape breaking`  
`NSLocationAlwaysUsageDescription`  
`NSObject subclass`  
`NSPredicate class`  
`NSString method`  
`NSUserDefaults`  
`NSZone parameter`  
![images](img/sq.jpg)  对象关系映射（ORM）  
`Object-relational mapping (ORM)`  
![images](img/sq.jpg)  粒子系统  
`Particle systems`  
`first particle`  
`color ramp`  
`final missile explosion particle`  
`particles into the scene`  
`spark.png`  
`visual effects`  
`Pickers`  
`in Clock application`  
`date picker`  
`data sources`  
`dependent components`  
`bundle`  
`compile and run app`  
`content view`  
`controller class`  
`delegate and data source methods`  
`delegate methods`  
`states array`  
`viewDidLoad method`  
`game creation`  
`controller header file, writing the`  
`fonts`  
`sound files`  
`spin method`  
`view building`  
`viewDidLoad method`  
`with images`  
`multicomponent picker`  
`buttonPressed method`  
`delegate method`  
`outlets and actions`  
`root view controller`  
`single-component picker`  



数据源方法

委托方法

实现

输出口和操作

SingleComponentPickerViewController.m

视图构建

捏合和旋转检测

操作方法

连续手势识别器

doPinch\:方法

图像变换

实例变量

PinchMe 应用程序

双指旋转

UIGestureRecognizerDelegate 协议

UIImageView

UIPinchGestureRecognizer

ViewController.m

弹出视图

configureView\: 方法

DetailViewController.h

DetailViewController.m

detailViewController 属性

LanguageListController.h

LanguageListController.m

MasterViewController.m

setDetailItem

setLanguageString\:

tableView\:cellForRowAtIndexPath\: 方法

tableView\:didSelectRowAtIndexPath\:

UIPopoverController 类

UIPopoverControllerDelegate 协议

viewDidLoad 方法

PreferenceSpecifiers

PresidentList.plist

详情视图控制器

insertNewObject\:( )

主视图控制器

尺寸检查器

表视图数据源方法

viewDidLoad 方法

维基百科条目

主动运动访问

CMMotionManager

startDeviceMotionUpdates

updateDisplay 方法

ViewController.m

viewDidLoad 方法

属性列表

持久化应用程序

创建

设计

编辑

第一个版本

viewDidLoad 方法

序列化

![images](img/sq.jpg)  Q

Quartz

闭合绘制

画家模型

Quartz 2D 方法 (*参见* Quartz 2D 方法)

QuartzFun 应用程序 (*参见* QuartzFun 应用程序)

Quartz 2D 方法

CGContextStrokePath( )

CGFloat

颜色规范

颜色理论

便利方法，颜色


```markdown

坐标系

描述

绘制线条

绘制形状

填充颜色

图形库

图形上下文

图像上下文绘制

颜色模型

参数

基色

QuartzDemo

RGB 颜色模型

RYB 颜色模型

描边颜色

工具采样器

QuartzDemo 示例项目

QuartzFun 应用程序

2D 绘制代码

操作方法

应用运行效果

`CGColor` 属性

`CGPoint`

`CGRect`

`changeShape`

颜色控制

颜色绘制

常量定义

`currentColor`

`currentColor` 属性

图像绘制

线条绘制

`drawRect` 方法

图像分段

初始化代码

`initWithCoder`

`lastTouch`

优化

输出口和操作

`QuartzFunView`

`QuartzFunView` 骨架

随机颜色生成

矩形和椭圆

`redDrawRect`

引用更改

`setNeedsDisplayInRect`

配置

`touchesMoved`

`UIView`

![images](img/sq.jpg)  R

`ReloadFiles` 方法

响应者链

事件

滑动手势

表视图单元格

`UIResponder`

`UIView`

`UIWindow` 对象

视图控制器

工作原理

滚动弹珠、陀螺仪与加速度计

`iPhone`

`Main.storyboard`

Quartz 2D

更新方法

`ViewController.m`

`viewDidLoad` 方法

根视图控制器

`RootViewController.class`

`UITableViewDataSource` 协议

值类

`viewDidLoad` 方法

旋转与自适应布局

应用级方向

自动布局约束

```


默认约束

设备处理

设备方向

全宽标签

iOS 应用程序

方向

点、像素与视网膜显示屏

重构应用程序

Auto Layout 约束

图形用户界面

iPhone 模拟器

横屏，iPhone

方向

单一视图项目

源代码归档

用户界面

Xcode 6

单一视图应用程序项目

尺寸类别

与故事板

具体的水平和垂直

描述

iPad

映射，设备与方向

故事板编辑器

用户界面

视图控制器

![images](img/sq.jpg)  S

设置捆绑包

晃动、陀螺仪与加速度计

brokenScreen

消费电子设备

homebroken.png

Images.xcassets

有意施加的力

Interface Builder

Main.storyboard

motionBegan\:withEvent

MotionMonitor

NSURL 对象

UIEventSubtypeMotionShake

ViewController.m

简单属性列表

单组件选择器

单文件持久化

SlowWorker

描述

实现

接口

sleepForTimeInterval

ViewController.m

SlowWorker 的 doWork

dispatch_async 函数

dispatch_get_global_queue( ) 函数

doWork 方法

startTime 变量

微调器

分割视图

自定义

定义

iPad

主从应用程序。主从应用程序

弹出视图

Sprite Kit 框架

SQLite3 数据库

优势

绑定变量

创建

动态库

对象-关系映射

结构化查询语言

视图控制器修改



股票应用

字符串表

结构化查询语言 (SQL)

滑动手势应用

属性检查器

自动手势识别

检测

`fabsf()` 函数

多指滑动

多次滑动实现

单视图应用模板

滑动报告方法

`touchesBegan:withEvent` 方法

`touchesMoved:withEvent` 方法

`ViewController.m`

`viewDidLoad` 方法

开关

`doSomethingButton`

带标签的开关

开启和关闭

输出口和动作

圆角矩形按钮

`switchChanged` 方法

![images](img/sq.jpg) T

标签栏应用

编译并运行应用

空应用模板

文件创建

图标

`TabBarController.xib`

表视图

要显示的应用

自定义单元格

添加子视图

属性检查器

添加单元格

`cellForRowWithIndexPath`

`CellTableIdentifier`

`contentView`

控制器代码

`dequenceReusableCellWithIdentifier`

拖拽标签

分组和目标

标识符字段

界面构建器

标签，静态文本

多行行，子视图

新建表格视图单元格

nib 编辑器

`NSMutableString`

调整大小的视图

行属性

`UITableCell` 子类

`UITableViewCell`，nib

用户界面

`viewDidLoad`

分组和索引分段

添加索引

属性检查器

`cellForRowAtIndexPath`

控制器实现

数据导入

字典

`NSDictionary`

`titleForHeaderInSection`

视图构建

带索引

带多个分段

不带索引

iOS 表格

和单元格

分组表格




索引表格
普通样式
`UITableViewCell`
`UITableViewDelegate` 协议
`UITableViewSource` 协议
搜索栏
简单表格
`cellForRowAtIndexPath`
单元格样式
修改字体大小和高度
编码
控制器编写
设计与视图
矮人荣耀
全尺寸视图
添加图片
设置缩进级别
对齐
`NSIndexPath` 实例
`numberOfRowsInSection`
选择器视图
属性与图片
行更改与委托
行选择处理
`SimpleTableIdentifier`
字符串实例
行内样式
副标题与样式
`textLabel`
修改文本标签
`UITableViewCellStyleDefault`
`ViewController`

轻触操作
描述
`doDoubleTap` 方法
`doSingleTap` 方法
手势识别器
标签
多点检测
识别器
轻触手势识别器
TapTaps 应用
`UITapGestureRecognizer`
`ViewController.m`
`viewDidLoad` 方法

文本标签单元格样式

TextShooter
攻击敌人
`EnemyNode.m`
`init` 方法
`initPhysicsBody` 方法
`PlayerBullet` 类
自定义碰撞
精准玩家生命数
子弹轨迹
委托方法
`EnemyNode.m`
`friendlyBumpFrom` 方法
`PhysicsCategories.h`
`receiveAttacker` 接触方法
`SKNode` 类别
`SKPhysicsWorld` 类
默认屏幕
结束游戏
`GameOverScene`
生命值归零
`triggerGameOver`
创建敌人
敌人场景
寻找敌人
`GameScene.m`。首先
`spawnEnemies` 方法
完成关卡




`checkForNextLevel`

`next level transition`

`update bullet method`

`updateEnemies`

`game on`

`initial scene customization`、`withEvent`方法

`particles spicing things`（*请参阅*粒子系统）

`player movement`

`geometry calculations`

`handling touches`

`PlayerNode`

`player to the scene`

`SKAction`

`SKNode`

`wobbling sequence`

`zRotation`

`SKView`对象

`start scene creation`

`start shooting`

`BulletNode`类（*请参阅* BulletNode 类）

`bullets adding scene`（*请参阅* Bullet adding scene）

`physical engine`

`worthy sound`

`BulletNode.m`

`PlayerNode.m`

`StartScene.m`

`triggerGameOver`方法

`Threads`

`TinyPixView`

`C struct`

`prepareWithInvocationTarget`

`toggleSelectedBlock`方法

`touch events`

`UIViewdrawRect`方法

`UIView`子类

`undoManager`

`Toggle switch setting`

`Touch`

`TouchExplorerApplication`

`attributes inspector`

`blue guidelines`

`compile and run application`

`implementation`

`messages`

`option key`

`shift key`

`view controller icon`

`ViewController.m`

`Xcode`

`Touch notification methods`

![images](img/sq.jpg) U

`UIApplication`

`UIApplicationWillEnterForegroundNotification`

`UIManagedDocument`

`UITableView`

`UIViewdrawRect`方法

`User interface`

`action sheets and alerts`

`active and passive controls`

`buttons`（*请参阅* Buttons）

`control fun application`

`initializer`方法

`iOS` *对比*. `Mac`控件

`segmented control action`

`static controls`

`switches`（*请参阅* Switches）

![images](img/sq.jpg) V

`ViewController.m`

`SlowWorker`

`UIActivityIndicatorView`

`UILabel`

`viewDidLoad`方法

`View Switcher`

![images](img/sq.jpg) W

`WhereAmI`应用程序

`Wi-Fi`定位服务（WPS）

`Workspace window`，Xcode

`Interface Builder`

`jump bar`

`LLVM 编译器`

`navigator view`

`breakpoint navigator`

`debug navigator`

`issues navigator`

`project navigator`

`report navigator`

`search navigator`

`symbol navigator`

`toolbar`

`activity view`

`assistant view`

`scheme`

`standard view`

`utility pane`

![images](img/sq.jpg) X、Y、Z

`Xcode`

`changing attributes`

`groups`

`Hello World`项目

`inspector pane`

`interface builder`

`history of`

`.xib`扩展名

`iOS`应用程序

`keyboard shortcuts`

`label`

`library`

`code snippet library`

`file template library`

`media library`

`object library`

`project creation`

`Bundle Identifier`

`Device Family`

`Name and Company Identifier`

`project template selection sheet`

`Single View Application`

`welcome window`

`settings bundle`

`workspace window`

`compiler and debugger`

`interface builder`

`jump bar`

`navigator view`

`toolbar`

`utility pane`
