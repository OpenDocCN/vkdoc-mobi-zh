# G

`GameScene.sks` 文件

`GameViewController`

`viewDidLoad()` 方法

`Gesture recognizer`

`getArea()` 方法

`getCircumference()` 方法

`Globally unique identifiers (GUIDs)`

`goToNextLevel()` 方法

`Grand Central Dispatch (GCD)`

`app.beginBackgroundTask()`

`applicationDidEnterBackground()`

`applicationWillEnterForeground()` 方法

`background processing`

`concurrent closures`

`doWork()` 方法

`endBackgroundTask()` 实现

`lambdas`

`life cycle`

`active`

`background`

`inactive`

`not running` `suspended`

`low-level queuing`

`resource management`

`saving state`

`SlowWorker` 应用

`State Lab` 应用

`system resources`

`UIApplication` 实例

`ViewController.swift`

`viewDidLoad()` 方法

`Xcode` 控制台

`Graphics-intense apps`

`Grayscale model`

`Greenwich Mean Time (GMT)`

`Grouped and indexed sections` 控制器，实现

`importing data`

`search bar` 应用

`class names`

`execution`

`table view` 数据源方法

`UISearchController`

`view controller`

`viewDidLoad()` 方法

`view building`

`View Debug`。参见 `View Debugging`

`Grouped tables`

`Gyroscope`

与加速计对比

`attitude` 结果

`core motion` 管理器。参见 `Core motion`

