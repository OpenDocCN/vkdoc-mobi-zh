# F

`fileExistsAtPath()` 方法

`First-in, first-out (FIFO)`

`Float` 类型

`FontInfoViewController`

`accessory` 动作

`constraints`

`set up navigation bar`

`prepareForSegue()` 方法

`ShowFontInfo`

`slideFontSize`

`FontListViewController`

`cellPointSize` 属性

`navigationItem` 属性

`numberOfSectionsInTableView()` 方法

`prepareForSegue()` 方法

`segue` 连接

`UIFont` 类

`viewDidLoad()` 方法

`viewWillAppear()` 方法

`Fonts`

`disclosure indicators`

`FontInfoViewController`

`favorite fonts`

`viewDidLoad()` 方法

`FontListViewController`

`FontSizesViewController`

`estimatedRowHeight` 属性

`pointSizes` 属性

`Helvetica` 列表

`controller`

`root view controller`

`saveFavorites()` 方法

`set up`

`sharedFavoritesList`

`Sizes View Controller`

`creation`

`dataSource` 方法

`drag-to-reorder`

`favorite fonts`

`features`

`FontInfoViewController` 实现

`prepareForSegue()` 方法

`swipe-to-delete`

`Force fields`

`ForSegue` 方法

