# T

`Tab bars`

`clockicon`

`controller`

`deletekey`

`dependenticon`

`doubleicon`

`files creation`

`Identity Inspector`

`on-screen position`

`root controller`

`source code archive`

`storyboard`

`TabBarController.xib`

`UIViewController`

`view-controller pairs`

`Table views`

用于显示的 `applications`

`custom cells`

`adding new cells`

`controller code`

`IB`。参见 `Interface Builder (IB)` 加载

`UITableViewCell`, `Nib`

`New Table` 子视图分组。参见 `Grouped` 部分和索引部分；`Grouped tables`

`implementation`

`cell styles`。参见 `Cell styles`

`changing`, 字体大小和行高

`controller`

`dataSource` 和 `delegate` 插座

`handling row selection`

`image` 对象库

`plain tables`。参见 `Plain tables`

`table view cells`

`Template view controller`

`textFieldDoneEditing()` 方法

`Text fields`

`Inspector`

`Settings`

`Object Library`

`second text field`

`TextShooter App`

`2D API`, SceneKit

`bullet` 爆炸

`BulletNode` 类

`checkForNextLevel()` 方法

`collisions`

`custom collision behavior`

`default SpriteKit app`

`didBegin(_ contact: )` 方法

`enemy` 爆炸

`EnemyNode.swift`: 代码

`explosion` 空飞船文件

`MissileExplosion.sks`。最终

`missile explosion particle`

`firing bullets`

`force fields`

`Game over screen`

`GameScene`

`Geometry.swift` 计算

`goToNextLevel()` 方法

`handling touches`, 玩家移动

`horizontalAlignmentMode` 属性

`init()`

`init(coder:)` 和 `encode(with aCoder:)` 方法

`initPhysicsBody()` 方法

`init(size:levelNumber:)` 方法

`low-level graphics APIs`

`modifications to GameScene.swift`

`moveToward:` 方法

`normalized unit vector`

`parameters`

`physics bodies`

`physics categories`

`PlayerBullet class`

`PlayerNode`

`preceding snippet`

`presentScene()` 方法

`receiveAttacker(_:contact:)` 方法

`scene(size:levelNumber:)`

`scene transitions`

`sequence`

`SKLabelNode`

`SKNode` 检查器

`SKView` 和 `SKScene`

`sound effects`

`spawnEnemies()` 方法

`StartScene` 类

`triggerGameOver()`

`update()` 方法

`viewDidLoad` 方法

`Xcode Project Navigator`

`Threading`

`Attributes Inspector`

`critical section` 描述

`doWork()` 方法

`GUI applications`

`main thread`

`mutex (mutual exclusion)`

`setAnimated()` 方法

`Spinner Functions`

`thread-safe`

`UIActivityIndicatorView`

`units of work`

`ViewController.swift`

`TinyPix application`

`AND` 操作

`1-bit color`

`blockSize` 属性

`button’s handler` 块

`class` 定义

`color selection`

`control`

`contentsForType()`

`createFileNamed()` 方法

`definition`

`document class` 创建

`Documents` 目录

`"FileCell" set`

`file-saving` 方法

`init(fileURL:)`

`key-value coding technique`

`loadFromContents(_:ofType:)`

`Main.storyboard`

`MasterViewController.swift`

`NSData Objects`

`NSUserDefaults` 参数

`Project Navigator`

`reloadFiles()` 方法

`tintColor` 属性

`TintColorForIndex()` 方法

`TinyPixDocument.swift`

`TinyPix GUI`

`TinyPixUtils.swift`

`UIAlertController` 类

`UIDocument`

`user defaults`

`whitespace characters`

`Xcode project`

`touchesBegan(_:withEvent:)` 方法

`touchesCancelled(_:withEvent:)` 方法

`touchesEnded(_:withEvent:)` 方法

`touchesMoved(_:withEvent:)` 方法

`TouchExplorer Application`

描述

`forceTouchCapability` 属性

`maximumPossibleForce` 属性

`updateLabelsFromTouches()`

`user interface view` 属性

`view controller` 类

`Touch notification` 方法

`allTouches()` 方法

`gesture/tap` 开始

`touchesBegan(_:withEvent:)`

`UITouch` 对象

