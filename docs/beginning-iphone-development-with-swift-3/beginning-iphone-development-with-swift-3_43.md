# 索引 A

`Accelerometer` 轴，图形表示。参见 `Core motion` 定义。 `Directional controller`。参见 `Directional controller`。 `g-forces` 与陀螺仪结果对比。

`Accessory` 图标

`Action sheets`

`addObserver(_:selector:name:object:)`

`AppDelegate`

`append()` 方法

`App ID` 改为 `co.myCo`

Apple 开发者中心资源网站

Apple 的 iCloud 服务

`app` 配置

`calculateGridForSize()` 方法

`chooseColor()` 方法

`controller` 的导航

`data source` 方法

`document` 存储, `UIDocument`

`document.undoManager()`

iOS 设备

iPad 和 macOS 应用

`masterToDetail`

强大的工具

`prepareWithInvocationTarget()` 方法

`segmented control`

`TinyPixView.swift`

`toggleSelectedBlock()` 方法

`touchesBegan(_:withEvent:)`

`touchesMoved(_:withEvent:)`

`UIDocument` 类

`UIView` 子类

`application(_:didFinishLaunchingWithOptions:)` 方法

`applicationWillResignActive()` 方法

