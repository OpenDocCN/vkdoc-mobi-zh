# Q

`Quartz 2D`

`approach`

`CGColor` 属性

`color specification`

`context`

`techniques`

`coordinate system`

`definition`

`draw(_ rect:)` 方法

`graphics context`

`line-drawing` 部分

`line`, `shape` 和 `image drawing` 函数

`painter’s model`

`QuartzDemo` 示例项目

`rectangle` 和 `ellipse handle` 形状

`stroke color` 和 `fill color`

`.strokePath()` 函数

`QuartzFun Application`

`changeShape` 动作

`currentColor` 属性

`description`

`Document Outline`

`draw(_ rect:)` 方法

`firstTouchLocation`

`image drawing`

`image` 属性

`labelled and positioned color segment control`

`lastTouchLocation`

`optimization`

`random color`

`shape and color` 枚举

`Single View Application` 模板

`touch methods`

`updated touches` 例程

`useRandomColor` 属性

