# U

`Ubiquity`

`UIAlertController` 类

`UIApplicationWillResignActiveNotification`

`UICollectionView`

`content cells`

`custom cells`

`ContentCell`

`defaultFont()`

`HeaderCell.swift`

`label` 属性

`text` 属性

`DialogViewer` 项目，创建

`header view`

`layout flow`

`View Controller`, 配置

`UIDocument`

`UIKit` 框架

`UINavigationController`

`UInt` 类型

`UIPopoverPresentationController`

`UISplitViewController`

`UITabBarController`

`UITableViewCell`

`Unicode character`

`updateBullets()` 方法

`updateDisplay()` 方法

`Updated spin()` 方法

`updateEnemies()` 方法

`urlForFilename()` 方法

`UserDefaults (NSUserDefaults)`

`User interactions`

`application delegate`

`ButtonFun App`

`MVC` 范式

`simple two-button app`

`ViewController`。参见 `ViewController`, 用户交互

`User interface` 乐趣

`action sheet` 和 `alert`

`onButtonPressed()` 方法

`popoverPresentationController` 属性

`presentViewController(_:animated:completion:)` 方法

`UIAlertAction`

`UIAlertActionStyle.Default`

`UIAlertActionStyle.Destructive`

`UIAlertController`

`UIAlertStyle.Cancel` 实现

`adding text fields`。参见 `Text fields`

`constraints`

`creating and connecting outlets`

`resize image view`

`view attributes` 设置。参见 `View attributes`

`iOS vs. Mac controls`

`keyboard` 动作方法

`actions and outlets`

`adding button`

`adding slider and label`

`backgroundTap` 动作

`Done` 按钮开关

`onSwitchChanged()` 方法

`outlets and actions`

`segmented control` 动作

