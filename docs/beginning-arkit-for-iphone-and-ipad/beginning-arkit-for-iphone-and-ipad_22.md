# 索引

**A, B**

`瞄准点`  
`ARKit`  
`苹果的免费 Xcode 编译器`  
`增强现实应用`  
`iOS 设备摄像头`  
`Macintosh`  
`选项`  
`SceneKit`  
`目标`  
`虚拟飞机`  
`Xcode 项目模板`  
`COLLADA`定义  
`宜家`，`Metaio 的技术`  
`iOS 12`  
`iPhone 6s/6s Plus` 或 `iPad Pro`  
`Macintosh`  
`Objective-C`  
`模拟器程序`  
`Swift`  
`Swift 代码` ( *请参阅* `Swift 代码`)  
`Xcode`  
`ARKit 坐标系`  
`ARKit SceneKit 视图` (`ARSCNView`)  
`增强现实(AR)`  
`苹果`  
`Akonia Holographics`  
`ARKit` ( *请参阅* `ARKit`)  
`Flyby Media 的技术`  
`宜家 Place 应用`  
`SensoMotoric instruments`  
`VRvana`  
`ARSCNView`对象  
`柏林墙应用`  
`相机隐私设置`  
`设备能力设置`  
`迪士尼公司`  
`法拉利`，应用  
`HUD`  
`瞄准镜`  
`宜家`，`Metaio 的技术`  
`Info.plist 文件`  
`iOS 设备`  
`高分辨率摄像头`  
`高分辨率显示屏`  
`运动追踪`  
`强大处理器`  
`量杯`  
`百事可乐促销恶作剧`  
`宝可梦 GO`  
`TAC`  
`用户界面`，`ARSCNView`对象

**C**

`清除按钮`  
`clearMap 函数创建`  
`具有显著特征的平坦表面`  
`持久化图标`  
`缩略图`  
`COLLAborative 设计活动(COLLADA)`  
`COLLADA (.dae)` 到 `SceneKit (.scn)` 的转换`  
`碰撞`，虚拟对象  
`addTargets 函数`  
`代码测试`  
`检测`  
`枚举`  
`结构体 switch 语句`  
`tapResponse 函数`  
`ViewController.swift 文件`  
`ViewController.swift 文件`  
`彩色光`

**D**

`didUpdate` 渲染器函数`  
`创建显示平面`  
`水平面`  
`导航器窗格`  
`ViewController.swift 文件`  
`显示文本`  
`增强现实视图`  
`挤出深度`  
`材质类`  
`节点的几何体`，定义`  
`橙色文字`  
`将 3D 模型导入 COLLADA (.dae)`  
`动态虚拟对象`

**E, F**

`欧拉角`



## G

`Geometric shapes augmented reality view box display color`（几何形状增强现实视图框显示颜色），定义显示框增强现实`chamferRadius`尺寸的几何对象，创建圆角`ViewController.swift`文件，绘制添加多条`addLine`命令`addLine`命令`BezierPath`对象挤出深度`plane.addLine`命令楔形，创建 x 和 y 坐标黄色平面，创建半径，定义球体创建纹理图像，添加增强现实视图显示`earth.jpg`材质金字塔和木纹理，在互联网上搜索`ViewController.swift`文件 Xcode 项目，透明度改变虚拟对象手势 GLKit 框架`GLKMathDegreesToRadians`函数`guard`语句

## H

`handleLongPress`函数 `handleTap`函数 抬头显示（HUD）`hitTest`函数 `hitTest`方法 水平平面 `addObject`函数 `Info.plist`文件 橙色金字塔 Swift 代码 点击手势识别器 `ViewController.swift`文件 `worldTransform`矩阵 Xcode，创建

## I, J

`IBAction`方法 `IBOutlets` 图像检测显示信息 `AR Resources`文件夹 `planeNode` `SCNNode` 黄色平面显示虚拟对象 `nodeFor`渲染器函数 `rootnode`名称 `ship.scn` `ViewController.swift`文件 `Info.plist`文件 Swift 代码 `switch`语句 Target Membership 复选框 视频（*参见*视频，显示）`ViewController.swift`文件 Xcode，创建 图像识别 ARKit `AR Resources` `guard`语句 多张图像存储 `ViewController.swift`文件 项的宽度和高度 黄色平面 图像追踪 ARKit 识别器 `AR Resources` `guard`语句 `Info.plist`文件 移动文本 Swift 代码 三维对象 `ViewController.swift`文件 Xcode，创建 `initialWorldMap`属性 仪表盘

## K

运动学虚拟对象

## L

灯光 颜色设计，用户界面 方向光 `Info.plist`文件，相机访问 强度 定义值，滑块 `lightOn`函数 `Main.storyboard`用户界面 泛光 `showLight.position`命令 色温 定义值 `UISlider`类型 `UIButtons` 背景色 宽度和高度 `ViewController.swift`文件 `colorButton` `IBAction`方法 `IBOutlet` `intensityChange` `intensityChange` `IBAction`方法 `lightOn`函数 `temperatureChange` `IBAction`方法 `temperature` `UISlider` `viewDidLoad`函数 `viewWillAppear`函数 虚拟对象 世界原点（0, 0）`loadMap()`函数 长按手势 `handleLongPress` 创建 `hitTest`函数 `ViewController.swift`文件 属性

## M, N

测量应用 `Info.plist`文件 弹出窗口 点击手势识别器 `ViewController.swift`文件 `ARSCNView` `UILabel` `UILabel`，对齐 Xcode，创建 多张图像 `AR Resources`文件夹 `ImageDetection`项目 `nodeFor`渲染器函数 `ViewController.swift`文件

## O

对象检测 ARKit 识别器 `AR Resources`文件夹 `Info.plist`文件 Swift 代码 `ViewController.swift`文件 Xcode，创建 对象库窗口 遮挡 `detectPlane`函数 `Info.plist`文件 不可见水平平面 真实水平表面 Swift 代码 点击手势 `tapResponse`函数 测试 `ViewController.swift`文件 Xcode，创建

## P, Q

平移手势识别器 `ARSCNView` 文档大纲 `IBAction`方法 对象库 `运行`按钮 `停止`按钮 平移移动 `UIPanGestureRecognizer` 虚拟平面 Xcode，类型 平移手势 创建平移手势识别器 平移属性部分 `ViewController.swift`代码 持久化 `didAdd`渲染器函数 `Info.plist`文件 Swift 代码 `ViewController.swift`文件 `viewDidLoad`函数 `viewWillDisappear`函数 Xcode，创建 捏合和旋转手势 添加识别器 `IBAction`方法 对象库窗口 `ViewController.swift`文件 捏合手势识别器 连接弹出菜单 文档大纲 `IBAction`方法 `Main.storyboard`文件 对象库 `运行`按钮 `停止`按钮 `UIPinchGestureRecognizer` `ViewController.swift`文件 捏合触摸手势 `hitTest`方法 `IBAction`方法 `pinchGesture`函数 状态 虚拟平面，缩放 平面检测 显示（*参见*显示平面）水平（*参见*水平平面）`Info.plist`文件 iOS 设备 渲染器函数 用户界面 垂直平面（*参见*垂直平面，检测）`ViewController.swift`文件 Xcode，创建 定位虚拟对象 几何形状增强现实视图 圆柱体 Macintosh，USB 线缆 半径 相对定位 `rootnode` `showShape` 雪人 球体 `ViewController.swift`相对位置 `ARSCNView` 增强现实视图 约束 坐标 距离计算 `Info.plist` `rootnode` `SCNVector3` `showShape` Swift 代码 `ViewController.swift` `viewDidLoad`函数 `viewWillAppear`函数 Xcode 项目 试错法 勾股定理

## R

关系对象 旋转 绿色金字塔 函数 相对定位 `showShape`函数 旋转对象 欧拉角 创建 Xcode 项目 `GLKMathDegreesToRadians` 绿色金字塔 修改`Info.plist` 修改`ViewController.swift`文件 `SCNVector3`值 用户界面 用户界面到 Swift 代码 `ViewController.swift`文件 旋转触摸手势 `currentAngleZ`变量 `IBAction`方法 识别器 虚拟平面 尺子应用 定义点 `hitTestResults`常量 `tapResponse`函数 `worldTransform`属性 虚拟对象间距离 `append`方法 `displayText`函数 测量真实世界对象 `realPoints`数组 `ViewController.swift`文件 `hitTest`方法 `ViewController.swift`文件

## S

`saveMap()`函数 扫描对象 安装步骤 SceneKit（`.scn`）文件 资源文件夹 增强现实视图 创建 资源文件夹 场景文件 图标检查器 检查器面板 链接虚拟对象 缩进 场景图视图 对象库 屏幕边缘平移手势识别器 `showShape()`函数 单视图应用模板 ARKit 场景视图 增强现实应用 `.dae` COLLADA 文件 ARKit SceneKit 视图 漫反射弹出菜单 3D 图像 Macintosh 场景图视图 `scnassets` `.scn`文件 显示材质检查器 纹理文件 `IBOutlet` ARKit SceneKit 视图 助手编辑器图标 定义名称 对象库窗口 `ViewController.swift`文件 `viewDidLoad`函数 `Info.plist`文件 iOS 设备的相机 `Main.storyboard`文件 隐私-相机使用描述 `sceneView`变量 故事板屏幕 `ViewController.swift`文件 聚光灯 添加约束 ARKit 和 SceneKit 旋转轴 设计，用户界面 `eulerAngles`属性 `Info.plist`文件，相机访问 iOS 设备位置和角度，定义 `Main.storyboard`用户界面 位置和角度 `UISlider` `ViewController.swift`文件 `IBOutlet` `lightOn`函数 `pitchChange` `IBAction`方法 `rollChange` `IBAction`方法 `viewDidLoad`函数 `viewWillAppear`函数 `yawChange` `IBAction`方法 虚拟对象 世界原点（0, 0） 黄色平面 z 轴 SpriteKit 静态虚拟对象 Swift 代码 ARKit `ARSCNViewDelegate` 3D 图像 `IBOutlet` SceneKit `sceneView` `ship.scn`文件 `texture.png`文件 UIKit `ViewController.swift`文件 `viewDidLoad`方法 `viewWillAppear`方法 轻扫手势 `handleSwipe`函数 `hitTest`方法 `numberOfTouchesRequired`属性 `UISwipeGestureRecognizer`

## T, U

战术增强现实（TAC）点击手势识别器 `tapResponse`函数 触摸手势 `ARSCNView` 创建 Xcode 识别 `hitTest`函数 `ViewController.swift`文件 `Info.plist`文件 平移手势（*参见*平移手势识别器）捏合手势（*参见*捏合触摸手势）捏合动作 识别器检测 `handleTap`函数 `UITapGestureRecognizer` 旋转手势（*参见*旋转触摸手势）用户界面到 Swift 代码 `ViewController.swift`文件 Xcode，创建 双指手势



## V

垂直平面、检测`addObject`函数、图像绘制、`tapResponse`函数、`viewDidLoad`函数。  
视频、显示 AR 资源文件夹、QuickTime Player 程序、`SKVideoNode`、`ViewController.swift`文件、`viewDidLoad`函数、`viewWillAppear`函数。  
虚拟对象、增强现实视图碰撞（*参见* 碰撞，虚拟对象）、`displayPlane`函数、绘制瞄准点、相机信息、`DispatchQueue`、`enumerateChildNodes`、每秒帧数、`guard`语句、`Info.plist`文件、`renderer`函数、`rootnode`、Swift 代码、`UIButton`（背景颜色）、`UISwitch`状态、用户界面（`ARSCNView`）、`ViewController.swift`文件、`willRenderScene`函数、Xcode（创建动态力）、`applyForce`方法、ARKit SceneKit 视图、相机位置、相机的位置和方向、约束（`.dynamic`）、`Info.plist`文件、抛射体、点击手势、`tapResponse`函数、用户界面（Swift 代码）、`ViewController.swift`文件、Xcode 项目、`Info.plist`文件、运动学物理特性、SceneKit（`.scn`）文件、球体下落、静态、Swift 代码、`ViewController.swift`文件、Xcode。  
虚拟现实（VR）的缺点、头戴式显示器。  
视觉惯性里程计。

## W, X, Y, Z

世界地图、加载、保存。  
世界原点、显示、增强现实应用、活动方案弹出菜单、ARKit SceneKit 视图、`ARSCNView`坐标、`Info.plist`文件（相机访问权限）、`Main.storyboard`文件步骤、`ViewController.swift`文件、Xcode、黄色球体、重置坐标、`IBAction`方法（创建）、`IBAction resetButton`函数、`IBOutlet`线路、`UIButton`添加（用户界面）、`UIButton`（创建）、`ViewController.swift`文件、黄色球体、`Add`按钮（`addButton`）、`IBAction`方法、显示多个、`IBAction`方法、`IBOutlet`创建（滑块）、最小值和最大值（滑块）、修改滑块属性、重新设计用户界面、`resetButton`函数、`SCNNode`、`showShape()`函数、滑块值（定义）、`UISliders`、`ViewController.swift`文件。  
世界追踪。
