# D

## 依赖注入 (DI)
- `@EnvironmentObject`
- 缺点
- 方法
  - `MenuItemDetail`
  - `OrderController` 实例
  - 共享实例
    - `MenuItemDetail.ViewModelTests`
    - `OrderController` 共享实例
- `TODO` 注释

## 依赖倒置原则 (DIP)
- 数据获取实现
- 依赖倒置与注入
- `fetchMenu` 方法
- 倒置
- `MenuFetching.swift`
- 网络组件
- 实际后果

## 虚拟文件
- 缓慢/有状态的依赖
  - `OrderStoring`
  - `UserDefaults`
- 虚拟测试替身
- `PaymentProcessingDummy`

## 动态测试过程
- 抽象层
- 异步预期
- 因果关系
- 依赖倒置原则
- `ObservableObject`
- `@Published` 属性

