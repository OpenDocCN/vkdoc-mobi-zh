# `while` 循环

`while` 循环在每次迭代开始时检查条件。如果条件为 `true`，则执行循环体，然后再次检查条件。语法如下：

```
while condition {
    // 当条件为 true 时重复执行
    condition = done
}

do {
    // 执行一次
    otherCondition = done
    // 重复执行直到 otherCondition 为 false
} while otherCondition
```

