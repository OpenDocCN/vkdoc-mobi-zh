# PRAGMA 数据库，172–173 `sqlite3_enable_load_extension`，54

Pragma 语句，59 `sqlite3_exec` 函数，171

`automatic_index`，60 `sqlite3_load_extension`，54

`auto-vacuum`，60 使用 Swift

`busy timeout`，60 构建阶段，56

`foreign_key_check`，59 编译源文件，56

`Foreign_key_list`，60 头文件，57

`integrity_check`，60 `sizeconverter`，57

`shrink_memory`，60 `sqlite3_user_data()` 函数，58

原型，158 值，55

SQLite 库，1

`SQLITE_LIMIT_ATTACHED`，162 SQLite Manager，1, 147

REPLACE 语句，66 SQLite Manager, f refox，11

RTRIM 类型，51 数据库设置，13

目录选择器，13

索引，11, 18 定义，19

`searchBarCancelButtonClicked` 方法，152 `sqlite_autoindex_book`，18 `searchBarSearchButtonClicked` 菜单选项，12

方法，152 `Sqlite_master`，14

`searchDatabase` 函数，153–154 浏览与搜索，14

`searchResultCellTableViewCell`，154 模式，14

SELECT 语句，89 `sqlite_sequence`，14

交叉连接，94 ATTACH 命令，14

数据类型绑定函数，89 AUTOINCREMENT 属性，14

显示图片，94 DETACH 命令，14

IBOutlets，106 ROWINDEX，14

内连接，93 表与列，11, 15

记录列表，112 定义，16

`MPMoviePlayerController`，96 删除，17

导航控制器，102 INSERT/UPDATE，16

外连接，94 主键，16

播放音频记录，95 重新索引，17

`selectWineList` 函数，99 表名，17

`selectWineriesList` 函数，98 终端，11

`SelectWineries UIPicker`，96 工具菜单，12

`selectWineryByName` 函数，99 触发器，11, 19, 21

`SQLITE_ROW` 常量，90 视图，11, 19

TableViewControllers，102 CREATE VIEW，20

`UIPickerView` 函数，97 SELECT 语句，20

`UITableViewController`，100 Windows 命令，11

使用子查询，93 `SQLiteSearch-Bridging-Header`，150

`ViewController` 函数，96 `SQLITE_TRANSIENT`，151

WHERE 子句，90, 92 Storyboard，154

`WineListTableViewController`，104 Swift iOS 应用，2

`Wineries UIPickerView`，111 添加 SQLite 3 库，3

`WineryListTableViewController`，107–108 桥接，5

`setDbPath` 函数，166 头文件，5

`sqldif`，188–189 Swift 编译器，7

## SQLite 函数

`corrupt`，61 `DetailViewController`，3

JSON 函数，53, 55 `MasterViewController`，3

## Swift iOS 应用（续）

`DbMgrDAO` 类，8 包装函数，7

`executeQuery` 函数，10 REPLACE 选项，117

`init` 函数，9 ROLLBACK，116

子查询，115 `SQLITE_DONE`，129–130

Swift 字符串常量，114 `SQLITE_OK`，127–128

### 表的修改

`updateRecords` 方法，117

添加列，47 WHERE 子句，113, 115–116

ALTER 命令，46–47 葡萄酒庄，125

DROP 命令，47 葡萄酒列表，128

外键，46 `wineyDAO` 类，118

REINDEX 命令，46, 48 列更新，118–119

RENAME 命令，46 `segue.destinationViewController`，123–124

UPDATE 命令，47 `showWineDetail` segue，120

### 触发器，49

`WineListTableViewController`，121–122

`WineryListTableViewController`，122–123

---

UIButtons，25

Assistant Editor，26

Attributes inspector，26 `viewDidLoad` 函数，151, 165, 181

`detailSQLiteQueryField`，25, 27

### 视图的修改，48

`@IBActions`，26

Interface Builder，25

重复过程，26

`UIPickerView`，168 `Winery application`，69

`UITableViewCell`，106, 150 添加桥接，69

`umberOfSectionsInTableView`，151 约束，74

### UPDATE 语句

创建，69

ABORT 选项，116 数据模型，75

`CityTemperature` 表，114 `imageView`，75

FAIL 选项，117 UI 组件，73

`FirstViewController`，125–126 视图控制器，72

IBOutlets，114

### Winery 数据库

IGNORE 选项，117 CRUD 操作，41

JOIN，116 `createDbButton` 函数，42

`Lamartine` 酒，126–127, 129 `detailSQLiteQueryField`，42

ON CONFLICT，116 `openSQLiteDatabase`，42

OR 子句，114 表创建，42

PL/SQL/T-SQL，114 视图创建，44
