# 排版后的内容

## ANALYZE 语句
- `ANALYZE`语句，187–188

## 备份 API
- 数据模型，75
- `BackupBridge`文件，179
- 桥接接口，178–179
- `FileManager`，175–176
- 内存数据库，176
- 磁盘操作，177
- `sqlite`库，179–180
- Swift 编译器构建设置，179–180
- UI 构建，183–185
- 视图应用程序项目，177–178
- `ViewController`，181–183

## 集合与排序
- 二进制排序规则，51
- 不区分大小写排序规则，51
- `RTRIM`类型，51
- `sqlite3_create_collation`函数，51

## 控制器
- `DbMgrDAO`控制器，30
- `contentsOfDirectoryAtURL`方法，34
- `COpaquePointer`，31
- `DetailViewController`，32
- `executeQuery`函数，34
- `initViewIndex`函数，33
- `NSFileManager`类，34
- `NSFileManager.defaultManage`类，34
- `cellForRowAt`，37
- `openSQliteDatabase`函数，35
- `populateIndexView`函数，31
- `preparedStatement`，32
- `sqlite3_column_text`方法，32
- `stringByReplacingOccurencesOfString`，35
- `TableView`，32
- `viewDidLoad`函数，33

## 数据库操作
### Swift 类
- `Db`，27
- `CREATE TABLE`语句，28
- `Enums`类，30
- `executeQuery`方法，29
- `insert`语句，28
- `selectDbSchemaListByType`方法，29
- `selectDbSchemaStructure`方法，29
- `tableDef`，28

### 构建模式
- `buildSchema`，79
- `FirstViewController`，81
- `init`函数，78
- `insertWineRecord`函数，80
- `insertWineryRecord`函数，80
- `photo capture`函数，82
- `imagePickerController`函数，82
- `insertRecordAction`，82
- `UISlider`，83
- `viewDidLoad`函数，82
- `wineRating`函数，83

## 版权信息
© Kevin Languedoc 2016 [197]  
K. Languedoc, Build iOS Database Apps with Swift and SQLite, DOI 10.1007/978-1-4842-2232-4

## 删除操作
- `DELETE`函数
  - 排序规则，53
  - `DELETE`语句，131
  - 索引，53
  - `LIMITS`子句，132
  - `sampleDelete`函数，132–134
  - 表，52
  - `TRIGGER`，53, 132
  - 视图，52
  - `WHERE`子句，131

## Winery 应用
- `Cellar TableView`，138–139
- `commitEditingStyle`，139
- `deleteRowsAtIndexPaths`，139
- `deleteWineRecord`，139–140
- 编辑模式，141–142
- `UITableView`，139
- `UITableViewCellEditingStyle`，139
- `ViewControllers`，136
- `viewDidLoad`方法，137
- `Wineries TableView`，140–144
- `WineryDAO`类，135–136
- `WineryListTableController`，136–137
- `dequeueReusableCellWithIdentifier`，152

## 附加数据库功能
- `ATTACH DATABASE`语句，161–162
- 附加 SQLite 功能，163–170
- `CROSS JOIN`，163
- 数据库模式，77
- 酒庄数据库，76
- `documentDirectory`，76
- `SearchPathDirectory`，76
- 类型，76

## 连接操作
- `INNER JOIN`，163
- `INSERT`语句，64
  - `ABORT`子句，67
  - 二进制大对象，68
  - 数据绑定函数，63
  - `DocumentDirectory`，65
  - `IGNORE`子句，67
  - `last`子句，67
  - `NSFileManager`，65
  - `NSSearchPathDirectory`，65
  - `NSSearchPathForDirectoriesInDomains`，65
  - 记录，84–85
  - `REPLACE`语句，66
  - `rollback`，67

## iOS 应用创建
- 创建桥接接口，150
- 数据库创建，146–147
- 项目创建，148–149
- `SQLiteSearchBridge`，149–150
- `UISearchBar`，146
- `ViewController`，150–159

## 项目创建
- 应用委托，23
- 捆绑包，23
- 数据库，22
- `didFinishLaunchWithOption`，23
- 文档目录，23
- 查找器，23
- 主-详细模板，22
- 资源，22

## 详细视图控制器
- `DetailViewController`，39
- `createDbButton`，39
- `dbStatusMsg`，41
- `detailSQLiteQueryField`方法，39–40
- `@IBAction`按钮，40
- `openSQLiteDatabase`方法，39
- `prepareForSeque`，40
- `viewDidLoad`函数，41
- `viewWillAppear`函数，41

## 主视图控制器
- `MasterViewController`，36
- `cellForRowAtIndexPath`，37
- `numberOfRowsInSection`，37
- `schemaDetailsItems`，36
- `segue`，36
- `titleForHeaderInSection`，37
- `UITableView`组件，36
- `unWindFromSegue`函数，38
- `viewDidLoad`函数，36

## 文件管理
- `FileManager`，175–176
- `getDatabasePath`函数，182
- `NSObject + SQLiteSearchBridge`，150

## 界面元素
- `IBAction`，164, 184
- `IBOutlets`，157, 164
- `numberOfRowsInSection`，37, 151

