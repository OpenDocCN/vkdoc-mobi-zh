# 该模板提供了大量代码，后续将需要对其进行修改。

请对`WineryList`控制器、`WineCellTableViewCell`和`WineryCellTableViewCell` `UITableViewCellController`重复相同的过程。接下来，我们将连接视图控制器并添加`IBOutlets`。连接`TableViewControllers`和`TableViewCellController`要添加`IBOutlets`，首先需要将视图控制器与其对应的 UI 元素连接起来。要连接表视图控制器，请在故事板中选择`WineList`。在 Xcode 编辑器的右侧选择标识检查器，然后在自定义类字段中选择`WineListTableViewController`（图 6-6）。对`WineryListTableViewController`重复此过程。

