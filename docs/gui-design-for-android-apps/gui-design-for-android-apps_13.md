# 第 2 章

本章将通过引导你创建一个名为 `GuiExam` 的简单应用程序，来介绍 Android 的界面设计。你将学习活动（Activity）的状态转换、`Context`（上下文）类、意图（Intent）以及应用程序与活动（Activity）之间的关系。最后，本章展示了如何通过修改布局文件 `activity_main.xml` 来使用布局作为界面，以及按钮、事件和内部事件监听器是如何工作的。

