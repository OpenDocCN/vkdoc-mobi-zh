# 索引

- **A**:
    - `AlignItemsVerticallyWithPadding` 方法
    - Amit’s Game Programming Information
    - `AnchorPoint` 属性
    - `AppDelegate` 类
        - 动画间隔
        - `applicationDidFinishLaunching` 方法
        - `FPS` 显示
        - `KKAppDelegate` 类
        - `setDisplayStats` 方法
    - Apple 的界面构建器



Apple 的 `UIResponder` 类

应用程序编程接口（API）

![images](img/square1.jpg) **B**

BATAK 对决源码

位图字体工具

`BodySprite` 类

cocos2d 精灵

派生自 `PhysicsSprite`

`GB2ShapeCache` 类

头文件

实现文件

独立对象

`TexturePacker`

耗时的方法

`Box2D`

API 参考

盒子

`b2World` 初始化

`C++`

碰撞检测

转换点

调试绘制

快速移动物体碰撞

`GDC`

`HelloWorldLayer` 初始化

`HelloWorldLayer` 接口

`initPhysics`

联合

物理引擎

限制运动（屏幕）

`update` 方法

冗长的语言

`BulletCache` 类

可复用的子弹

`CGRectIntersectsRect` 检查

导入头文件

`InputLayer` 类

`@interface`

`screenRect` 方法

移除飞船方法

`shootBulletFrom` 方法

![images](img/square1.jpg) **C**

捕获状态

`CCActionEase` 类

`CCActionInstant`

类层次结构

子类

`CCActionInterval`

动作

类层次结构

子类

`CCCallBlock` 动作

`CCCallFunc` 动作

`CCGridAction` 类

`CCGrid3DAction`

类层次结构

子类

`CCInstantAction` 类层次结构

`CCKeyboardEventDelegate` 协议

`CCLabelBMFont` 类

`CCLabelTTF` 节点

`CCLayer` 类

`CCMenuItemSprite`

`CCMenuItemToggle`

`CCMotionStreak` 类

`CCRibbon`

类

线条描边效果

`CCMouseEventDelegate` 协议消息

`CCNode` 类

`CCPageTurn3D` 动作

`CCParallaxNode` 类



`CCMoveBy`动作  
`CGPoint`  
深度错觉  
视差滚动  
`parallaxRatio`  
`ScenesAndLayers08`项目  
`CCParticleSystem`类  
属性  
`blendFunc`  
`CCTextureCache`方法  
发射器持续时间  
`endRadius`  
`endSize`属性  
`GL_SRC_ALPHA`和`GL_ONE`  
重力模式  
`kCCParticleStartSizeEqualToEndSize`  
粒子方向  
粒子生命周期  
粒子位置  
`radialAccel`属性  
`redcross.png`  
`rotatePerSecond`属性  
`sourcePosition`  
`speed`属性  
`startColor`  
`startRadius`属性  
`totalParticles`属性  
`variance`  
子类化  
`CCParticleSystemQuad`  
`ParticleEffects01`  
`ParticleEffectSelfMade`类  
`CCProgressTimer`类  
`CCProgressTimer`节点  
`CCProgressTimerType`  
`scheduleUpdate`  
`update`方法实现  
`UserInterfaceLayer`类  
`CCRepeatForever`  
动作  
序列  
`CCScene`类  
`CCSequence`动作  
`CCSpriteBatchNode`  
`CCSprite`节点  
`CCStandardTouchDelegate`协议  
`CCTargetedTouchDelegate`协议  
`CCTexture2D`纹理  
`CCTiledGrid3DAction`类层次结构  
`CCTintTo`动作  
`ccTouchBegan`  
`ccTouchesEnded`方法  
`CCTransitionFade`  
`CCTransitionFadeTR`  
`CCTransitionJumpZoom`  
`CCTransitionMoveInL`  
`CCTransitionPageTurn`  
`CCTransitionRadialCCW`  
`CCTransitionRotoZoom`  
`CCTransitionSceneOriented`  
`CCTransitionShrinkGrow`  
`CCTransitionShrinkGrow`类  
`CCTransitionSlideInL`  
`CCTransitionSplitCols`  
`CCTransitionTurnOffTiles`  
`CGGeometry`方法  
`CGRectContainsPoint`  
`Chipmunk`  
`addNewSpriteAtPosition`方法  
`C`  
碰撞


`cpInitChipmunk`

`HelloWorld` 接口

`joints`

屏幕边界碰撞

`update` 方法

`Cocos2d`

抽象战争 2.0

加速度计控制

动画卡通儿童故事

API 文档

自动引用计数 (ARC)

内存泄漏

Objective-C 程序员

规则

BATAK 决斗源代码

`bloomies`

积木块

`central`

cocoa touch

`AppDelegate.m` 文件

`卡卡颂`

切换场景

差异

界面构建器视图

局限性

模型-视图-控制器模式

OpenGL ES

`startAnimation` 方法

`stopAnimation` 方法

`switchChanged` 方法

用户界面

基于视图的应用程序项目

开发者

`elements`

要素

`Farmville`

游戏中心

游戏引擎

Android cocos2d 移植版

C++ 版本

Objective-C 代码

移植版

“Hello World” 项目

首页

`iOS`

应用开发者

应用商店

Cocos2D 中心

2D

Objective-C

OpenGL ES 代码

开源

物理引擎

编程

版税

等距瓦片地图

iUridium 源代码

`Kobold2D`

划线游戏入门套件

OpenGL ES

视差滚动射击游戏

粒子设计器工具

物理引擎

弹球游戏

播客

前提条件

Objective-C

`ParticleDesigner`

编程经验

Quexlor 动作角色扮演游戏引擎代码与教程

sapus tongue 源代码

射击类游戏

源代码

太空游戏入门套件

精灵深入剖析




- `stack exchange network`
- `stickwars`
- `super turbo action pig`
- `TexturePacker`
- `Tilemaps`
- `tools`
- `bitmap font`
- `particle editing`
- `scene editing`
- `texture atlas`
- `tilemap editing`
- `trainyard`
- `Tutorials`
- `UIKit View`
- `Adding Views`
- `alert`
- `background, UITextFields`
- `bounding box`
- `CCGLView class`
- `hitTest method`
- `hitTestNodeChildren method`
- `interface builder`
- `sendSubviewToBack method`
- `skinning, UITextField`
- `textFieldFront`
- `touch events`
- `transparent`
- `UIKit views`
- `ZombieSmash`
- `CocosDenshion audio functionality`
- `Cocos2d games`
- `ARC enabling`
- `Automatic Reference Counting deselection`
- `Build Settings pane`
- `Include Unit Tests deselection`
- `libs folder`
- `Link Binary`
- `memory management`
- `Project’s Source Code`
- `Remove References`
- `small + button`
- `Target button`
- `Target template dialog`
- `CCLabelTTF`
- `ccTouchesBegan`
- `certificates and provisioning profiles`
- `changing the world, CCLabelTTF object`
- `Default.png file`
- `fps_images.png files`
- `getChildByTag`
- `Info.plist file`
- `iOS Developer registration`
- `iOS devices`
- `dual-core CPU`
- `hardware differences`
- `iOS simulator`
- `device features`
- `memory usage`
- `performance and logging`
- `performance of`
- `runtime behavior`
- `Kobold2D (*参见* Kobold2D)`
- `内存使用情况`
- `NSAssert 方法`
- `项目导航区域`
- `在 iOS 模拟器中运行的项目`
- `self.isTouchEnabled`
- `支持文件分组`
- `AppDelegate 类 (*参见* AppDelegate 类)`
- `HelloWorldLayer (*参见* HelloWorldLayer)`
- `main 函数`
- `前缀头文件`




系统要求

iOS 开发者注册

Mac OS X 版本

Xcode 项目模板

HelloWorld 项目

安装

iOS 模拟器

iOS 模板

Xcode 版本下载

碰撞检测

使用子弹

BOOL player

声明

isPlayerBullet 参数

isPlayerBullet 属性

包装方法

EnemyCache 类

碰撞多边形

Component 类

StandardMoveComponent

实现

接口

StandardShootComponent

实现

初始化

源代码

contentSize 属性

ConvertEventToGL 方法

![images](img/square1.jpg)  **D**

David Gervais 的瓦片集

DoodleDrop 游戏

加速度计输入

启用 ARC 的 cocos2d 项目

碰撞检测

ccpDistance 方法

图片尺寸

径向检测

范围检测/径向碰撞检测即可满足需求

创建

最终版本

首次测试运行

HelloWorld 层

AppDelegate.m

CCNode 派生类

Cocos2d 与 Kobold2D

GameLayer 类

iPad 移植

App Store

iPhone 和 iPad 版本

Retina iPad

通用应用

使用 Xcode 4

标签与位图字体

CCLabelBMFont

CCLabelTTF 类

Glyph Designer

分数标签

添加障碍物

CCCallFuncN 动作

CCMoveTo 动作

GameScene.h 文件

GameScene 初始化方法

initSpiders 方法

日志语句

节点位置

numSpidersMoved

重置蜘蛛位置

runSpiderMoveSequence 方法

spiderMoveDuration

spider.png

重置蜘蛛精灵位置

spidersUpdate 方法



tempSpider CCSprite

玩家精灵

加速度计输入

alien-hd.png

苹果的 @2x 文件扩展名

CCSprite* Player

contentSize 属性

文件添加

GameLayer 类

图片高度

图片尺寸

png 和 alien-hd.png

定位

资源文件添加

资源组

玩家速度

加速与减速

加速度计代码

加速度计控制

动作

CGPoint 变量

当前速度

设计参数

GameScene init 方法

imageWidthHalved

-(void) update:(ccTime)delta 方法

播放音频

afconvert 命令

CocosDenshion 音频功能

MP3 文件

SimpleAudioEngine

SoundConverter

项目设置

cocos2d 应用程序模板

保存文件对话框

![images](img/square1.jpg)  **E**

EnemyCache 类

CCSpriteBatchNode 初始化

getSpawnFrequencyForEnemyType 方法

导入头文件

初始化敌人

类接口声明

spawnEnemyOfType 方法

生成敌人

Enemy 类

创建类层次结构

组合/聚合

敌人属性

使用的图形

initSpawnFrequency 方法

initWithType 方法

类接口声明

spawn 方法

Essentials Xcode 项目

动作

CCAction 类层次结构

CCFollow 动作

CCRepeatForever 动作

CCSpeed 动作

缓动动作

网格动作

即时动作

时间间隔

序列

CCDirector 类

职责

sharedDirector 方法

单例

CCLabelTTF 类

CCLayer 类

加速度计事件

添加场景



`backgroundLayer`

`grouping concept`

`keyboard events`

`mouse events`

`touch events`

`userInterfaceLayer`

`CCNode`类

`abstract class`

`actions`

`child nodes`

`hierarchy`

`scheduled messages`（参见 Scheduled messages）

`tag`参数

`z`参数

`CCScene`类

`applicationDidFinishLaunching`方法

`CCLayer`对象

`CCSceneTransition`

`CCTransitionShrinkGrow`类

`pushScene`方法

`replaceScene`方法

`runWithScene`方法

`CCSprite`类

`anchor points`

`CCTexture2D`纹理

`CCTransitionScene`子类

`hierarchy`

`popScene`

`replaceScene`和`pushScene`

`transitions`

cocos2d API 参考

cocos2d 场景图

`background layer`

`CCScene`和`CCLayer`

`child node`位置

容器，`layer`

派生

展开视图

游戏对象层

`node`层级

场景层级

射击类游戏

虚拟摇杆层

Cocos2d 测试用例

菜单

`blocks`

`CCMenu`类

`CCMenuItem`类层级

`CCMenuItemFont`

`CCMenuItemImage`

`CCMenuItemLabel`类

`CCMenu`节点

`menuItem1Touched`方法

`MenuScene`类

方向教程

场景的压栈与出栈

场景与内存

单例

`MyManager`类

静态自动释放初始化器

紧耦合

`Every frame`方法

`FadeIn`/`FadeOut`动作

`Flippers`

`CCLayer`类

`ccTouchBegan`方法

创建

接口

`isTouchForMe`方法

旋转关节

`SetBullet`方法

静态刚体

**G**



游戏构建块

CCNode 类

CCMotionStreak （*参见* CCMotionStreak 类）

CCParallaxNode （*参见* CCParallaxNode）

CCParallaxNode 类

CCProgressTimer （*参见* CCProgressTimer 类）

CCSprite 子类化

CCRenderTexture

淡入/淡出动作

游戏对象，CCSprite

组合（*参见* 游戏对象组合）

多层

CCLayerColor 和 CCLayerGradient

ccTouchBegan

ccTouchEnded

ccTouchMoved 事件

CGRectContainsPoint

dealloc 方法

GameLayer 类

getChildByTag 方法

isTouchForMe

LayerTagGameLayer

关卡实现

MultiLayerScene 类

multiLayerSceneInstance 变量

registerWithTouchDispatcher

ScenesAndLayers04 项目

半单例对象

sharedLayer 方法

TargetedTouchDelegate

TargetedTouchHandlers

UserInterfaceLayer 类

多场景

CCDirector replaceScene 方法

CCScene 类

FirstScene dealloc 方法

init/dealloc 方法

LoadingScene 类

LoadingScene.h 文件

onEnter 方法

onExit 方法

ScenesAndLayers02 项目

ScenesAndLayers03 项目

sceneWithTargetScene 方法

scheduleUpdate

SecondLayer 的 init 方法

switch 语句

TargetScenes 枚举

Game Center

成就

决心

GameKitHelper showAchievements 方法

getAchievementByID 方法

loadAchievements 方法

报告

重置

AppController 和 NavigationController

可用性

block 对象

Game Kit API

GameKitHelper 委托

Game Kit 编程

iOS 4.1

iPhone

iTunes Connect

排行榜

Cocos2d 的根视图控制器

GKScore 对象



`landscape orientations`  
`NSRange 参数`  
`retrieved scores`  
`submitScore 方法`  
`user interface`  
`leaderboard and achievements`  
`local player`  
`local player’s friend list`  
`matchmaking`  
`existing match`  
`expectedPlayerCount 属性`  
`onMatchFound 方法`  
`peer-to-peer networks`  
`showMatchmakerWithRequest 方法`  
`network packets`  
`onReceivedData 方法`  
`sending and receiving data`  
`summary`  
`Xcode Project`  
`Bundle identifier key`  
`Game Kit requirement`  
`Kobold2D projects`  
`Game developer`  
`freelancers`  
`indie game tools`  
`marketing`  
`blogging software`  
`public relations and press releases`  
`screenshots`  
`twitter`  
`videogame`  
`OpenGameArt website`  
`publishers`  
`revenue stream`  
`Ads and analytics`  
`Buzzword`  
`iCloud`  
`In-App purchase`  
`push notification providers`  
`social networks`  
`socket server technology`  
`Game Developers Conference (GDC)`  
`GameEventLayer`  
`Game objects composition`  
`CCNode class`  
`CCStandardTouchDelegate protocol`  
`CCTargetedTouchDelegate protocol`  
`ccTouchBegan 方法`  
`CCTouchDispatcher class`  
`changed Spider class`  
`initWithParentNode 方法`  
`moveAway 方法`  
`NSObject class`  
`parentNode 参数`  
`Spider class implementation`  
`Spider class interface`  
`getChildByTag 方法`  
`Global identifiers (GIDs)`  
`Graphical user interfaces (GUIs)`  
![images](img/square1.jpg)  **H**  
`HealthbarComponent`  
`header file`  
`init 方法`  
`onHit 方法`  
`scaleX 属性`  
`spawn 方法`  
`Hello Kobold2D Implementation File`  
`HelloWorldLayer`  
`CCLabelTTF class`  
`CCLayer class`  
`CCNode class`



CCScene 节点

头文件

实现文件

init 方法

方法 `+(id) node`

触摸与访问权限

![images](img/square1.jpg)  **I**, J

独立游戏制作工具

initSpawnFrequency 方法

initWithParentNode 方法

initWithSpriteFrameName 方法

initWithTargetScene 方法

initWithType 方法

iPhone/iPad 模拟器

isKindOfClass 方法

等距瓦片地图

阿米特的游戏编程信息

边界区域

cocos2d

碰撞

设计图形

dg_iso32.png

底层瓦片

等距投影

视错觉

正交瓦片集

透视投影

三维物体

视觉深度

菱形形状

游戏

游戏编程

可移动玩家角色

创建新瓦片地图

创建新瓦片集

NPC 角色

可玩区域

玩家移动方向

调整大小

规则

滚动

Tiled (Qt) 地图编辑器

isTouchEnabled 属性

iUridium 源代码

![images](img/square1.jpg)  **K**

KKAppDelegate 类

Kobold2D

cocos2d 引擎

跨平台开发

currentDeviceIsSimulator

currentPlatformIsIOS

自定义 config.lua 设置

Default.png 文件

下载

空项目模板

游戏开发套件

目标

Hello-Kobold2D

Hello-Kobold2D 模板项目

HelloWorldLayer

HelloWorldSettings

injectPropertiesFromKeyPath

安装

iOS 与 Mac OS 平台

iSimulate

KKConfig 类方法

库

Mac DoodleDrop, KKInput

main 与 AppDelegate

预处理器宏

项目导航区域

项目启动工具



启动配置文件

启动过程

升级

Xcode

Xcode Organizer 窗口

![images](img/square1.jpg) **L**

画线游戏入门套件

![images](img/square1.jpg) **M**

米-千克-秒 (MKS) 单位制

模型-视图-控制器 (MVC) 模式

![images](img/square1.jpg) **N**

节点层级

NodeHierarchy 项目

非玩家角色 (NPCs)

![images](img/square1.jpg) **O**

onCallFunc 方法

正交瓦片地图

cocos2d

CCTMXTiledMap

ccTouchesBegan 方法

绘制对象层矩形

GameEventLayer

GIDs

iPhone 模拟器

定位被触摸的瓦片

NSDictionary

对象层

removeTileAt 与 setTileGID 方法

滚动

tileGIDAt 方法

TileMapLayer 类

勇者斗恶龙

深度感

瓦片化 (Qt) 地图编辑器

创世纪 7

![images](img/square1.jpg) **P**, Q

视差滚动

背景源代码

CCLayerColor

.getNodeByTag 方法

init 方法

iPad 版本

背景条纹

anchorPoint 属性

CCSpriteBatchNode

增量时间

无限滚动

闪烁

全屏图像

init 方法

移动

NSNumber

自有层

快速粗糙处理

重复纹理

滚动速度

Seashore 图像

源图像

速度因子

纹理图集

update 方法

虚拟手柄

粒子编辑工具

粒子系统

addChild 代码

CCParticleExamples.m 文件

CCParticleExplosion

CCParticleSystem 类（*参见* CCParticleSystem 类）

设计器

背景颜色设置

发射器配置视图

endRadiusVar 属性

iPhone 模拟器



ParticleEffects01 项目

particleWithFile 方法

plist 文件

positionType 属性

ramdomize 方法

runEffect 方法

fire.png 文件

NSStringFromClass 方法

ParticleEffects01 类

particleType 变量

runEffect 方法

分享功能

ShootEmUp04 项目

fx-explosion.plist 和 fx-explosion2.plist 文件

gotHit 方法

init 方法

SimpleAudioEngine 类

switch 语句

物理引擎

动态物体与静态物体

局限性

刚体

PhysicsSprite 类

弹球游戏

挡板和球体图像

碰撞多边形

凹形与凸形

逆时针方向

PhysicsEditor 工具

应用程序文件夹

Box2D 物理世界

磁盘映像

下载

导出器设置

GB2ShapeCache 文件

.pes 文件

PTM-Ratio 设置

弹球台

弹射器形状

锚点

类别与碰撞位掩码

碰撞参数

手动操作

矩形与三角形形状

编程实现

角阻尼值

球体加速度

球体类接口

球体图像

物体与手指位置

BodySprite（另请参阅 BodySprite 类）

bodyToFingerDirection 向量

Box2D 调试绘制

挡板

击球杆（另请参阅 击球杆）

引力模拟

初始化与清理方法

maxSpeed 值

PhysicsSprite 类

弹球台（另请参阅 弹球台）

弹射器（另请参阅 弹射器）

竖屏方向

setBallStartPosition 方法

SetTransform 方法

触摸委托方法

update 方法

台面形状

锚点

形状追踪器

台面底部图像



-   表格碰撞
-   表格左对齐图像
-   表格顶部图像
-   容差设置
-   弹球桌
-   碰撞参数
-   `initBox2dWorld` 方法
-   `initPhysics` 方法
-   `PinballTableLayer` 类
-   PTM 比例
-   `TablePart` 类
    -   实现
    -   接口
-   `TableSetup` 类
    -   头文件
    -   实现
-   弹射器
    -   类接口
    -   接触事件
    -   `CreateJoint` 方法
        -   头文件
    -   初始化
    -   `maxMotorForce` 值
    -   棱柱关节创建
    -   伸缩杆
-   通用接触监听器
    -   Box2D 接触方法
    -   碰撞处理
    -   `Contact` 类
    -   `ContactListener` 类
    -   `contactType`
    -   `notifyAB` 方法
    -   `notifyObjects` 方法
    -   `worldAxis`
-   `popScene` 方法
-   `pushScene` 方法
-   ![images](img/square1.jpg)  **R**
    -   Ray Wenderlich 的太空游戏教程
    -   恢复系数
    -   Retina 与 iPad 分辨率
        -   设计瓦片地图
        -   iTileMaps
        -   SD/HD 版本
        -   TexturePacker
        -   TMX 文件
-   ![images](img/square1.jpg)  **S**
    -   Sapus Tongue 源代码
    -   场景编辑工具
    -   场景层级结构
    -   `sceneWithTargetScene` 方法
    -   预定消息
        -   优势
        -   `_cmd` 隐藏变量
        -   调试器控制台
        -   `delta` 参数
        -   Objective-C
        -   Lingo
        -   方法
        -   参数名称和数量
        -   `scheduleupdate` 方法
        -   `tenMinutesElapsed` 方法
        -   未声明的选择器
        -   更新方法
        -   `-(void) update:(ccTime)delta` 方法
        -   `scheduleUpdate` 方法
    -   `sharedManager` 静态方法
    -   `shootBulletFrom` 方法
    -   `ShowAlertView` 方法
    -   `SimpleAudioEngine`
    -   `SneakyInput`
        -   动作控制
        -   死区
        -   旋转




- `ship’s setPosition method`（飞船的`setPosition`方法）
- `skinned joystick`（蒙皮摇杆）
- `thumb controller`（拇指控制器）
- `update method`（`update`方法）
- `velocity property`（`velocity`属性）
- `button skinning`（按钮蒙皮）
- `ccTime variables`（`ccTime`变量）
- `computer graphics`（计算机图形学）
- `disabled image`（禁用状态的图像）
- `fireButton`（`fireButton`）
- `GameLayer file`（`GameLayer`文件）
- `update method`（`update`方法）
- `button to shoot`（射击按钮）
- `bullets`（子弹）
- `header files`（头文件）
- `radius property`（`radius`属性）
- `scene method`（`scene`方法）
- `update method`（`update`方法）
- `digital control`（数字控制）
- `download`（下载）
- `integration`（集成）
- `open source software`（开源软件）
- `SoundConverter`（`SoundConverter`）
- `Sprites`（精灵）
- `animations`（动画）
- `CCAnimationCache`（`CCAnimationCache`）
- `CCRepeatForever action`（`CCRepeatForever`动作）
- `code`（代码）
- `frames`（帧）
- `stringWithFormat method`（`stringWithFormat`方法）
- `CCAnimationHelper class`（`CCAnimationHelper`类）
- `animationWithFile helper method`（`animationWithFile`辅助方法）
- `@implementation`（`@implementation`）
- `@interface for`（`@interface`用于）
- `ship-anim`（`ship-anim`）
- `CCSpriteBatchNode`（`CCSpriteBatchNode`）
- `batch rendering`（批量渲染）
- `CCSprites`（`CCSprites`）
- `functions`（函数）
- `Sprites01 Demo Project (*see* Sprites01 Demo Project)`（Sprites01 演示项目（*参见* Sprites01 演示项目））
- `draw call`（绘制调用）
- `retina display`（视网膜显示屏）
- `AppDelegate.m file`（`AppDelegate.m`文件）
- `CCDirector method`（`CCDirector`方法）
- `CCFileUtils singleton class`（`CCFileUtils`单例类）
- `Cocos2d`（`Cocos2d`）
- `-hd suffix`（`-hd`后缀）
- `high-definition (HD) graphics`（高清（HD）图形）
- `-ipad and -ipadhd`（`-ipad`和`-ipadhd`）
- `scale property`（`scale`属性）
- `ship.png`（`ship.png`）
- `technical specifications, iOS devices`（iOS 设备的技术规格）
- `Sprite batching`（精灵批处理）
- `texture atlas`（纹理图集）
- `AppDelegate.m file`（`AppDelegate.m`文件）
- `assets folder`（资源文件夹）
- `Border Padding and Shape Padding settings`（边框填充和形状填充设置）
- `CCAnimationHelper class`（`CCAnimationHelper`类）
- `cocos2d`（`cocos2d`）
- `definition`（定义）
- `dithering option`（抖动选项）
- `GameArt folder`（`GameArt`文件夹）
- `-hd`（`-hd`）
- `init method`（`init`方法）
- `LoadingScene`（`LoadingScene`）
- `memory`（内存）
- `orientation`（方向）
- `output settings`（输出设置）
- `(.pvr.ccz)`（`.pvr.ccz`）
- `PVRTC2 and PVRTC4 image`（PVRTC2 和 PVRTC4 图像）
- `removeUnused methods`（`removeUnused`方法）
- `resources folder`（资源文件夹）
- `RGBA8888`（`RGBA8888`）
- `RGBA4444 format`（RGBA4444 格式）
- `RGB565 format`（RGB565 格式）
- `RGB5551 format`（RGB5551 格式）
- `TexturePacker`（`TexturePacker`）
- `texture space`（纹理空间）
- `.tps`（`.tps`）
- `Sprites01 Demo Project`（Sprites01 演示项目）
- `bullet`（子弹）
- `CCMoveTo or CCMoveBy actions`（`CCMoveTo`或`CCMoveBy`动作）
- `CGPoint`（`CGPoint`）
- `ship shooting`（飞船射击）
- `update method`（`update`方法）
- `CCSpriteBatchNode`（`CCSpriteBatchNode`）
- `GameScene`（`GameScene`）
- `sharedGameLayer and bulletSpriteBatch accessors`（`sharedGameLayer`和`bulletSpriteBatch`访问器）
- `-(id) init method`（`-(id) init`方法）
- `initWithFile`（`initWithFile`）
- `optimizations`（优化）
- `Bullet class’s shoot method`（`Bullet`类的`shoot`方法）
- `GameScene’s init method`（`GameScene`的`init`方法）
- `nextInactiveBulle`（`nextInactiveBulle`）
- `StandardMoveComponent`（`StandardMoveComponent`）
- `implementation`（实现）
- `interface`（接口）
- `StandardShootComponent`（`StandardShootComponent`）
- `implementation`（实现）
- `initialization`（初始化）
- `source code`（源代码）

`T`
- `Texture atlas tools`（纹理图集工具）
- `TexturePacker`（`TexturePacker`）
- `Tiled (Qt) Map Editor`（Tiled（Qt）地图编辑器）
- `cocos2d game engine`（cocos2d 游戏引擎）
- `dg_grounds32.png tileset`（`dg_grounds32.png`图块集）
- `new tilemap`（新瓦片地图）
- `pixel-padding value`（像素填充值）
- `texture atlas`（纹理图集）
- `tileset image`（图块集图像）
- `TMX files`（TMX 文件）
- `TileMapLayer Class Interface`（TileMapLayer 类接口）
- `Tilemaps`（瓦片地图）
- `arbitrary positions`（任意位置）
- `2D game worlds`（2D 游戏世界）
- `editing tools`（编辑工具）
- `isometric (*see* Isometric tilemaps)`（等距（*参见* 等距瓦片地图））
- `optical illusion`（视觉错觉）
- `orthogonal (*see* Orthogonal tilemaps)`（正交（*参见* 正交瓦片地图））
- `Retina and iPad resolutions`（Retina 和 iPad 分辨率）
- `TexturePacker`（`TexturePacker`）
- `tiled (Qt) Map Editor`（Tiled（Qt）地图编辑器）
- `trigger areas`（触发区域）
- `Ultima 7`（《创世纪 7》）
- `Touch location`（触摸位置）

`U`
- `UIAlertView`（`UIAlertView`）
- `Ultima 7`（《创世纪 7》）

`V`, `W`, `X`, `Y`, `Z`
- `vertexZ values`（`vertexZ`值）
