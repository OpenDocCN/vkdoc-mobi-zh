# 正确共享

与他人共享数据库时，*切勿将其放在可同时被多人访问的目录中*。这包括文件服务器上的文件夹或任何类型的云存储（例如 Dropbox、Google Drive 等）。两个用户直接从文件夹打开同一数据库文件必定会导致文件损坏。应改用 FileMaker 的*点对点共享*，或将文件托管在 *FileMaker Server* 上，以确保正确的多用户读写管理，并更好地防止用户崩溃（参见第 29 章）。

