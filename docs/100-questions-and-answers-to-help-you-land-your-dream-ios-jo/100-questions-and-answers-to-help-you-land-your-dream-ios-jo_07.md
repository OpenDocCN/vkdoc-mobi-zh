# 问题 54：你会如何将一个数组序列化到磁盘？

需要使用的关键类是一个名为 `NSCoder` 的抽象类，以及一个名为 `NSCoding` 的协议。这两者一起设计用来标准化将类与序列化数据格式相互转换的过程。如果你曾经使用过 NIB 文件或 storyboards，你可能已经以某种形式接触过 `NSCoding`，因为 `NSCoding` 是 Cocoa 用来从 NIB 加载视图和视图控制器的底层机制。

