# 索引

### A

`AddBookViewController` 类
警告控制器
添加页面
`BookStoreDelegate`
`configureView`
编辑
显示 Segue
`MasterViewController` 类
命名
Segue 标识符
`prepareForSegue` 方法
读取开关
`UIAlertController`
算法
AND 运算符
Apple 人机界面指南
Apple Watch 应用
助理编辑器
`bookLabel` 输出口，连接
`Book.swift`/`BookStore.swift` 文件
Control-拖拽，创建输出口
创建
`interfaceController.swift` 文件
界面故事板
添加标签，表格行
标签增长
设置行属性
输出口命名
选项
添加表格
将表格行类设置为 `BookRow`
选择表格行控制器
更改表格行标识符
目标
WatchKit 应用启动
WatchKit 目标
Array 类
ASCII 字符
助理编辑器
属性
A12 仿生处理器
作者实体

### B

Bits
空白 iOS 模板
`Book` 实体
`Bookstore` 类
创建 BookStore 应用
访问属性
样板项目，源代码清单
`Book` 类
空白 `Book` 类
空白 Swift 文件
Master-Detail Application 模板
选择产品名称/选项
`Book` 类的属性
创建 Swift 文件
Bookstore 程序
Bookstore 的详情视图
创建数据模型类
添加描述
`DetailViewController`
修改 `MasterViewController.swift` 文件
`BookStore WatchKit App`
布尔表达式
布尔逻辑
二元运算符
比较运算符
复杂运算符
真值表
AND 运算符
NAND 运算符
NOR 运算符
NOT 运算符
OR 运算符
XOR 运算符
一元运算符
断点导航器
Bugs
字节

### C

`calculateTotalSpent`
Cocoa Touch 类
代码重构
集合类
命令行工具 `macOS` 项目
比较函数
比较数据
日期
`if` 语句
关系运算符
Xcode 应用
比较运算符
计算机程序
条件控制循环
控制台
常量和变量
计数控制循环



### D

`Database` `Data model` `Data types` `Date attribute` `Debug area` `Debugging breakpoint navigator configuration controls`、`Xcode` 断点位置设置 步进控制 `configureView` 方法 变量 `Xcode` 工具栏 `Debug navigator` `Delegation` `descriptionTextView` 属性 `Design requirements` `DetailViewController` `DetailViewController` 类 `DetailViewController.swift` 文件 `Developers` *vs.* `management` `Dictionary` 类 `didReceiveMemoryWarning`

### E

`Editor area` `Electronic Numerical Integrator and Computer (ENIAC)` `else if` 语句 `Errors` `Issue Navigator` `Xcode`

### F

`Fast enumeration/For-In Loops` `Fetch requests` `Find navigator` `Flowcharting` `Forced unwrapping` `func` `methodName()`

### G

`generateAction` 方法 `Generate Random Number` 按钮

### H

`Hexadecimal` `Human Interface Guidelines (HIG)`

### I, J, K

`IBOutlet` `if` 语句 `Implicitly unwrapped optionals` `Infinite loop` `Inheritance` `Instance methods` `Integrated development environment (IDE)` `Interface Builder` 操作和输出口设计 `iPhone app` ( *参见* `iPhone app` with `Interface Builder`) `MVC` 模式 `nib` 文件 属性 `.storyboard` 扩展名 `.swift` 文件 `XML` 文件格式 `InterfaceController.swift` 文件 `Interface`, 设置 添加操作 `addNew` 方法 `bar button item` 创建 最终应用 `loadBooks` 方法 `navigation controller` 输出口创建 协议 `style/identifier` `cell` `table` `view` `ViewController` 类 `ViewController.swift` 文件 `iPhone app` `addBookSegue` `AddBookViewController` 类 `AddBookViewController`/对象 `AddBookViewController.swift` `File` `add missing constraints` `bar button item` `book` 类更改 `book` 功能 `insertNewObject` `Function` 购物清单 `MasterViewController` 方法 `prepareForSegue` `Method` 属性 `Swift` 的标志性特性 `UISwitch` `iPhone app` with `Interface Builder` 操作 `Cocoa Touch` 对象 创建输出口 `Generate Random Number` 按钮 `IBOutlet` `Inspector` `pane/selector` 栏 `Label` 对象 `Main.storyboard` 文件 `MVC` 范式 创建新组 `Object Library` 随机数生成器 重命名按钮标题 `seedAction`/`generateAction` 方法 `Single View App` 模板 `source files` `Stack View` 居中 嵌入 `.swift` 文件 `Swift` 项目 `UILabel` 值 `ViewController.swift` 文件 `View Controller` 场景 工作区窗口 `Issue navigator`

### L

`let` *vs.* `var` `loadBooks()` 数组 `Logical operators` `AND` 运算符 `NOT` 运算符 `OR` 运算符 `Looping` `Lower camel case`

### M

`Main.storyboard` 文件 `Markdown` `method` `Master-Detail` 应用 `Model-View-Controller (MVC)` 模式 架构 业务 设计 `HIG` 人机界面指南 `iPhone` 银行应用 对象 `OOP` 范式 `Moore's Law`（摩尔定律）

### N

`Navigator area` `Nest` `if` 语句 `newBook` 变量 `NeXT` 产品 `NOT` 语句 `NSObject` `Numbering systems` `bits` `bytes` `hexadecimal` `unicode`

### O

`Object-oriented programming (OOP)` `class` `Bookstore` 项目 `Cocoa Touch` `Class` `company identifier` `Customer.swift` 文件 `empty parentheses` `Master-Detail App` 方法 `NSObject` 属性 `Xcode` 易于调试 易于替换 继承 接口对象 `polymorphism` 原则 冗余代码，消除 `Value orientated programming` `OmniGraffle` `Optional binding` `Optionals`, `Swift` `Ordered collection`

### P

`Playgrounds` 界面 `Polymorphism` `Preferences`/`UserDefaults` 读取 存储信息 写入 `print` 函数 `Project editor` `Project navigator` `Protocols and delegates` `createRandomNumber` `Function` 猜数字游戏应用 `Guess Input View Controller` 类 `GuessInputViewController.swift` 文件，创建 `Guess View Controller` 和对象，创建 `IBAction` `Function` 多重继承 `prepareForSegue` `Method` `seque identifier` `Single View App` 模板 `User Guess Delegate` `Function` `View Controller` `viewDidLoad()` 函数 `Pseudocode`

### Q

`Quality assurance (QA)`

### R

`RadioStation` 类 代码 操作创建 `Assistant Editor` 图标 `buttonClick` 方法 连接创建 `label` 连接，`stationName` 属性 文件 对象添加 程序运行 `play` 按钮 `radio station` 信息 项目创建 命名，`iOS` 应用模板选择 `Xcode` 工作区窗口 `type` 方法 用户界面 `button` 对象添加 `label` 对象添加 拉伸 `label` 对象 `ViewController.swift` 文件 `randomNumberLabel` 输出口 `randomNumberLabel.text` `RandomNumber` 项目 `Relational operators` 比较数字 比较运算符 `Xcode` 应用 `Rental report` 应用 完成屏幕 模拟屏幕 `Report navigator`

### S

`Sandbox` `saveBookAction` `seedAction` 方法 `Seed Random Number Generator` `Show Segue` 对象 `Software development cycle` 完整调试 `developers` *vs.* `management` `QA` 测试 `Software development life cycle` `Source Control` 导航器 `Source editor` `SQLite` 数据库 `Standard editor` `State of an object` `Storing information` `Core Data` 数据库 `Mac` *vs.* `iPhone` 模型添加新实体属性 `book` 实体 `Entity's Codegen` 状态 界面 ( *参见* `Interface`, 设置) `NSManagedObject` 关系添加 `string(forKey:)` 方法 `Strings` `Subclass` `Superclass` `Swift` 编译型语言 语言 符号 对象 基础框架 `HelloWorld` `LogFile` 调用 `swift` 方法 `Swift` `Class` 定义 实例方法 属性 `RadioStation` `type` 方法 `.swift` 文件 `Swift` 项目 `Switches` `Switch statement` `Boolean logic` 组合比较 比较日期 `Symbol navigator` `Synchronize function` `Synchronize method`

### T

`Test navigator` `Timeline area` `Transistors` `Type casting` `Type methods`

### U

`UITableViewController` `UITableView` 方法 `UITextView` 类型 `Unicode` `Unified Modeling Language (UML)` `UserDefaults` 对象 `User interfaces` 构建器 ( *参见* `Interface Builder`) 创建 `User interface (UI)` `Utility area`

### V

`Value orientated programming` `Variables` `Variables`, `playground` `Version editor` `ViewController.swift` 文件 `viewDidLoad` 函数

### W

`WalkAround` 应用 `Warnings` `WatchKit` 应用控制器类，设置 控制器文件，添加 `DetailController.swift` 详情视图场景 `InterfaceController` 类 `labels` 对象 输出口连接 `segue`，创建 `watchOS 5.0` `watchOS App` `Wireframe` 线框图 `Woodforest Mobile Banking` 应用

### X, Y, Z

`Xcode` `Apple Developer Program` `Blank iOS` 模板 `debugger` `documentation` `editor`/`result` 区域 `IDE` 安装 `Interface Builder` 启动 `navigator` 区域 `playground` 窗口 `user interface` 版本 `workspace` 窗口 `Xcode` 项目 应用运行 `button` 对象的标题，修改 连接菜单对话框 编辑器 `iPhone` 视图 `label` 对象 主屏幕 `object library` 对象的变量引用 输出口 模板 工具栏 `touch up inside` 事件 `utility` 视图，界面 `ViewController.swift` 文件 `XOR` 运算符
