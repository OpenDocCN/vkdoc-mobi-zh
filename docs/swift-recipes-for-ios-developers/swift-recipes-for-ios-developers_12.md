# 索引

## A
- `Alpha` 组件
- `AnimatedImageView`
- 动画视图（`UIView`）
- `UIView` 自身的属性
- 动画约束：通过更改约束实现布局变化、限制类型
- `Apple Map`
- 应用程序编程接口（API）
- 面积转换
- `Array`
- 异步函数
- `Async Wrapper`

## B
- `Base64` 编码
- 模糊效果
- 布尔转换

## C
- `CachedFile` 结构体
- `Caches` 目录
- 缓存大小验证
- 缓存文件：文件系统、内存
- `Canvas`
- `CGColor`
- `CGImage`
- `CIImage`
- 客户端-服务器模型
- 编码键（`Coding keys`）
- 复杂数据类型
- 内容裁剪
- 转换：布尔转换、数字转字符串、安全数字转换、字符串与数据
- `CoreImage`
- 信用卡验证
- 自定义个人资料图片视图
- 自定义可复用组件
- 自定义字符串插值
- 自定义过渡动画

## D
- 数据绑定
- 数据提取：数组、字典
- 数据处理
- 数据类型
- 日期与时间选择器
- 日期选择器弹窗
- 日期
- 日期选择对话框
- `decUser` 对象
- 反序列化：定义、对象
- 桌面服务器应用
- 字典
- 距离/长度转换
- `Double` 类型
- 下载文件：`Alamofire`、缓存、`URLSession`/`URLRequest`
- 缩小（`Downscaling`）
- `dropShadow` 属性
- 动态导航

## E
- 电子邮件输入验证器
- 禁止字符串中的表情符号
- `Encodable` 转数据与字典
- `Encodable` 转 `JSON`
- 错误对话框
- 可扩展标记语言（XML）：生成、解析

## F
- 淡入效果
- 淡出效果
- 文件格式
- `FileImageCache`
- 文件系统缓存系统
- 浮动前缀
- 格式化用户输入：银行卡号、电话号码

## G
- 使用 `CoreImage` 实现高斯模糊
- 使用 `GPUImage` 实现高斯模糊
- 通用问题弹窗
- 生成 `XML`
- `GET` 方法
- `GIF` 动画
- `Google Maps`
- `GPUImage` 框架
- 渐变（`Gradients`）

## H
- `Hashable` 类
- `Hashable` 协议
- 辅助函数
- `Hero` 动画：屏幕间飞行到 `UIViewController`
- `Hex` 编码
- 超文本传输协议（HTTP）

## I
- 图像缓冲区
- 图像缓存视图
- 图像上下文
- 图像裁剪
- 图像文件
- 图像缩放
- 整数索引
- ISO 8601 格式

## J
- JavaScript 对象表示法（JSON）：结构、可解码、编码数据、生成、解析

## K
- 键盘处理：更改布局、隐藏可滚动区域、文本框、用户离开屏幕、通知、滚动使文本框可见
- `Kingfisher`

## L
- `LazyVStack`
- 加载图像：文件、内存
- 本地化文件（含翻译）
- `LocalizedStringKey` 结构体、`NSLocalizedString` 宏、语法糖

## M
- 遮罩（`Mask`）
- `MD5` 哈希
- 内存缓存
- 合并图像
- `MKMapView`

## N
- 导航：返回、iOS 应用场景
- 导航栈：移除元素
- 非透明像素

## O
- 对象表示法

## P, Q
- 视差效果
- 视差头部：`UIScrollView`、视差功能、用户界面、`UITableView` 与 `UICollectionView`
- 解析 `XML`
- `PhoneNumberKit` 库
- 电话号码验证
- `Pin Pad` 用户界面组件
- 弹窗窗口：错误对话框、通用问题、选择日期和时间、简单文本问题
- 预乘阿尔法（`Premultiplied alpha`）
- 个人资料图片：处理、显示
- `@propertyWrapper`

## R
- 正则表达式
- 根视图控制器
- 圆角

## S
- 安全数字转换：`Double` 类扩展、`rounded()` 方法
- 盐值（`Salt`）
- 保存图像：文件、内存
- 场景
- 模式（`Schema`）
- 序列化：定义对象
- 阴影
- `SHA` 哈希
- `SimpleImageCache` 类
- `SizeThreshold` 字段
- `SnapKit`
- 特定视图控制器
- 标准过渡动画
- 字符串与数据
- `String` 类型
- 字符串内容分析：信用卡信息、出生日期、数字与字母、电子邮件地址、扩展、性别、姓名验证、密码、电话号码
- 字符串插值
- 后缀（`Suffixes`）
- `SwiftUI`：应用构建块、组合、控件、修饰符、自定义访客列表组件、自定义视图、画布组件、状态数据绑定、使用 `ViewModifier` 的样式、链式修饰符创建、`headertext`（无需 `UIKit`）、`UIKit` 组件
- `SwiftyGif`

## T
- 文本输入对话框
- `TextFieldWithPadding`
- 文本问题弹窗
- `TextViewWithPadding`
- 时间戳
- 过渡动画库
- 类型转换

## U
- `UIAlertController`
- `UIColor`
- `UIGraphicsGetImageFromCurrentImageContext` 函数
- `UIImage`
- `UIKit` 组件
- `UILabel`
- `UINavigationController`
- `UINavigationController` 导航栈、特定视图控制器
- `UIScreen`
- `UITableView` 视差效果
- `UITextField`：前缀、后缀
- `UITextField` 内边距
- `UITextView`
- `UITextView` 内边距
- `UIViewController`：动画、应用变换、计算变换、编码、创建与显示
- `UIView`：隐藏
- `UIView` 源视图
- `UIWindow`
- `UIWindow` 扩展
- 统一资源定位符（URL）
- 单位转换：面积单位、距离与长度单位、体积单位、重量单位
- 通用导航
- 通用个人资料图片组件
- 通用个人资料图片视图
- 通用 Windows 平台（UWP）
- `updateShadow` 方法
- 放大（`Upscaling`）
- URL 编码字符串
- URL 编码参数字典
- 用户实时输入
- UTF-8
- UTF-16

## V
- `ViewModifier`
- 体积单位转换

## W, X
- 重量单位转换

## Y, Z
- Yet Another Markup Language（YAML）
