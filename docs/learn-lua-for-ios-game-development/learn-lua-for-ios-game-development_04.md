# 第 3 章：文件操作

## 隐式函数

- `io.close([file])`
- `io.flush()`
- `io.input([file])`
- `io.lines([filename])`
- `io.open(filename[,mode])`
- `io.output([file])`
- `io.read(...)`
- `io.tmpfile()`
- `io.type(obj)`
- `io.write(...)`

## 显式函数

- `file:close()`
- `file:flush()`
- `file:lines()`
- `file:read([format])`
- `file:seek([whence][,offset])`
- `file:setvbuf(mode[,size])`
- `file:write(...)`

## 文件 I/O 在游戏开发中的应用

### 保存变量

- 获取数据
- 代码是如何工作的
- 潜在问题

### 将数据保存到变量

- 代码是如何工作的
- 潜在问题

### 将数据写入文件

### 保存表

### 动态变量

### 本章小结

