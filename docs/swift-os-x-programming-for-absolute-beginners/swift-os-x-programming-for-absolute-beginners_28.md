# 索引

## A

### 提醒
`alertStyle`、`customize`、`feedback`、`generic alert`、`icon`、`informativeText`、`messageText`、`myAlert`、`NSAlert`类、`sheets`、`AlertProgram`项目、`AppDelegate.swift`文件、`beginSheetModalForWindow`方法、`closure`、`IBAction`、`showAlert`方法、Swift 代码、`showsSuppressionButton`、`suppressionButton`、`And (&&)`逻辑运算符、`AppDelegate`声明、`applicationDidFinishLaunching`方法。

### 数组
- 添加项目
- 升序排列
- 数据类型
- 删除项目
- 降序排列
- 字典：添加项目、`addValueField`、`deleteKeyField`、删除数据、显示结果、`IBAction`方法、查询数据、检索和更新数据、Swift 文件、用户界面列表
- 检索数据
- 排序

### 助理编辑器

## B

### 按钮
- 添加图像
- 定义
- `IBAction`方法
- 修改文本：按钮修改标题属性
- 按钮：多个用户界面程序、弹出过程、位置属性、对齐文本和图像、声音过程、声音属性、选择类型

## C

### 计算函数

### 复选框
- 不同的`IBAction`方法
- 过程
- 属性
- 用户界面程序
- Xcode 首选项对话框

### 类创建
- 初始化方法
- 对象属性访问
- 计算属性
- `getters`和`setters`
- 属性观察器
- 最简单的类
- 变量和函数

### Cocoa 应用程序
- Cocoa 框架
- `button`类
- `cookie cutter`界面项
- `label`项
- `text field`项

### 常用逻辑运算符

### 计算机程序
- Cocoa 框架
- 事件驱动语言
- C 和 C++编程
- Swift 编写代码
- 面向对象编程：封装、继承、多态
- 程序员呈现
- 编程结构：视图-模型-控制器设计
- 编写指令

### 条件断点

### 连接检查器

### 约束，自动布局
- 删除/清除
- 编辑常量值
- 修改值
- 优先级值
- 过程
- Xcode OS X 程序
- UIProgram 窗口大小
- 放置约束：control-drag 方法、Pin 图标、Xcode 选择约束、窗口大小。(参见窗口大小, 用户界面)

## D

### 数据类型

### 日期选择器
- `IBAction`方法
- `IBOutlet`出现
- 过程
- 属性
- 类型
- 用户界面程序

### 调试区

### 调试
- 注释：注释选择
- 调试区
- 华氏和摄氏温度
- 逻辑错误
- `print`命令
- 运行时错误
- 语法错误
- Xcode 调试器。(参见 Xcode 调试器)

### 防御性编程
- 错误处理：`do-try-catch`语句、`enum`和`ErrorType`
- 函数
- 极值
- 可选链
- 类声明
- 数据类型

### 委托

### `didSet`属性观察器

### 基于文档的应用程序

### 文档大纲
- 工作过程

## E, F

### 编辑下拉菜单。参见下拉菜单

### 指数函数

### 扩展类
- 添加代码而不使用子类
- 添加属性和方法
- `getters`和`setters`
- 初始化器
- 属性观察器
- 字符串数据类型
- 结构

## G, H

### Getters

## I, J, K

### `IBAction changeCase`方法

### `IBAction`方法

### `If-else if`语句

### 继承
- `AppDelegate`类
- 多个类之间
- `className`
- 代码复制：`copyCat`类、`copyDog`类
- 菊花链效应
- 文档窗口
- OS X 程序中的`firstClass`
- 名称属性
- 目的
- `superClass`
- 使用协议
- 工作过程

### `Init`函数

### 检查器面板

### 界面生成器

### `InternalChange`函数

## L

### 对数函数

### 循环
- `do-while`循环：布尔值、运行 playground、结构
- 提前退出循环
- `for in`循环
- `for`循环
- `loopingProgram` `AppDelegate.swift`文件：数组计数、属性检查器、更改属性、定义变量、`IBAction`方法、`IBOutlet`、`init`方法、整型变量、消息标签、用户界面
- `while`循环：布尔比较、布尔值、结构

## M

### 操作数字
- 数学运算符：赋值运算符、常见运算符、数据类型、括号
- 前缀和后缀
- 数学函数：API 引用、计算函数、指数函数、对数函数、舍入函数、三角函数
- 参数：`IBAction`方法、`inout`、名称、返回值、简单函数
- 变量
- 字符串函数

### 方法创建
- 创建
- 设置
- `decrementByValue`方法
- `decrement`方法
- `Float`数据类型

### 多用户界面程序

## N

### 导航器面板

### 非 (`!`)逻辑运算符

## O

### 对象库
- 搜索方法
- 用户界面项：接受命令、显示和接受文本、分组、限制选择

### 面向对象编程
- 封装
- 继承
- 多态

### 对象通信
- 创建类。(参见类创建)
- 创建方法。(参见方法创建)
- 封装

### OS X 程序
- `AppDelegate.swift`文件：`applicationDidFinishLaunching`方法、文件选择
- `hitPoints`、`personClass.swift`文件
- `sheriffHitPoints`和`outlawHitPoints`
- `警长射击`和`歹徒射击`按钮
- `shootButton` `IBAction`方法
- `Tag`属性
- 用户界面
- 工作过程
- 与子程序对比

### 打开面板

### 运算符
- 布尔数据类型
- 比较运算符
- 逻辑运算符

### OS X 程序
- 对齐文本字段
- 属性检查器
- 创建
- `IBAction`方法
- `IBOutlet`
- 大小检查器
- Swift 文件
- 用户界面

### 或 (`||`)逻辑运算符

### OS X 人机界面指南

### OS X 程序
- `AppDelegate` `applicationDidFinishLaunching`函数
- `更改大小写`按钮
- Cocoa 框架：类文件、连接检查器、创建
- Cocoa 应用程序：基于文档的应用程序、框架及库、其他类别、系统插件
- 文档大纲
- 文档窗口：编辑器、颜色代码、剩余代码
- `uppercaseString`方法
- 用户界面设计：出现 `MyFirstProgram`图标、助理编辑器、属性检查器面板、蓝色指引线、连接 Swift 代码
- control-dragging
- `IBAction`方法
- `IBOutlet`
- `NSApplicationDelegate`协议
- `NSControl`
- `NSObject`
- `NSString`
- `NSTextField`类文件
- 对象库
- 弹出窗口
- 运行程序
- 大小检查器面板
- `uppercaseString`方法
- 用户界面出现
- 编写 Swift 代码

## P, Q

### 面板
- `NSOpenPanel`类
- `NSSavePanel`类
- 打开面板
- 保存面板

### 规划
- 标识
- 程序结构
- 用户界面.(参见用户界面设计)

### 多态
- `flyingObject`类
- `gameObject`类
- “`override`”关键字
- 覆盖属性
- 预防
- 工作过程

### 弹出按钮
- 添加过程
- 删除过程
- 双击菜单项
- 菜单项
- `NSMenu`类
- 对象库菜单项
- 过程
- Swift 代码过程

### 属性观察器

### 面向协议编程
- 采用多个协议
- 优势
- `Cat`协议：定义方法、定义、不同结构、扩展动物变量
- `CatType`协议：默认值、默认值、扩展通用数据类型、初始值、初始值、`protocol2Extend`实现、属性
- `ProtocolPlayground` Swift 代码变量

### 协议
- 添加 Swift 代码
- 通用用户界面
- 声明
- 定义属性和方法
- 继承：与`NSApplicationDelegate`
- 可选方法和属性
- 工作过程

### 下拉菜单
- 添加新命令
- 添加菜单项和下拉菜单标题
- 文档大纲
- 编辑
- 编辑命令
- 文件格式
- 帮助
- 退出命令
- Swift 代码
- 连接检查器面板
- 第一响应者图标
- `IBAction`方法
- `IBAction`方法菜单项
- `NSApplication`
- `NSResponder`类
- `NSView`
- `NSWindow`
- 保存命令
- 故事板视图
- 查看菜单
- 窗口
- `xib`/`storyboard`文件

## R

### 单选按钮
- `IBAction`方法
- `IBOutlet`出现
- 修改选择过程
- 行和列显示
- `selectedTag()`方法
- 透明属性
- Xcode 首选项对话框

### 舍入函数

## S

### 保存面板

### 场景
- 添加
- 与 segue 连接
- 显示初始
- 数据传递
- 移除

### 搜索文本字段

### 安全文本字段

### Segue
- 连接场景与
- 显示场景从
- 移除场景后
- `SelectedTag()`方法

### 集合
- 创建
- 定义
- insert 命令
- 操作 playground
- 查询
- remove 命令

### Setters

### `shootButton` `IBAction`方法

### 滑块
- `IBAction`方法
- `IBOutlets`出现
- 过程
- 属性
- 刻度标记外观
- 用户界面程序

### 标准编辑器

### 语句
- `enum`数据结构
- `if-else if`语句
- `if-else`语句
- `if`语句
- `switch`语句：布尔值、代码点和符号、列表值、多个值、运行

### 步入命令

### 步出命令

### 跳过命令

### 故事板
- 附加窗口
- 项目场景：添加、与 segue 连接、显示初始、数据传递、移除
- 删除 segue：连接场景与、显示场景从、移除场景后
- 视图控制器和视图
- 缩放
- `.storyboard`文件. 另见故事板
- 添加
- 选择

### 结构
- 定义
- 特性
- 属性
- 检索数据
- `StructureProgram` `AppDelegate.swift`文件：`IBAction`方法、用户界面

### 子程序

### Swift
- C 编程语言
- 注释创建
- 编译器
- 计算属性
- 数据类型转换
- `IBAction changeCase`方法
- `IBOutlet`变量
- 解释器
- Objective-C
- 可选变量
- playgrounds
- 类型别名
- Unicode 字符
- 变量和常量

### 符号断点

### 系统插件

## T

### 文本字段
- 组合框：数据源、内部列表、`NSComboBox`类、`NSTextField`类、用户界面
- `doubleValue`
- `floatValue`
- `intValue`
- 数字格式化器
- 文档大纲
- `IBAction`方法
- `IBOutlet`
- 行
- 本地化复选框
- 最小和最大复选框
- 修改弹出按钮菜单
- 修改弹出按钮
- `NumberProgram`
- 对象库
- 显示属性检查器面板
- 搜索文本字段
- 安全文本字段
- `stringValue`
- `TextProgram`
- 令牌字段

### 三角函数

### 断点故障排除

### 元组
- 数据访问
- 定义
- 特性

### 类型别名

## U

### 用户界面约束，自动布局。参见约束，自动布局

### 用户界面
- 拖放项目
- 界面生成器
- 主要用户界面
- 对象库.(参见对象库)
- OS X 人机界面指南
- `.storyboard`文件视图
- Xcode
- `.xib`文件.(参见`.xib`文件)

### 用户界面设计
- 约束冲突
- 压缩
- 固定值
- 优先级问题
- 定义
- 纸和铅笔规则
- 软件 Balsamiq Mockups
- 博客
- 免费的有用信息
- 操作“精简版”
- 营销
- 发布视频
- PowerPoint
- 社交网络
- 堆栈视图
- 故事板参考
- control-dragging
- 创建场景和 segue
- 实用工具面板

## V

### 视图-控制器通信

### 视图-模型-控制器设计

## W

### While 循环

### `willSet`属性观察器

### 窗口大小，用户界面
- 窗口初始位置
- 最小和最大大小
- OS X 程序大小更改方法
- 大小检查器
- 小窗口
- `.storyboard`文件
- 视图控制器窗口位置和最小/最大大小
- `.xib`文件

## X, Y, Z

### Xcode
- 助理编辑器：浏览、书签、展开三角形
- 文档库
- 文档窗口：类文件、`NSControl`、`NSTextField`、快速帮助、`stringValue`
- Cocoa 框架：`button`类、`cookie cutter`界面项、`label`项、`text field`项
- 代码补全
- 命令创建
- 选择文件/项目
- 包含 Swift 代码
- 包含用户界面
- 项目导航器
- 自定义用户界面功能
- 面板
- 调整窗口大小
- OS X 程序.(参见 OS X 程序)
- 运行程序
- 搜索文档
- 标准编辑器

### Xcode 调试器
- 断点
- `applicationDidFinishLaunching`函数
- 断点导航器
- `C2F`函数
- 条件断点
- 定义
- 执行
- 管理断点
- 步入命令
- 步出命令
- 跳过命令
- 符号断点
- 故障排除
- 变量监视

### Xcode 窗口

### `.xib` 文件
- 界面生成器：添加
- 视图控制器
- Swift 文件
- 显示多个文件
